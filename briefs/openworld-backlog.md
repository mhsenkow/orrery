# ORRERY — open world

**400 items.** Generated from `scripts/openworld.mjs` — edit that file, not this one, then run `node scripts/openworld.mjs`.

The machinery that keeps the palette open: world definitions as authored data, a material layer stack, palettes with provenance, an artefact detector that looks at pixels, and per-world art direction as a unit.

The measurement this pass starts from: the data layer landed — **78 authored rows** across five files with compilers and 35 provenance tags — and **64 hand-written per-body functions** still decide what a world looks like. `planetLook.js` alone carries 30 PAINT kinds built from 37 lambdas and 85 `lerp` calls, and `applyPlanetLook` sets exactly two fields. The surface is one byte deep. One hundred and fifty-four colour triples remain in code. And nothing in the test suite renders anything at all.

Kind: **76** model, **107** picture, **204** measurement and proof. Effort is S/M/L. Impact is 1–3.

## What the audit found

- **The data layer landed and it is real.** `vr/data/worlds/` holds 24 materials, 11 cover kinds, 12 processes, 21 landforms and 10 column layers — 78 authored rows — each with a compiler on the pattern `lifegrammar.mjs` proved, and `substrates.json` and `cover.json` carry 35 provenance tags between them. `kinds.json` lists all 120 bodies with their resolved kind and is what keeps the 40-Io result from returning. This is a genuine foothold and the worldspace pass was right to call it one.
- **Appearance is data; kind selection is a table plus axes.** PAINT, stamps and ice-shell maps compile from JSON. Named kinds are `kindRules.json`. A new body with an existing shape is a JSON review. Axis fallbacks (ice → europa, heatpipe → Io) stay in `planetKind` because they are physics, not a body list.
- **The surface is one byte deep.** `W.substrate` is a `Uint8Array` holding the top material and nothing else, `W.rock` is a second byte array with eight Earth rock types, and `columnAt` builds a stack on demand from **one recipe per world** cached in `W._columnRecipe`. So every cell on a planet has the same layer sequence with per-cell depths. There is no per-cell stack, which means nothing can bury anything, expose anything or accumulate — and `layers.js`, which does have a real non-destructive stack, only ever operates on height.
- **A colour is one triple with no light in it.** `substrates.json` gives each material one `rgb`, one `albedo` and a tag — no ramp against depth, wetness, grain size, age or angle, and no dependence on the illuminant. Meanwhile 154 hardcoded triples remain in code: 76 in `lifeColour.js`, 67 in `planetLook.js`, 11 in `render.js`. `sensory.js` already integrates Planck spectra against band transmission for eyes and no surface colour has ever asked it anything.
- **Nothing in the test suite renders anything.** `pictureStats` is good work and it measures *fields* — height, temperature, precipitation, life, moisture. `readPixels`, `toDataURL` and any headless GL context appear nowhere in `vr/sim/test.mjs`. `capture-site.mjs` drives Playwright and screenshots four site pages, not worlds. The realism backlog's central finding — that the picture has never been measured — is still literally true; what has been measured is the data behind the picture.
- **The artefact detector runs on one world out of a hundred and twenty.** `pictureStats(Wcont)` runs on a single continental test world inside one test block, with 14 assertions. That is 0.8% of the catalogue. The fault this entire programme exists to catch — 40 bodies resolving to Io — is invisible at that sample size, and it went unnoticed for months.
- **Art direction is two lambdas.** Lighting, exposure, atmosphere, sky colour, limb, haze layering, detail grammar, sound, named features and the camera that introduces a world are decided elsewhere by code that does not know which world it is drawing, or not decided at all. `site/img/` holds four screenshots and all four are Earth.
- **Nothing names a landform.** `W._plateNames` names plates. Sputnik Planitia, Caloris, Valles Marineris, the tiger stripes and the Great Red Spot are the reason anyone recognises those bodies, they are published for every Solar System world, and none of them exists in the product. `audio.js` layers one soundscape and it is Earth's everywhere.

## Where the machinery actually is

- **Construct 16 is a foothold, and its own note says so.** The join landed: `definitions.json` + `worldDef.js` tie column, bedrock, cover, paint and features per body; `node scripts/data.mjs` is the one-build compiler and scoreboard (`newworldcost 0`, paint/stamps/shell in data). Remaining: per-cell deposition in play, pixel baselines, illuminant art direction, schema CI.
- **Construct 17 has not started, and it blocks the most.** The column recipe is good work aimed at a different problem — describing a world's layering rather than storing a cell's. Deposition, burial, exposure, differential erosion, the fossil record, stratigraphic colour banding and the surface backlog's `strata` item are all waiting on a per-cell stack that does not exist.
- **Construct 18 is a third done and the remaining two thirds are the hard part.** Materials have colours with provenance, which is the part that needed authoring discipline. What is left is the part that needs physics: a ramp instead of a triple, a spectrum instead of a ramp, and an illuminant so the same rock is a different colour under a different star.
- **Constructs 19 and 20 are the same missing machine.** A render harness produces both the metrics and the contact sheet. Building it once serves the artefact detector, the art-direction review, the world cards, the picker thumbnails and the README images. Neither construct is reachable without it and both are cheap once it exists.
- **The measurement discipline is working where it exists.** `kinds.json` is committed and tested against a live audit, so the Io result cannot return silently. `pictureStats` has 14 assertions that caught real regressions. The generate-reset test added this session found four separate leaks including a vent field that carried 173 vents from one planet to the next. The pattern is proven; it is the coverage that is thin.

## The critical path

The capabilities the largest number of other items are waiting on.

| Capability | Item | Unblocks |
|---|---|---|
| `artdir` | 321. Art direction is two lambdas | 39 items |
| `headlessgl` | 241. Nothing in the test suite renders anything | 36 items |
| `wschema` | 21. Write the schema down | 25 items |
| `contact` | 381. The contact sheet | 25 items |
| `illum` | 201. Colour is reflectance times illuminant | 23 items |
| `spectrum` | 181. Carry a spectrum where one exists | 22 items |
| `fleetrun` | 281. One world is measured; there are a hundred and twenty | 22 items |
| `colstack` | 81. A per-cell material stack | 21 items |
| `deposit` | 101. Deposition writes a layer | 20 items |
| `stackops` | 121. Six operations, defined once | 20 items |
| `stackbudget` | 141. State the memory budget before writing it | 20 items |
| `baseline` | 301. Commit the baselines | 19 items |
| `palview` | 221. A palette preview page | 18 items |
| `matramp` | 161. A material needs a ramp, not a triple | 17 items |
| `onebuild` | 41. One build that runs every compiler | 15 items |
| `placenames` | 361. Named features, per world | 15 items |
| `worldrec` | 2. A world object that ties the five files together | 10 items |
| `edgemetric` | 261. Axis-aligned edge runs | 8 items |

## The data layer replaced the per-body appearance functions

PAINT, stamps and ice-shell maps are authored JSON with compilers and fixtures. Named kinds are `kindRules.json`. `node scripts/data.mjs` prints `dataratio` and `newworldcost` (named-kind regexes left in `planetKind`).

| # | Item | What and why | Kind | E | I |
|---|---|---|---|---|---|
| 1 | **Count what is data and what is code, and print it** <br>gives `dataratio` | Landed. `node scripts/data.mjs` prints authored rows against remaining per-body functions (`land*`/`stamp*`/`paint*`). The migration scoreboard, not a feeling. | Prove | S | 3 |
| 2 | **A world object that ties the five files together** <br>gives `worldrec` | `substrates.json`, `cover.json`, `processes.json`, `landforms.json` and `columns.json` are five parallel tables with no record joining them to a world. A world definition is the join: which materials, which processes at which rates, which landforms, which cover, which column. Without it the data describes a vocabulary and never a sentence. | Model | L | 3 |
| 3 | **Retire the PAINT table into data** <br>gives `paintdata` <br>needs `worldrec` | Landed. Thirty kinds live in `paint.json`, compile through `paint.mjs`, and run in `paintEval.js`. `planetLook.js` binds ramps. Earth / Daisyworld / temperate generic keep Whittaker. | Draw | L | 3 |
| 4 | **Retire the stamps into the landform grammar** <br>needs `worldrec` | Landed. Rocky hypsometry is `stamps.json` ops in `stampApply.js`. Ice-shell maps are fill/add/set of masks (bowl, cycloid, ridged pit, south stripe) in `shellApply.js`, not `opEuropa`. Fixtures stay the validation cases. A genuinely new primitive still needs an interpreter op. | Model | L | 3 |
| 5 | **`kinds.json` is an audit table, not a definition** | It is 27 KB listing all 120 bodies with their resolved kind and the term that decided it, regenerated by `scripts/worldspace.mjs`, and it is exactly the right artefact — it is what keeps the 40-Io result from returning. It is not a world definition and the README should say which of the six files is which. | Prove | S | 2 |
| 6 | **Provenance on the join, not just on the rows** <br>needs `worldrec` | Basalt is measured. That Mars's surface is basalt-plus-dust is an inference. That a given exoplanet is basalt at all is a guess. Tagging the material and not the assignment launders the guess. | Prove | M | 3 |
| 7 | **Which worlds have a complete definition** <br>needs `worldrec` `dataratio` | A coverage report per body: axes computed, substrate assigned, processes selected, landforms available, cover model, column recipe, palette, art direction. Most bodies will show gaps and the gaps are the work list. | Prove | M | 3 |
| 8 | **The data should be able to say "unknown"** | For 94 exoplanets almost every field is an extrapolation. A schema that requires a value forces a fabrication; one that admits absence lets the renderer draw something deliberately provisional, which the worldspace pass argued for and nothing implements. | Model | M | 3 |
| 9 | **Version the data** | `lifeGrammar.js` carries a `GRAMMAR_VERSION`. The world tables do not. A save that references material 17 needs to know which table that was. | Model | S | 2 |
| 10 | **One directory, one README, one contract** | `vr/data/worlds/README.md` exists and is good. It should also state the invariant: nothing in `vr/sim/` may name a specific body except as a validation case. | Prove | S | 3 |
| 11 | **A lint that fails on a new body name in sim code** <br>needs `dataratio` | The regression risk for this whole construct is somebody adding `if (kind === 'triton')` because it is faster than editing JSON. A grep-level check in the test suite makes that a deliberate act rather than an accident. | Prove | M | 3 |
| 12 | **Keep the named-body functions as fixtures** | Landed. `stampFixtures.js` and `shellFixtures.js` keep the old heightfields. Tests assert data ≈ fixture for Mars / Mercury / Moon and Europa / Enceladus / Titan / Pluto / Miranda. | Prove | M | 3 |
| 13 | **Data should be diffable** | A 27 KB generated JSON with 120 rows changes constantly. Stable key order, stable number formatting and one row per line make a review possible; a re-serialised blob makes every change look total. | Prove | S | 2 |
| 14 | **Separate authored data from generated data** | `vr/data/worlds/` currently mixes both — five authored tables and one generated audit. Two directories, or one naming convention, so nobody hand-edits a generated file. | Prove | S | 3 |
| 15 | **The catalogue and the definitions must agree** | `validateCatalogueWorlds` exists. Extend it so every one of the 120 bodies resolves to a definition that exists, and the build fails otherwise. | Prove | M | 3 |
| 16 | **Loading cost of the data layer** | Six JSON files compiled into runtime modules, imported at boot. Measure the parse and evaluation cost before adding palettes and art direction to it. | Prove | S | 2 |
| 17 | **A world definition should be shareable** | The seed-word system encodes a world in four words. A full definition is bigger, and a URL that carries a whole invented world is the natural endpoint of making it data. | undefined | M | 2 |
| 18 | **Let a player edit a definition** <br>needs `worldrec` | The strongest argument that something is data is that it can be changed without a build. A definition editor in the Lab — change the dominant volatile, watch the world change — is what makes the layer visible. | undefined | L | 3 |
| 19 | **Do not let the join become 120 files** <br>needs `worldrec` | Stated in the worldspace pass and worth restating because it is the failure mode: one JSON per body is 64 lambdas with a different extension. The join must be a small set of rules that *select* from the tables, with per-body overrides as the exception. | Model | M | 3 |
| 20 | **A migration order** <br>needs `dataratio` | Sixty-four functions is a long list. Do them by how many catalogue bodies each serves: the `io` and `mars` fallbacks first because they cover 58 bodies between them, and Iapetus last because it covers one. | Prove | S | 3 |

## What a world definition would have to say

A definition is not a list of properties, it is a contract: which axes place this world, which substrate mixture its surface is, which processes run and at what rates, which landforms those processes can make, which cover accumulates, what the column looks like, what it is lit by, and how confident we are about each of those. Today the first six exist in five separate files with no object tying them to a world, and the last two do not exist at all.

| # | Item | What and why | Kind | E | I |
|---|---|---|---|---|---|
| 21 | **Write the schema down** <br>gives `wschema` | Six tables exist with no document saying what a world definition is. Write it as a schema — required fields, optional fields, defaults, units, allowed references — and generate the validator from it rather than hand-writing checks per compiler. | Model | M | 3 |
| 22 | **Axes place the world; the definition describes it** <br>needs `wschema` | `worldAxes` computes seven physical numbers. Those select a definition; they are not the definition. Keeping the two separate is what stops the taxonomy from becoming a lookup table, which the worldspace pass named as the failure mode. | Model | M | 3 |
| 23 | **Selection rules, not assignments** <br>needs `wschema` | "A world with this gravity, this dominant volatile and this interior state gets these materials" is a rule that covers worlds nobody has named. "Pluto gets nitrogen ice" is an assignment that covers one. | Model | L | 3 |
| 24 | **Overrides, and a rule about when they are allowed** <br>needs `wschema` | Some bodies genuinely are exceptions — Iapetus's two-tone surface is a dust-sweeping accident of its orbit. Allow overrides, require a `why`, and count them, because the count is the honest measure of how well the rules work. | Model | M | 3 |
| 25 | **Units on every quantity** <br>needs `wschema` | `substrates.json` has `density` 2900, `meltK` 1473, `k` 2.0, `thermalInertia` 2200, `strength` 250. Four of those five units are guessable and none is stated. The life grammar states them; this should too. | Prove | S | 3 |
| 26 | **Ranges and sanity bounds** <br>needs `wschema` | A validator that only checks references will happily accept an albedo of 4. Bounds per field turn the compiler into something that catches an author's slip, which is the main thing an author wants from it. | Prove | S | 3 |
| 27 | **A definition needs a confidence, not just tags per row** <br>needs `wschema` | Three levels — measured, inferred, invented — at the level of the whole world, so the interface can say "most of this world is extrapolated" without summing 30 tags. | Prove | M | 3 |
| 28 | **Timescales belong in the schema** <br>needs `wschema` | `processes.json` has 12 processes. A process without a rate is a label. Rates in metres per million years, with the gravity and material dependence stated, are what let the same process produce different landscapes on different worlds. | Model | M | 3 |
| 29 | **The schema should carry the epoch axis** <br>needs `wschema` | The worldspace pass established that a body at a different time is a different world. A definition that cannot express "Mars, 3.5 Ga" will be copied and edited, which is how 120 files happen. | Model | M | 3 |
| 30 | **Inheritance between definitions** <br>needs `wschema` | Icy moons share most of their definition. A base plus a diff is how this stays small; a flat table is how it stops being maintainable at 120 entries. | Model | M | 3 |
| 31 | **Reference by id, never by name** <br>needs `wschema` | The whole reason `planetKind` produced 40 Ios is that identity was a string match. Ids in the data, validated at compile time, are the structural fix. | Model | S | 3 |
| 32 | **Say what a field is for** <br>needs `wschema` | `substrates.json` carries a `why` on every material and it is the best thing about the file. Extend the convention: every field in the schema gets a sentence, because that sentence is the documentation nobody writes separately. | Prove | S | 3 |
| 33 | **Schema for the things that are not yet data** <br>needs `wschema` | Palette, art direction, sound and named features are all in this document and none is in the schema. Design their shape now, even if they land later, or the schema will be retrofitted twice. | Model | M | 3 |
| 34 | **Machine-readable, so the tooling is cheap** <br>needs `wschema` | A JSON Schema file lets an editor autocomplete, a CI step validate, and a doc page generate itself. `world-record.schema.json` already exists in `vr/data/` as a precedent. | Prove | M | 2 |
| 35 | **A definition should round-trip** <br>needs `wschema` | Compile it, run a world from it, serialise that world, reload it, and get the same world. The life pass asserts this for genomes and it is the only real proof that a definition is complete. | Prove | M | 3 |
| 36 | **Deprecation, because tables change** <br>needs `wschema` | When a material is removed, saves that reference it have to do something defined. A deprecation field and a migration path are cheaper to add now than to retrofit after the first save breaks. | Model | M | 2 |
| 37 | **Keep the schema smaller than the data** <br>needs `wschema` | If the schema needs a field for every special case, the special cases are the model. A schema growing faster than the table is the signal that the selection rules are wrong. | Prove | S | 2 |
| 38 | **One page that documents the whole layer** <br>needs `wschema` | Generated from the schema: every table, every field, every unit, every provenance tag, with counts. It is the document that lets somebody else contribute a world. | Prove | M | 3 |
| 39 | **Validate against the real bodies first** <br>needs `wschema` | If the schema cannot express Titan — organic sand over ice bedrock over an ammonia ocean, methane hydrology, a haze that hides the surface — it is not ready for the 94 worlds nobody has seen. | Prove | M | 3 |
| 40 | **Then validate against a world nobody has built** <br>needs `wschema` | A hot super-Earth with a supercritical CO₂ ocean and a silicate atmosphere. If the schema handles it without a new field, the selection rules are doing their job. | Prove | M | 3 |

## Compilers, validation and the build

Five compilers exist — `substrates.mjs`, `cover.mjs`, `processes` and `landforms` via `landgram.mjs`, `columns.mjs` — on the pattern `lifegrammar.mjs` proved, and they validate cross-references. What is missing is the thing that makes a data layer trustworthy: one build that runs them all, fails on a broken reference, and refuses to ship a world whose definition is incomplete.

| # | Item | What and why | Kind | E | I |
|---|---|---|---|---|---|
| 41 | **One build that runs every compiler** <br>gives `onebuild` | Five compilers exist and each is run by hand. `npm run data` that runs all of them, fails on any error, and leaves the tree clean is the difference between a data layer and five scripts. | Prove | S | 3 |
| 42 | **Fail the test suite if the generated modules are stale** <br>needs `onebuild` | `lifeGrammar.js`, and the world tables, are generated and committed. Nothing checks that they match their sources, so an edit to the JSON that nobody compiled is invisible until it is not. | Prove | M | 3 |
| 43 | **Cross-file validation in one place** <br>needs `onebuild` | `landforms.json` references materials and processes; `columns.json` references materials; `cover.json` references materials. Each compiler validates its own file. One pass over the whole set catches the references that cross. | Prove | M | 3 |
| 44 | **The validator should report, not just throw** <br>needs `onebuild` | `lifegrammar.mjs` prints every problem and then exits non-zero, which is the right shape. Match it everywhere: an author wants the whole list, not the first error. | Prove | S | 3 |
| 45 | **Freeze the output** | The compilers emit `Object.freeze`d tables, which is right. Assert it in a test, because a mutable shared table is a bug that appears months later as a world that changed itself. | Prove | S | 2 |
| 46 | **Emit derived indexes, not lookups at runtime** | `ORGAN_BY_ID` and `BAND_BY_ID` in the life grammar are the pattern. Every table wants its id map emitted rather than built on first use. | Model | S | 2 |
| 47 | **Print a summary the reviewer can read** | `lifegrammar.mjs` prints the morphospace size. The world compilers should print counts, coverage and the number of overrides, so a diff on the summary line says whether the change was structural. | Prove | S | 3 |
| 48 | **A dry-run mode** <br>needs `onebuild` | Validate without writing. It is what a pre-commit hook needs and it is three lines. | Prove | S | 2 |
| 49 | **Deterministic output** <br>needs `onebuild` | Same input, same bytes. Sorted keys, fixed precision. Without it every regeneration is a diff and reviewers stop reading them. | Prove | S | 3 |
| 50 | **The compiler should refuse an incomplete world** <br>needs `onebuild` `worldrec` | A body with no substrate assignment should fail the build, not render as generic. That is the whole argument of the worldspace pass expressed as a build step. | Prove | M | 3 |
| 51 | **Warn on unused rows** | A material nothing selects, a landform no process makes, a cover kind no world uses. Unused rows are either dead or a missing rule, and both are worth surfacing. | Prove | S | 2 |
| 52 | **Generate the documentation** <br>needs `wschema` | The tables carry `why` on every row. A generated reference page is free and it is the artefact that makes the layer contributable. | Prove | M | 3 |
| 53 | **Compile-time cost of the whole set** <br>needs `onebuild` | Six files, five compilers. Measure it, because this runs on every data change and a slow build is a build people skip. | Prove | S | 1 |
| 54 | **Source maps back to the JSON** | When a runtime error names material 17, the developer wants the line in `substrates.json`. Emitting the source row index costs nothing. | Prove | S | 1 |
| 55 | **A schema-driven validator** <br>needs `wschema` `onebuild` | Five hand-written validators drift. One generated from the schema does not, and it is the difference between a convention and a contract. | Prove | M | 3 |
| 56 | **Test the compilers themselves** <br>needs `onebuild` | Feed each a known-bad file and assert it fails with the right message. The `lifegrammar.mjs` validator caught a real error on its first run; nothing proves it still would. | Prove | M | 3 |
| 57 | **Keep the runtime module free of build concerns** | The emitted module should be data and index maps, nothing else. Every helper that creeps into it is a thing that cannot be swapped for a different table. | Model | S | 2 |
| 58 | **Support a partial rebuild** <br>needs `onebuild` | Editing one material should not regenerate six files. It matters for the diff more than for the seconds. | Prove | S | 1 |
| 59 | **The build should run in CI** <br>needs `onebuild` | Everything above is a convention until something enforces it on a machine that is not the author's. | Prove | M | 3 |
| 60 | **One command that reports the state of the data layer** <br>needs `onebuild` `dataratio` | Rows per table, coverage per body, overrides, unused rows, provenance mix, and the data-to-code ratio. The review surface for this entire construct. | Prove | M | 3 |

## Adding Pluto should be a JSON file and a review

Named kinds are `kindRules.json`. Paint, stamps and ice-shell maps are data. `newworldcost` is leftover name-regexes in `planetKind`.

| # | Item | What and why | Kind | E | I |
|---|---|---|---|---|---|
| 61 | **Count the code sites a new world still needs** <br>gives `newworldcost` | Landed as a generated metric. Named kinds, paint, stamps and ice-shell maps are data; `newworldcost` is remaining name-regexes in `planetKind` (0). Axis fallbacks stay in code because they are physics. | Prove | S | 3 |
| 62 | **A worked example, end to end** <br>needs `newworldcost` | Pick a body that is not in the catalogue, add it entirely through data, and write down every place the attempt failed. That list is the real backlog for this category and it will be shorter and more specific than anything written in advance. | Prove | M | 3 |
| 63 | **A template definition** <br>needs `wschema` | A commented skeleton with every field, its unit and its provenance slot. It is the single cheapest thing that makes contribution possible. | Prove | S | 3 |
| 64 | **Errors an author can act on** <br>needs `onebuild` | "material `nitrogenIce` not found — did you mean `n2Ice`?" is a different experience from a stack trace. The compiler already knows the valid set. | Prove | S | 3 |
| 65 | **Preview without a full run** <br>needs `newworldcost` | A definition should be renderable to a thumbnail in a second, not by launching the app and navigating to a world. Authoring loops are won or lost on this. | Draw | M | 3 |
| 66 | **Show what a definition implies** <br>needs `newworldcost` | Given a definition, print the derived consequences — liquid window, viable senses, available processes, expected landforms. Most authoring errors are visible in that list before anything is rendered. | Prove | M | 3 |
| 67 | **Copy an existing world as a starting point** <br>needs `wschema` | Inheritance makes this safe. Without it, copying is duplication and duplication is how the table becomes 120 files. | Prove | S | 2 |
| 68 | **A contribution guide** <br>needs `wschema` | What a world needs, what evidence is expected for a `measured` tag, and what happens to a world that is mostly invented. The project has this discipline for planetary parameters and not for worlds. | Prove | M | 3 |
| 69 | **Review as a diff of pictures** <br>needs `contact` | A pull request that adds a world should show the thumbnail. This is the review surface that makes an art-direction change discussable. | Prove | M | 3 |
| 70 | **A world that fails review should say why** <br>needs `newworldcost` | Too close to an existing world, missing provenance, out-of-range values, no named feature. Machine-checkable review criteria are what let this scale past one author. | Prove | M | 2 |
| 71 | **Let the app write a definition** <br>needs `newworldcost` | A player who builds a world in the sandbox should be able to export it as a definition. It is the shortest path from playing to contributing. | undefined | M | 3 |
| 72 | **Import a definition at runtime** <br>needs `newworldcost` | Paste JSON, get a world. It makes the data layer testable by anyone and it is how somebody proves a schema gap. | undefined | M | 2 |
| 73 | **Name the authorship** | Who wrote this definition and against what sources. The catalogue credits the NASA archive; the worlds should credit their authors. | Prove | S | 2 |
| 74 | **Definitions for the invented rulesets too** | Vermis, Selene, Ares and Daisyworld predate all of this and live in `rulesets.js`. They should go through the same door or they are a permanent exception. | Model | M | 3 |
| 75 | **A definition for the unknown case** | The honest default for a world with three measured parameters. It should look provisional on purpose, and it is the definition 94 bodies will actually use. | Model | M | 3 |
| 76 | **Authoring is where provenance is decided** <br>needs `wschema` | The tag is easy to add and easy to inflate. A `measured` tag should require a source field, which makes the honest thing the easy thing. | Prove | S | 3 |
| 77 | **A changelog per world** | When a world's appearance changes, somebody should be able to find out why. This is the thing that makes 120 worlds maintainable rather than 120 things nobody dares touch. | Prove | M | 2 |
| 78 | **Bulk edits should be possible** | "Every ice moon gets a slightly bluer ice" is a reasonable art direction change and today it is 11 file edits. Inheritance and shared palettes make it one. | Prove | M | 2 |
| 79 | **A definition should be smaller than the code it replaced** <br>needs `dataratio` | If Pluto's JSON is longer than `paintPluto`, the schema is carrying the wrong things. Track the ratio per migrated body. | Prove | S | 2 |
| 80 | **The success condition, stated** <br>needs `newworldcost` | Adding a world touches zero files under `vr/sim/`. Until then this construct is open, whatever the row counts say. | Prove | S | 3 |

## The surface is one byte deep

`W.substrate` is a `Uint8Array` — one index per cell into the material table, the top material and nothing else. `W.rock` is a second `Uint8Array` with eight Earth rock types. `columnAt` builds a stack on demand from **one recipe per world** cached in `W._columnRecipe`, so every cell on a planet has the same layer sequence with per-cell depths. There is no per-cell stack anywhere, which means nothing can bury anything, expose anything, or accumulate.

| # | Item | What and why | Kind | E | I |
|---|---|---|---|---|---|
| 81 | **A per-cell material stack** <br>gives `colstack` | `W.substrate` is a `Uint8Array` holding the top material and nothing else; `columnAt` builds a stack on demand from **one recipe per world** cached in `W._columnRecipe`. So every cell on a planet has the same layer sequence and the surface is one byte deep. A real stack is per cell: an ordered list of (material, thickness) that processes read and write. | Model | L | 3 |
| 82 | **Fixed depth first, variable later** <br>needs `colstack` | Eight layers of one material byte and one thickness float is 40 bytes a cell — 2.2 MB at N=96. Start there, because a fixed stack is a typed array and a variable one is an allocation problem, and the fixed version already unlocks every process in this category. | Model | M | 3 |
| 83 | **The recipe becomes the initial condition** <br>needs `colstack` | `columnAt`'s recipe is good work and it is exactly the right thing to *stamp with*. Run it once per cell at generate to fill the stack, then let processes change it. Nothing about the recipe is wasted; it stops being the answer and becomes the starting point. | Model | M | 3 |
| 84 | **Thickness in metres, everywhere** <br>needs `colstack` | `columnAt` already returns `depthKm`. Keep one unit through the whole stack and state it, because the surface pass found three different implied scalings of one quantity in the field atlas and this is the same trap. | Model | S | 3 |
| 85 | **The top of the stack is the surface** <br>needs `colstack` | `W.substrate` becomes derived — the material of layer zero — rather than an independent field that can disagree with the column. One writer, as the biosphere architecture insisted for `lifeClass`. | Model | M | 3 |
| 86 | **`W.rock` is the Earth stack in disguise** <br>needs `colstack` | Eight Earth rock types in a parallel byte array, written by the tectonics and extinction code. Once the stack exists, `rock` is layer zero on an Earth-like world and the two arrays stop being able to contradict each other. | Model | M | 3 |
| 87 | **A layer needs an age** <br>needs `colstack` | `W.age` is one number per cell. A layer has its own age, and the difference between the top layer's age and the one below it is an unconformity — which is how a geologist reads a cliff. | Model | M | 3 |
| 88 | **Mixtures within a layer** <br>needs `colstack` | Mars is basalt plus dust plus salt plus ice, not four layers. A layer wants a dominant material and a minor fraction, which is two bytes instead of one and covers most real surfaces. | Model | M | 2 |
| 89 | **Porosity, because it changes everything mechanical** <br>needs `colstack` | Regolith is 40% void. It changes density, thermal conductivity, strength and how much volatile a layer can hold. It is one float and it is the difference between rock and rubble. | Model | M | 2 |
| 90 | **The stack under the ocean** <br>needs `colstack` | Sediment on the abyssal plain is the reason it is flat, which the landscape pass measured as half the planet at one elevation with the wrong cause. Ocean cells need a stack as much as land cells. | Model | M | 3 |
| 91 | **The stack under an ice shell** <br>needs `colstack` | Ice, ocean, high-pressure ice, rock. `columnAt`'s recipes already express this per world; per cell it becomes a shell thickness that varies — which is what makes Enceladus's south pole different from its equator. | Model | M | 3 |
| 92 | **A stack for a world with no surface** <br>needs `colstack` | The worldspace pass wants pressure as the vertical coordinate on a giant. That is the same data structure with a different axis, and designing them together is cheaper than twice. | Model | M | 2 |
| 93 | **Isostasy should integrate the column** <br>needs `colstack` | `applyIsostasy` uses a crust thickness and a density. With a stack it is a sum over layers, and an ice sheet, a sediment pile and a lava flow all load the crust correctly and for the same reason. | Model | M | 3 |
| 94 | **Thermal conductivity through the stack** <br>needs `colstack` | `substrates.json` carries `k` and `thermalInertia` per material and nothing integrates them. The surface temperature response depends on the top few centimetres and the geothermal gradient depends on the whole column. | Model | M | 2 |
| 95 | **The stack decides erodibility** <br>needs `colstack` | `erodeFactor` reads the top material. With a stack, erosion cuts down into the next one and the rate changes — which is the mechanism behind every mesa, cuesta and waterfall. | Model | M | 3 |
| 96 | **Save the stack** <br>needs `colstack` | A quantised heightfield is in the save format. A stack is bigger and compresses well — run-length by layer is the natural encoding since most cells have the same sequence. | Model | M | 2 |
| 97 | **A stack inspector** <br>needs `colstack` | `coreSample` and `formatColumnAt` exist and read the recipe. Pointed at a real per-cell stack they become the instrument that explains a landscape, which is what the geology backlog wanted from a drill. | Draw | M | 3 |
| 98 | **Do not let the stack drift from the picture** <br>needs `colstack` | The surface backlog's rule: one description of a cell consumed by every view. A stack whose top material is not what the shader paints is a new class of the same bug. | Draw | M | 3 |
| 99 | **Layer count as a diagnostic** <br>needs `colstack` | A world whose cells all have three layers has had no history. Mean layer count per world is a one-number summary of how much has happened. | Prove | S | 2 |
| 100 | **Assert the known columns per cell** <br>needs `colstack` | The worldspace pass asserted Europa, Titan and the Moon at the world level. At cell level the assertion gets sharper: Enceladus's south pole shell is 5 km and its equator is 30. | Prove | M | 3 |

## A stack is a history, or it is a texture

The point of layers is not that a surface is made of several things — it is that the order records what happened and in what sequence. Sediment over lava over impact melt is a story. The column recipe cannot hold a story because it is a property of the world, not of the cell, and it is recomputed rather than accumulated.

| # | Item | What and why | Kind | E | I |
|---|---|---|---|---|---|
| 101 | **Deposition writes a layer** <br>gives `deposit` <br>needs `colstack` | The surface backlog asks for a sediment budget; this is where the sediment goes. Material removed from a slope arrives somewhere and becomes a layer with a thickness, a material and an age. Without it erosion is subtraction and the abyssal plain is flat for no reason. | Model | M | 3 |
| 102 | **Burial, and what it does to what is buried** <br>needs `deposit` | Compaction, cementation, and the transition from sediment to rock. It is why a layer's properties change with depth and why the column is not just a stack of the things that landed on it. | Model | M | 2 |
| 103 | **Exposure, and the unconformity it leaves** <br>needs `deposit` | When erosion removes layers and deposition resumes, the gap is the most information-dense feature in any cliff face. It is free once the stack has ages. | Model | M | 2 |
| 104 | **Lava flows lay down a layer** <br>needs `deposit` | `W.lava` exists as a field. A flow that cools becomes basalt at the top of the column, which is how Venus resurfaced itself and how the lunar maria formed. | Model | M | 3 |
| 105 | **Impact ejecta is a layer** <br>needs `deposit` | `ROCK_NAMES` already has `impact ejecta` as rock type 5. A crater throws a blanket over everything around it and the blanket is a datable layer — which is how the entire lunar timescale was built. | Model | M | 3 |
| 106 | **Ash falls** <br>needs `deposit` | `W.ash` is a field that shades the planet and never lands. An ash layer is thin, widespread and instantaneous, which makes it the best correlation marker a stratigraphy can have. | Model | M | 2 |
| 107 | **Evaporites when a basin dries** <br>needs `deposit` | A closed basin that evaporates leaves salt in layers, and the layers record the wet and dry cycles. It links the arid processes to the column and it produces one of the brightest surfaces a world can have. | Model | M | 2 |
| 108 | **Biological layers** <br>needs `deposit` | Limestone, chalk, chert, coal, banded iron. `bifRock` exists. The life pass wants organisms to be a geological force and this is the mechanism — biology writing rock. | Model | M | 3 |
| 109 | **The fossil record lives in the stack** <br>needs `deposit` | `W.fossils[c]` is a per-cell array capped at eight with no depth. Fossils in layers means a section you can read, which is what the life pass asked for and could not have. | Model | M | 3 |
| 110 | **Preservation should be biased** <br>needs `deposit` | Skeleton, environment and sedimentation rate decide what survives. A perfect record teaches the wrong lesson; a biased one teaches the right one and makes the geology matter to the biology. | Model | M | 3 |
| 111 | **Correlate two sections** <br>needs `deposit` | Two cliffs on opposite sides of a continent with the same ash layer in them. Matching them is what stratigraphy *is*, and it is a genuinely playable instrument. | undefined | M | 2 |
| 112 | **A dated column is a clock** <br>needs `deposit` | Layer ages give a sedimentation rate, and a rate plus a thickness gives a date for anything in it. It is the same inference chain a real geologist uses and the simulation knows the true answer. | undefined | M | 2 |
| 113 | **Deformation folds and faults the stack** <br>needs `deposit` | Layers tilt, fold and offset. Once they are a stack rather than a recipe, tectonics has something to deform, and a folded sequence exposed by erosion is the most legible geology there is. | Model | L | 2 |
| 114 | **Metamorphism changes a layer in place** <br>needs `deposit` | `ROCK_NAMES` has `metamorphic` as a type. It is a transformation of whatever was there under heat and pressure, which the stack makes expressible for the first time. | Model | M | 2 |
| 115 | **The Anthropocene is a layer** <br>needs `deposit` | Concrete, plastics, radionuclides, a metals spike. The worldspace pass put this under the technosphere; the stack is where it would actually appear. | Model | M | 2 |
| 116 | **A stratigraphic column view** <br>needs `deposit` | The standard figure in the field: layers drawn to scale with materials, ages and a description. `formatColumnAt` is most of the text version. | Draw | M | 3 |
| 117 | **Layers exposed on a cliff** <br>needs `deposit` | The local map at 1,632 km is exactly the scale where a cut bank or crater wall shows a section. It is the payoff for the whole category and it needs the sub-cell work from the surface pass. | Draw | M | 3 |
| 118 | **Colour banding from real layers, not noise** <br>needs `deposit` | The banded terrain of Mars's polar deposits and the Grand Canyon are layers seen edge-on. It is the one kind of banding this project should be trying to produce. | Draw | M | 3 |
| 119 | **Sedimentation rate per world** <br>needs `deposit` | Earth deposits centimetres per thousand years; the Moon deposits almost nothing. It is a per-world rate that follows from the process set and it is a good one-number check. | Prove | M | 2 |
| 120 | **Assert a known section** <br>needs `deposit` | Build a world, run it a billion years, and check the column at a delta looks like a delta: young sediment over older sediment over basement. | Prove | M | 3 |

## The verbs a stack needs

Erode takes from the top. Deposit adds to the top. Intrude inserts. Melt removes and re-adds elsewhere. Weather transforms in place. Compact reduces thickness. Every geomorphic process in `processes.json` is one of these six operations on a column, and none of the six exists because the column is not a field.

| # | Item | What and why | Kind | E | I |
|---|---|---|---|---|---|
| 121 | **Six operations, defined once** <br>gives `stackops` <br>needs `colstack` | Erode from the top, deposit to the top, intrude between, melt out, weather in place, compact. Every process in `processes.json` is one of these six on a column, and defining them once means a new process is a parameter set rather than new code. | Model | M | 3 |
| 122 | **Erosion must know what it is cutting** <br>needs `stackops` | `erodeFactor(W, c)` reads the top material. With a stack it reads the layer at the current depth, which is what makes differential erosion possible and what the surface backlog's `strata` item is waiting for. | Model | M | 3 |
| 123 | **Conservation: what leaves must arrive** <br>needs `stackops` `deposit` | A stack makes mass balance checkable for the first time. Sum the material removed against the material deposited and assert it closes, the same way the water budget does. | Prove | M | 3 |
| 124 | **Weathering transforms rather than moves** <br>needs `stackops` | Rock becomes regolith becomes soil in place. It is the operation that makes a surface look old without moving anything, and it is how a fresh lava flow stops looking fresh. | Model | M | 3 |
| 125 | **Space weathering on an airless world** <br>needs `stackops` | Micrometeorite gardening darkens and matures the top layer over billions of years. It is why fresh craters are bright and old terrain is not, and it is a direct readout of surface age. | Model | M | 3 |
| 126 | **Sublimation removes a layer and leaves a lag** <br>needs `stackops` | The involatile fraction stays behind, darkens the surface and slows further loss. Two operations composed, and it is most of what happens on a comet and on Pluto. | Model | M | 3 |
| 127 | **Melting moves material between layers** <br>needs `stackops` | Ice at the base of a glacier, permafrost thawing, a magma body rising. All three are the same operation with different materials and different depths. | Model | M | 2 |
| 128 | **Intrusion, because not everything arrives from above** <br>needs `stackops` | A sill, a dyke, a salt diapir, a cryovolcanic diapir. Inserting into the middle of a stack is the operation that produces Triton's cantaloupe terrain and Europa's lenticulae. | Model | M | 2 |
| 129 | **Mixing at the top** <br>needs `stackops` | Bioturbation, cryoturbation, impact gardening, ploughing. Mixing destroys the record and that destruction is itself informative. | Model | M | 2 |
| 130 | **Operations should be reversible in the log** <br>needs `stackops` | The god layer has receipts and undo for terrain. A stack operation is a better receipt than a height delta because it says what material moved and where it went. | undefined | M | 2 |
| 131 | **Rates come from the process table** <br>needs `stackops` | `processes.json` has 12 processes. Each needs a rate, a material dependence and a gravity dependence, and then the same process gives different answers on different worlds — which is the whole point. | Model | M | 3 |
| 132 | **Order of operations within a tick** <br>needs `stackops` | Erode then deposit, or deposit then erode, give different columns. The life pass had to write its tick order down; this needs the same. | Model | M | 3 |
| 133 | **Numerical stability at long timesteps** <br>needs `stackops` | A 10 kyr tick can erode more than a layer is thick. Sub-stepping or a flux limiter is required or the stack will develop negative thicknesses on the first mountain. | Model | M | 3 |
| 134 | **Merge thin layers** <br>needs `stackops` | A fixed-depth stack fills up. Merging adjacent layers of the same material, and dropping layers below a thickness threshold, is what keeps the depth bounded without losing the sequence. | Model | M | 3 |
| 135 | **The player should be able to add a layer** <br>needs `stackops` | The god layer can raise crust. Laying down a flood basalt, an ash fall or an ice sheet is a more interesting verb and it is legible for the rest of the run. | undefined | M | 2 |
| 136 | **Operations must be deterministic** <br>needs `stackops` | The golden run covers the fields. A stack is new state and it needs the same guarantee, especially with merging and thresholds involved. | Prove | M | 3 |
| 137 | **Profile the stack tick** <br>needs `stackops` | Six operations over 55,296 cells with eight layers each. Measure it before wiring every process through it. | Prove | M | 3 |
| 138 | **Operations on a world with no surface** <br>needs `stackops` | On a giant the equivalents are condensation, precipitation and mixing between pressure levels. Same six verbs, different axis. | Model | M | 2 |
| 139 | **An operation log per cell, capped** <br>needs `stackops` | What last happened here, and when. It is what the surface backlog's process-attribution overlay needs and it is a byte per cell. | Prove | M | 2 |
| 140 | **Assert a simple case analytically** <br>needs `stackops` | Constant uplift against constant erosion reaches a known steady-state elevation. If the stack cannot reproduce that, none of the landscapes built on it mean anything. | Prove | M | 3 |

## What a stack costs

At N=96 there are 55,296 cells. A fixed eight-layer stack of one material byte and one depth float is 40 bytes a cell — 2.2 MB, which is nothing. A variable-length stack with a real history is a different shape and a different problem. `N_ALLOWED` reaches 768, where the same structure is 142 MB. The design has to state which it is before it is written.

| # | Item | What and why | Kind | E | I |
|---|---|---|---|---|---|
| 141 | **State the memory budget before writing it** <br>gives `stackbudget` | At N=96, 55,296 cells: eight layers of one material byte plus one float thickness is 2.2 MB. At N=768, 3.5 million cells, the same structure is 142 MB. `N_ALLOWED` reaches 768 and nothing measures what the world already costs there. | Prove | S | 3 |
| 142 | **Structure of arrays, not array of structures** <br>needs `stackbudget` | One `Uint8Array(NC * L)` for materials and one `Float32Array(NC * L)` for thicknesses beats 55,296 little objects by orders of magnitude in both memory and cache behaviour. | Model | M | 3 |
| 143 | **Most cells share a sequence** <br>needs `stackbudget` | A palette of common sequences with a per-cell index, and a full stack only where a cell has diverged. It is the compression the data suggests and it may make the variable-depth version affordable. | Model | M | 2 |
| 144 | **Only the top layers change often** <br>needs `stackbudget` | Deep layers are inert for most of a run. Splitting an active top window from a frozen tail cuts the per-tick cost to a fraction of the storage. | Model | M | 3 |
| 145 | **Reallocation on resolution change** <br>needs `stackbudget` | `changeResolution` replaces every array and the surface pass already notes that this throws terrain away. A stack makes the loss larger and the resample harder. | Model | M | 2 |
| 146 | **The stack must not reach the GPU whole** <br>needs `stackbudget` | The field atlas carries twelve channels at eight bits. The shader needs the top material and maybe the one below it, not the column. | Draw | M | 3 |
| 147 | **Serialisation size** <br>needs `stackbudget` | Run-length by layer, quantised thickness. A save that doubles in size for a feature nobody sees will get cut, so measure it against the current format. | Prove | M | 2 |
| 148 | **Allocation-free per tick** <br>needs `stackbudget` | The renderer already learned this lesson with scratch buffers. A stack operation that allocates per cell per tick will show up as a stutter every tick. | Model | M | 3 |
| 149 | **Bound the layer count hard** <br>needs `stackbudget` | A merge policy that can fail leaves an unbounded structure. Pick the cap, enforce it, and make the merge lossy in a stated way. | Model | M | 3 |
| 150 | **Measure at three resolutions** <br>needs `stackbudget` | N=32 for the probes, 96 for play, 192 for the ambitious case. A structure that only works at one of them is a structure that will be rewritten. | Prove | M | 3 |
| 151 | **The GPGPU path should not be blocked by it** <br>needs `stackbudget` | Climate fields already run on the GPU. Keeping the stack CPU-side is fine; keeping it in a place that stalls the readback is not. | Model | M | 2 |
| 152 | **A degraded mode** <br>needs `stackbudget` | On a machine that cannot afford the stack, fall back to the recipe that exists today rather than failing. The recipe is already written and already correct as an initial condition. | Model | M | 2 |
| 153 | **Worker-side stack updates** <br>needs `stackbudget` | `worker.js` exists. Column operations are local per cell and are the most parallelisable thing in the simulation. | Model | L | 1 |
| 154 | **Do not let the stack force a resolution cap** <br>needs `stackbudget` | The sub-cell items in the surface backlog want higher N. If the stack is what stops that, the trade has to be made deliberately rather than discovered. | Prove | M | 3 |
| 155 | **Benchmark the six operations separately** <br>needs `stackops` `stackbudget` | Erode, deposit, intrude, melt, weather, compact. One of them will dominate and it will not be the one anybody guesses. | Prove | M | 2 |
| 156 | **Memory report in the HUD** <br>needs `stackbudget` | The product already prints GPGPU timing. Field memory, and what the stack costs of it, belongs next to it. | Prove | S | 2 |
| 157 | **A stack-free world should cost nothing** <br>needs `stackbudget` | Modern Earth in the Holocene does not need a deep column. Allocating lazily means the common case pays nothing. | Model | M | 2 |
| 158 | **Growth over a long run** <br>needs `stackbudget` | A 4.5 Gyr run deposits a lot. Measure whether the stack converges to a steady layer count or grows without bound, because the merge policy is what decides it. | Prove | M | 3 |
| 159 | **Compare against doing nothing** <br>needs `stackbudget` | The honest baseline: how much does the picture actually improve per megabyte. It is the question that decides whether this construct is worth its cost. | Prove | M | 2 |
| 160 | **Write the budget into the limits document** <br>needs `stackbudget` | `briefs/model-limits.md` states the physical limits. Memory and resolution limits belong there too, because they bound every visual item in three backlogs. | Prove | S | 3 |

## A colour is one triple with no light in it

`substrates.json` gives each of 24 materials an `rgb` triple, an `albedo` and a `tag`. That is more honest than what it replaced and it is still a single colour: no ramp against depth, wetness, grain size, age or angle, and no dependence on what is illuminating it. Meanwhile **154 hardcoded triples remain in code** — 76 in `lifeColour.js`, 67 in `planetLook.js`, 11 in `render.js`.

| # | Item | What and why | Kind | E | I |
|---|---|---|---|---|---|
| 161 | **A material needs a ramp, not a triple** <br>gives `matramp` | `substrates.json` gives each of 24 materials one `rgb`, one `albedo` and a `tag`. A real surface colour varies with wetness, grain size, age, depth and viewing angle. Make the colour a small ramp keyed on those, which is the same shape `landMars` already implements in code with four `lerp` calls. | Draw | M | 3 |
| 162 | **One hundred and fifty-four triples are still in code** <br>gives `triplecount` | Measured: 76 in `lifeColour.js`, 67 in `planetLook.js`, 11 in `render.js`. Every one is a colour decision nobody can review, swap or check for contrast. Count them in a test so the number can only fall. | Prove | S | 3 |
| 163 | **Ramp stops with positions and a colour space** <br>needs `matramp` | A ramp as data is a list of (position, colour) plus an interpolation space. Blending in sRGB bytes — which is what all 85 `lerp` calls in `planetLook.js` do — passes through a muddy middle, and the surface backlog already flagged this. | Draw | M | 3 |
| 164 | **Depth ramps for anything submerged** <br>needs `matramp` | The surface pass fitted the ocean ramp so it no longer saturates at 530 m. That fix belongs to a *material* — water — not to the ocean branch of `refreshColours`, and then a methane sea and a brine ocean get the same treatment for free. | Draw | M | 3 |
| 165 | **Wetness darkens, and it is one term** <br>needs `matramp` | Wet rock, wet sand, wet soil are all darker and glossier. One modifier on the material ramp covers rain, tide, snowmelt and river overflow, and the surface backlog asked for exactly this and put it in the wrong category. | Draw | S | 3 |
| 166 | **Grain size is an albedo axis** <br>needs `matramp` | Fresh fine frost is near 0.9 and coarse old ice below 0.4 — same material, different history. `substrates.json` has one albedo per material and the difference between Enceladus and Callisto is largely this. | Draw | M | 3 |
| 167 | **Age darkens an airless surface** <br>needs `matramp` | Space weathering matures regolith over billions of years. It is a ramp against surface age and it is why the lunar rays fade. | Draw | M | 3 |
| 168 | **Cover composites over substrate, it does not replace it** <br>needs `matramp` | `cover.json` has 11 kinds with their own triples. Frost over basalt at 30% coverage is a blend, not a switch, and the surface backlog's four-stage pipeline is where that blend belongs. | Draw | M | 3 |
| 169 | **Retire the PAINT lambdas into ramps** <br>needs `matramp` `paintdata` | Thirty kinds, 37 functions, 85 `lerp` calls. Each is a function of elevation, ice, temperature and moisture — four inputs, which is a small ramp table. This is the concrete migration for construct 18. | Draw | L | 3 |
| 170 | **Ramps must be dithered on the way to eight bits** | The surface pass landed vertex dithering. A data ramp has more stops and shallower gradients, which makes banding more likely, not less. | Draw | S | 3 |
| 171 | **A ramp per era** <br>needs `matramp` | An Archean ocean is iron-green, a Cryogenian one is white. The worldspace pass wants an epoch to be a world; a palette is a large part of what makes it feel like one. | Draw | M | 3 |
| 172 | **Ramps for overlays are a separate family** <br>needs `matramp` | Surface ramps describe materials; overlay ramps encode data and need perceptual uniformity and a diverging option. Same file format, different rules, and mixing them produces rainbow scalars. | Draw | M | 3 |
| 173 | **Named ramps, reused across worlds** <br>needs `matramp` | "Dirty ice", "oxidised basalt", "organic haze". Named ramps shared by several worlds are what stop the palette becoming per-body, which is the failure mode this whole document is about. | Draw | M | 3 |
| 174 | **A ramp is not a biome** <br>needs `matramp` | `lifeColour.js` has 76 triples covering guilds, classes, biomes and kinds. Some of those are materials and some are living cover, and separating them is what lets the life pass's pigment work reach the picture. | Draw | M | 3 |
| 175 | **Interpolate in a perceptual space** <br>needs `matramp` | Oklab or CIELAB for ramp evaluation. It is a contained change with a visible payoff on every gradient, and it is a precondition for any contrast check being meaningful. | Draw | M | 3 |
| 176 | **The ocean is a material too** <br>needs `matramp` | `rule.ocean` is a second lambda per kind in the PAINT table. Water, brine, methane and magma are materials in `substrates.json`, and a sea is a material with a depth ramp. | Draw | M | 3 |
| 177 | **Sea ice is three constants** | `[222, 234, 246]` partial, `[248, 251, 255]` full, one blue for a lead. Age, thickness, snow cover, melt ponds and ridging are all tracked or trackable and none reaches the colour. | Draw | M | 2 |
| 178 | **Emissive materials need a separate channel** <br>needs `matramp` | Lava glows, aurorae glow, bioluminescence glows, cities glow. A reflectance ramp cannot express emission and the night side is where all four live. | Draw | M | 3 |
| 179 | **Specular and roughness belong with the ramp** <br>needs `matramp` | The realism pass gave the ocean a GGX lobe with wind-driven roughness. Land, ice and lava have no material response at all, so a salt flat and a forest scatter light identically. | Draw | M | 2 |
| 180 | **Assert every material has a ramp** <br>needs `matramp` `onebuild` | Twenty-four materials, 11 cover kinds, 21 landforms. The compiler should refuse a row without one, which is how the count of code triples gets driven down and stays down. | Prove | M | 3 |

## Where a colour comes from, and how sure we are

`substrates.json` and `cover.json` carry 35 provenance tags between them, which is the right discipline. But a tag on a hand-picked triple says only that somebody was honest about guessing. The materials that have published reflectance spectra — basalt, water ice, sulfur, tholin, hematite, olivine — could carry the spectrum and derive the triple, and then the tag would mean something stronger.

| # | Item | What and why | Kind | E | I |
|---|---|---|---|---|---|
| 181 | **Carry a spectrum where one exists** <br>gives `spectrum` | `substrates.json` has 24 materials with a `tag` on each. Basalt, water ice, sulfur, tholin, hematite and olivine all have published reflectance spectra. Carrying a few sampled bands and deriving the triple makes `measured` mean something stronger than "somebody was honest about guessing". | Prove | L | 3 |
| 182 | **Cite the source on the row** <br>needs `spectrum` | A `measured` tag with no reference is an assertion. The catalogue credits the NASA Exoplanet Archive by name; material optics deserve the same discipline. | Prove | M | 3 |
| 183 | **Derive the triple, do not author both** <br>needs `spectrum` | If a spectrum exists, the RGB must be computed from it under a stated illuminant. Two sources of truth for one colour is how they drift. | Draw | M | 3 |
| 184 | **The Earth reference path already exists** <br>needs `spectrum` | `EOREF` holds a shelf of NASA stills and the realism pass built the comparison path. Extend it from "does Earth look right" to "does this material look right", which is a much sharper question. | Prove | M | 3 |
| 185 | **Bond albedo should agree with the picture** <br>needs `spectrum` | The surface backlog asks for this and it belongs here: integrate the rendered disc luminance and compare against the model's albedo. If a world looks brighter than it is, one of the two is wrong. | Prove | M | 3 |
| 186 | **Measured albedos for every Solar System body** <br>needs `spectrum` | Enceladus 0.81, Iapetus a factor of ten between hemispheres, the Moon 0.12. These are published, they are unambiguous, and they are the cheapest possible calibration for the whole palette. | Prove | M | 3 |
| 187 | **Phase angle changes brightness** <br>needs `spectrum` | The opposition surge is real and large on regolith surfaces. It is one term and it is why the full Moon is more than twice as bright as the half Moon. | Draw | M | 2 |
| 188 | **Say when a colour is a guess, on the world** <br>needs `spectrum` | The realism backlog wants a visual honesty label. For 94 exoplanets every colour is invented, and the interface presenting them identically to Mars is the problem. | Prove | M | 3 |
| 189 | **Colour for a material nobody has seen** <br>needs `spectrum` | Supercritical CO₂, high-pressure ice, a silicate vapour atmosphere. Derive from composition and physics where possible, label invented where not, and never quietly pick something pretty. | Prove | M | 3 |
| 190 | **Spectra let the life pass reach the surface** <br>needs `spectrum` | `sensory.js` integrates a Planck spectrum against band transmission for eyes. The same integral against a surface reflectance gives the colour under any star, and the code is already written. | Draw | M | 3 |
| 191 | **The red edge is a spectral feature** <br>needs `spectrum` | Vegetation's reflectance jump near 700 nm is the canonical biosignature. It is a spectrum, not a triple, and drawing it as one is what makes the transit spectrum and the surface agree. | Model | M | 3 |
| 192 | **Store spectra coarsely** <br>needs `spectrum` | Eight to sixteen bands from 300 nm to 2500 nm is enough for colour and for the biosignature work, and it keeps the table readable. | Model | M | 2 |
| 193 | **A spectrum viewer in the Lab** <br>needs `spectrum` | Pick a material, see its reflectance and the resulting colour under the current star. It is the tool that makes the whole construct inspectable. | Draw | M | 2 |
| 194 | **Provenance mix as a number per world** <br>needs `spectrum` | "This world's appearance is 20% measured, 30% inferred, 50% invented." One number, on the world card, computed from the tags already present. | Prove | M | 3 |
| 195 | **Do not let provenance become decoration** <br>needs `spectrum` | Thirty-five tags exist and nothing surfaces them. A tag nobody reads is worse than no tag because it implies a rigour that is not being exercised. | Prove | S | 3 |
| 196 | **Reference imagery per body** <br>needs `spectrum` | `site/img/` has four screenshots and none is a reference photograph. Every Solar System body has public imagery, and the comparison is the only external truth the picture has. | Prove | M | 3 |
| 197 | **Colour-match against the reference** <br>needs `spectrum` | Histogram, dominant hue, light–dark contrast, limb profile. Four numbers per body against its photograph, and a stated tolerance. | Prove | M | 3 |
| 198 | **Beware the enhanced-colour trap** <br>needs `spectrum` | Most published planetary images are stretched or false-colour. Matching against them without saying so would encode a processing choice as a physical fact — and the honest version is more interesting. | Prove | M | 3 |
| 199 | **Human colour vision is the last step** <br>needs `spectrum` | A spectrum becomes a colour only through an observer. State which observer, because on a red-dwarf world the interesting question is what its own inhabitants would see, and the life pass computes that. | Draw | M | 2 |
| 200 | **Assert a known colour end to end** <br>needs `spectrum` | Basalt under the Sun should come out near the measured lunar mare reflectance. One assertion that pins spectrum, illuminant and tonemap together. | Prove | M | 3 |

## The same rock under a different sun

A surface colour is a reflectance spectrum multiplied by an illuminant. `EOREF` holds a shelf of Earth stills; `planetLook.js` returns display-referred bytes from `lerp` constants tuned under a G star. On a 2,560 K dwarf the same basalt is a different colour, and the life pass already computes exactly this integral for eyes and never for surfaces.

| # | Item | What and why | Kind | E | I |
|---|---|---|---|---|---|
| 201 | **Colour is reflectance times illuminant** <br>gives `illum` <br>needs `spectrum` | `planetLook.js` returns display-referred bytes from constants tuned under a G star. The same basalt under a 2,560 K dwarf is a different colour, and `sensory.js` already integrates Planck spectra for eyes — the machinery exists and never touches a surface. | Draw | M | 3 |
| 202 | **Exposure per world, from insolation** <br>needs `illum` | Mercury at 6.7 solar constants and Pluto at 0.001 are drawn with the same exposure. The realism pass added eye adaptation; making its target per-world is a small change with an enormous effect on whether a world reads as far from its star. | Draw | M | 3 |
| 203 | **The sky is a second light source** <br>needs `illum` | On Earth shadows are blue because the sky fills them. On an airless world they are black. It is the strongest single cue for whether a world has air and it is one term. | Draw | M | 3 |
| 204 | **Sky colour from the atmosphere it has** <br>needs `illum` | Rayleigh gives blue, Mars's dust gives butterscotch with a blue sunset, Titan's haze gives orange gloom. Composition and optical depth are in the model. | Draw | M | 3 |
| 205 | **Low sun is red, not dim** <br>needs `illum` | The terminator is where a planet looks best and it is currently a brightness ramp. Reddening through a long air path is the physics and it is the same integral as the sky colour. | Draw | M | 3 |
| 206 | **Atmospheric perspective toward the limb** <br>needs `illum` | More air between surface and camera means desaturation and a blue shift. The realism pass added a limb volume term and the surface colour does not know about it. | Draw | M | 2 |
| 207 | **Night is a different colour, not a darker one** <br>needs `illum` | Real night shows what emits — lava, aurorae, lightning, cities, bioluminescence — plus whatever a moon reflects onto it. Four of those five exist as fields. | Draw | M | 3 |
| 208 | **Moonlight, and a sky with a planet in it** <br>needs `illum` | Europa has Jupiter filling a third of its sky. Reflected light from a parent body is a real illuminant with a real colour and `MOON_PARENTS` already knows the geometry. | Draw | M | 2 |
| 209 | **Two suns** <br>needs `illum` | `binaryInsolation` and `circumbinaryBeat` exist. Two illuminants of different temperatures means two shadows of different colours, which is the most legible possible statement that this is not the Solar System. | Draw | M | 2 |
| 210 | **Eclipses and transits as lighting events** <br>needs `illum` | A moon crossing the star, a parent planet eclipsing its moon for hours. These are dramatic, computable from orbits already in the catalogue, and they change the light rather than the surface. | Draw | M | 2 |
| 211 | **A rogue world has no illuminant** <br>needs `illum` | Sixteen bodies in the `dark` category. Lit only by internal heat and starlight, which is a rendering problem nobody has had to solve here and the honest answer is nearly black. | Draw | M | 2 |
| 212 | **Tone mapping is per world too** <br>needs `illum` | The realism pass found the atmosphere shell clipped to white because it was never tonemapped. A world at 6.7 solar constants and one at 0.001 cannot share a curve. | Draw | M | 3 |
| 213 | **White balance is a choice, so state it** <br>needs `illum` | Rendering a red-dwarf world as the eye would adapt to it, or as a camera calibrated to the Sun would record it, are different pictures. Both are defensible; picking silently is not. | Draw | M | 3 |
| 214 | **Show the same material under three stars** <br>needs `illum` | A strip of the same surface under a G, K and M star. It is the clearest demonstration that colour is not a property of a thing, and it is a Lab panel. | Draw | M | 3 |
| 215 | **Illumination belongs in the world definition** <br>needs `illum` `worldrec` | Star temperature, insolation, sky model, exposure target and white balance decided together per world — which is where this construct meets art direction. | Model | M | 3 |
| 216 | **Cloud shadow on the ground** <br>needs `illum` | The cloud shell blocks light in the radiation budget and not in the picture. It is one of the strongest cues that a planet has weather. | Draw | M | 3 |
| 217 | **Ring shadows** <br>needs `illum` | Saturn's rings cast a hard band across its cloud tops and the shadow moves with the season. It is one of the most recognisable images in the Solar System. | Draw | M | 2 |
| 218 | **Specular highlight as a diagnostic** <br>needs `illum` | The glint off Titan's lakes is how they were confirmed. A specular return says "liquid" more clearly than any colour. | Draw | M | 2 |
| 219 | **Do not fake it with a colour grade** <br>needs `illum` | A per-world LUT would get most of the way there and would encode a look rather than a physics. Where the physics is cheap, spend it; where a grade is used, label it. | Draw | M | 3 |
| 220 | **Assert the illuminant path** <br>needs `illum` | Same material, three star temperatures, three different output colours in the right direction. If the surface colour does not move with the star, the whole construct is inert. | Prove | M | 3 |

## Tooling for a palette that is data

Once ramps are data they can be previewed, diffed, swapped, checked for contrast and shipped per world. None of that tooling exists, and without it a data palette is a JSON file nobody can evaluate — which is worse than a lambda somebody can at least read next to the thing it paints.

| # | Item | What and why | Kind | E | I |
|---|---|---|---|---|---|
| 221 | **A palette preview page** <br>gives `palview` <br>needs `matramp` | Every material, every cover kind, every landform, every ramp, with its stops, its provenance tag and its current data range. Generated from the tables. It is the fastest way to see that a ramp is saturating, which is how the ocean ramp went unnoticed for the life of the project. | Prove | M | 3 |
| 222 | **Diff two palettes** <br>needs `palview` | A change to a shared ramp affects many worlds. Showing which, and by how much, is what makes a palette edit reviewable rather than terrifying. | Prove | M | 3 |
| 223 | **Contrast check between adjacent categories** <br>needs `palview` | Two materials with similar colours are indistinguishable at orbital scale; two with wildly different ones make a boundary look painted. Compute ΔE between every adjacent pair. | Prove | M | 3 |
| 224 | **Colour-blind safe variants** <br>needs `palview` | The land–sea distinction and the vegetation ramp both lean on red–green separation. Once ramps are data, an alternative palette is a setting rather than a rewrite. | Draw | M | 2 |
| 225 | **Edit a ramp live** <br>needs `palview` | Change a stop and see the globe change. It is the tool that makes art direction possible for somebody who is not going to edit JSON and rebuild. | undefined | M | 2 |
| 226 | **Palette entropy per world** <br>gives `palentropy` <br>needs `palview` | How many distinct colours a world actually uses. A world drawing three colours is flat; one drawing thousands is noise. It is one of the four metrics construct 19 asks for and it is computable from a render. | Prove | M | 3 |
| 227 | **A palette should be exportable** <br>needs `palview` | Somebody who wants to make art from a world wants its colours. It is a small, generous feature and it makes the palette work visible outside the app. | Draw | S | 1 |
| 228 | **Version and freeze the shipped palettes** <br>needs `palview` | A screenshot in the README is a claim about a palette. When the palette changes the screenshot is stale, and nothing currently connects them. | Prove | M | 2 |
| 229 | **Which ramp painted this pixel** <br>needs `palview` | A debug mode that names the ramp under the cursor. Half the palette faults in this document are invisible until somebody can attribute a colour. | Prove | M | 3 |
| 230 | **Find unused ramps** <br>needs `palview` `onebuild` | A ramp no world selects is either dead or a missing rule. The compiler already walks the tables. | Prove | S | 2 |
| 231 | **Find worlds sharing a palette** <br>needs `palview` | This is the Io problem in colour form. If eleven ice moons all draw the same ramp, the palette is doing no work and nobody can tell them apart. | Prove | M | 3 |
| 232 | **A palette budget per world** <br>needs `palentropy` | State how many distinct ramps a world should use. Too few and it is flat; too many and it is incoherent. It is an art-direction decision that can be checked. | Prove | M | 2 |
| 233 | **Ramps in the legend** <br>needs `palview` | The KEY panel lists surface categories with swatches from code. Driving it from the same data as the surface guarantees the legend cannot lie. | Draw | M | 3 |
| 234 | **Palette in the world card** <br>needs `palview` | Five swatches and a provenance mix. It is the thumbnail of a palette and it makes 120 worlds comparable at a glance. | Draw | M | 3 |
| 235 | **Let a player reskin a world** <br>needs `palview` | The god layer changes physics. Changing the look — and having the app say which parts are now invented — is a different kind of authorship and a natural sharing surface. | undefined | M | 1 |
| 236 | **Keep the ramp count small** <br>needs `palview` | The temptation once ramps are data is one per world. The measure of the system working is that 120 worlds draw from far fewer than 120 palettes. | Prove | M | 3 |
| 237 | **Document why a palette is what it is** <br>needs `palview` | `substrates.json` carries a `why` per material and it is the best thing about the file. Ramps need the same, because a colour choice without a reason cannot be argued with. | Prove | S | 3 |
| 238 | **Palette review as part of adding a world** <br>needs `palview` `contact` | A new world's palette should be shown against its nearest neighbours before it is merged. That is the review that stops the catalogue converging on one look. | Prove | M | 3 |
| 239 | **Regression on palette, not just on pixels** <br>needs `palentropy` | A change that shifts every world slightly is fine. One that collapses two worlds onto the same palette is a bug, and only a pairwise measure sees it. | Prove | M | 3 |
| 240 | **The palette is the product** <br>needs `palview` | Most people will see this project as a set of images. The palette is the largest single determinant of whether those images are worth looking at, and it currently lives in 154 unnamed triples. | Draw | S | 3 |

## There is no pixel in the test suite

`pictureStats` is good work and it measures **fields** — height, temperature, precipitation, life, moisture. It never renders anything. `readPixels`, `toDataURL` and any headless GL context appear nowhere in `vr/sim/test.mjs`. `capture-site.mjs` drives Playwright and screenshots four site pages, not worlds. So the realism backlog's central finding — that the picture has never been measured — is still true; what has been measured is the data behind the picture.

| # | Item | What and why | Kind | E | I |
|---|---|---|---|---|---|
| 241 | **Nothing in the test suite renders anything** <br>gives `headlessgl` | `pictureStats` measures fields — height, temperature, precipitation, life, moisture — and is good work. `readPixels`, `toDataURL` and any headless GL context appear nowhere in `vr/sim/test.mjs`. `capture-site.mjs` drives Playwright and screenshots four *site pages*, not worlds. So the realism backlog's finding stands: the picture has never been measured, only the data behind it. | Prove | L | 3 |
| 242 | **Pick the rendering path and commit to it** <br>needs `headlessgl` | Three options: a headless GL context in Node, Playwright driving the real app, or a CPU reimplementation of the shader. The first is fastest and fragile, the second is honest and slow, the third drifts from what ships. `capture-site.mjs` already proves the second works here. | Prove | M | 3 |
| 243 | **Render one world, save one PNG, assert one number** <br>needs `headlessgl` | The smallest possible version, landed end to end, before any of the metrics below. Every ambitious testing plan in this project has been blocked on not having this. | Prove | M | 3 |
| 244 | **Determinism of a rendered frame** <br>needs `headlessgl` | The surface pass found `refreshColours` reading `performance.now()` for the stroke fade, so a frame was not a pure function of world state. A render test needs a fixed clock, a fixed camera and a fixed sun. | Prove | M | 3 |
| 245 | **A fixed camera rig per world** <br>needs `headlessgl` | Same distance, same sun angle, same up vector, or every comparison is measuring the camera. It is also the thing art direction wants for a hero shot. | Prove | S | 3 |
| 246 | **GPU output varies across drivers** <br>needs `headlessgl` | Byte-exact comparison will fail on somebody else's machine. A perceptual metric with a stated tolerance is the difference between a test that guards the picture and one everybody learns to skip. | Prove | M | 3 |
| 247 | **Render at a small size** <br>needs `headlessgl` | 256×256 is enough for every metric in the next category and it makes 120 worlds affordable. Hero shots are a separate, rarer job. | Prove | S | 3 |
| 248 | **Keep the CPU field stats** <br>needs `headlessgl` | `surfaceStats` is fast, deterministic and driver-independent. Pixels are the ground truth; fields are the cheap check that runs on every commit. Both, not one. | Prove | S | 3 |
| 249 | **Render the flat map too** <br>needs `headlessgl` | The product's pitch is two views at once. The local map has its own colour path in `localview.js` and its own chance to disagree with the globe. | Prove | M | 3 |
| 250 | **Render the overlays** <br>needs `headlessgl` | Twenty overlay modes, each a full-globe repaint. The surface pass fixed the wash and the stencil; nothing checks the other nineteen. | Prove | M | 3 |
| 251 | **Render at two resolutions** <br>needs `headlessgl` | Almost every artefact in the surface backlog scales with N. A seam at N=32 and one at N=192 look different and one of them may be the only one anybody notices. | Prove | M | 2 |
| 252 | **Render at two zoom rungs** <br>needs `headlessgl` | The zoom contract says the same world at different scales should be the same world. A test that renders orbital and regional and compares low-frequency content is what enforces it. | Prove | M | 3 |
| 253 | **A no-ink render mode** <br>needs `headlessgl` | The surface pass asked for a screenshot key that strips every overlay and HUD. The test harness needs the same switch and it is the same code. | Draw | S | 3 |
| 254 | **Store the reference images somewhere sane** <br>needs `headlessgl` | One hundred and twenty PNGs regenerated on every change will make the repository unpleasant. Low resolution, lossy, and a stated policy on when they are regenerated. | Prove | M | 2 |
| 255 | **Fail with the image, not just the number** <br>needs `headlessgl` | A failing render test should write the actual, the expected and the difference. Otherwise the first thing anybody does is reproduce it by hand. | Prove | M | 3 |
| 256 | **Time budget for the render suite** <br>needs `headlessgl` | One hundred and twenty worlds at two rungs is 240 renders. If that is ten minutes it runs nightly; if it is thirty seconds it runs on every commit. Measure before designing the cadence. | Prove | M | 3 |
| 257 | **Run it in CI** <br>needs `headlessgl` | A render test on the author's machine is a screenshot habit. On a machine that is not theirs it is a guard. | Prove | M | 3 |
| 258 | **The WebGL path must be optional** <br>needs `headlessgl` | The existing suites run anywhere with Node. Do not make `npm test` require a GPU; make the render suite a separate command that CI runs. | Prove | S | 3 |
| 259 | **A render is also an artefact for humans** <br>needs `headlessgl` `contact` | The same harness that measures produces the contact sheet, the README images and the world cards. Building it once for tests pays three times. | Prove | M | 3 |
| 260 | **Say in the limits document that the picture is untested** <br>needs `headlessgl` | Until this lands it is the largest known gap in the project's verification, and `briefs/model-limits.md` is where the known gaps are supposed to live. | Prove | S | 3 |

## What to count once there are pixels

Axis-aligned edge runs, a contour-step histogram, per-face discontinuity across all twelve cube edges, palette entropy, luminance histogram, dominant hue, limb profile, and the perceptual distance between two worlds. Each targets a named fault from the surface and worldspace passes, and each is a number that should move in a stated direction.

| # | Item | What and why | Kind | E | I |
|---|---|---|---|---|---|
| 261 | **Axis-aligned edge runs** <br>gives `edgemetric` <br>needs `headlessgl` | Count pixels lying on a straight horizontal or vertical run longer than a threshold. It targets the cube-seam class directly, it goes to zero as those fixes land, and nature almost never produces a straight line at planetary scale — which is why it is a good detector. | Prove | M | 3 |
| 262 | **A contour-step histogram** <br>needs `headlessgl` | Distribution of neighbour-pixel ΔE. A hard classification produces long thin connected runs at a characteristic contrast, which is a signature you can detect rather than a thing you have to notice. | Prove | M | 3 |
| 263 | **Per-face discontinuity, in pixels** <br>needs `edgemetric` | `pictureStats.heightSeam` measures this on the *field* and the assertion passes. The rendered version measures whether the shader, the atlas and the mesh agree — which is where the surface pass found two separate bugs. | Prove | M | 3 |
| 264 | **Palette entropy** <br>needs `palentropy` `headlessgl` | Distinct colours in a rendered frame, and their distribution. Too few is flat, too many is noise, and the number distinguishes a world that is drawing detail from one that is drawing dither. | Prove | M | 3 |
| 265 | **Luminance histogram and clipping** <br>needs `headlessgl` | The realism pass found the atmosphere shell clipping the sunward limb to flat white. Counting clipped pixels is a one-line detector for the whole class of tonemapping faults. | Prove | S | 3 |
| 266 | **Dominant hue and saturation** <br>needs `headlessgl` | The single number that says Mars is red and Europa is not. It is also the number that would have shown 40 worlds converging on Io. | Prove | S | 3 |
| 267 | **The limb profile** <br>needs `headlessgl` | A radial slice through the edge of the disc. Hard means airless, soft means atmosphere, layered means haze. It is one of the two edges that survive a thumbnail and it is a one-dimensional signal. | Prove | M | 3 |
| 268 | **Spatial frequency spectrum** <br>needs `headlessgl` | A radially averaged power spectrum says whether a world has detail at every scale or only at the cell size. It is the direct measure of whether the detail synthesis is doing anything. | Prove | M | 2 |
| 269 | **Pairwise perceptual distance between worlds** <br>gives `pairdist` <br>needs `headlessgl` | The Io detector. Compute the distance between every pair of world renders; pairs below a threshold that are not physically justified are the bug. This is the metric this whole programme has needed from the start. | Prove | M | 3 |
| 270 | **Coastline staircase, in pixels** <br>needs `headlessgl` | `pictureStats.staircase` measures cell-level runs and sits at 0.771, which is expected until sub-cell coverage lands. The pixel version measures what the player sees, which is the number that should actually fall. | Prove | M | 3 |
| 271 | **Terminator gradient** <br>needs `headlessgl` | How smoothly the day side becomes the night side. Banding there is the most visible quantisation artefact on any render and it is a one-dimensional profile. | Prove | M | 2 |
| 272 | **Overlay ink budget, measured** <br>needs `headlessgl` | The surface pass specified a budget: no overlay may move more than a stated fraction of pixels by more than a stated ΔE. Diff each overlay render against the no-ink render. | Prove | M | 3 |
| 273 | **Every metric needs a direction and a target** <br>needs `edgemetric` | A number with no stated direction is a fact, not a guard. Each metric gets: what it detects, which way is better, the current value, and the budget. | Prove | S | 3 |
| 274 | **Metrics must be cheap enough to run on all of them** <br>needs `edgemetric` | Twelve metrics over 240 renders. Anything requiring a full-image transform per metric should share one pass over the pixels. | Prove | M | 2 |
| 275 | **Do not over-fit to the metric** <br>needs `edgemetric` | A world can score well on all twelve and look wrong. The contact sheet is the check on the metrics, which is why construct 20 and construct 19 belong in the same document. | Prove | S | 3 |
| 276 | **Compare against a photograph, not only against yesterday** <br>needs `spectrum` `headlessgl` | A regression suite measures drift from your own last answer. For twelve Solar System bodies there is an external answer and it is the only one that can say the picture is wrong rather than different. | Prove | M | 3 |
| 277 | **Report per world, not aggregated** <br>needs `edgemetric` | A mean over 120 worlds hides the one that broke. The output is a table with a row per world and a column per metric. | Prove | S | 3 |
| 278 | **Metrics for the flat map** <br>needs `headlessgl` | Stamp density, colour agreement with the globe for the same cells, and whether anything moves between frames — the living backlog measured that the flat map draws the identical image every frame. | Prove | M | 3 |
| 279 | **A single headline number** <br>needs `edgemetric` | One score per world, from the twelve, so a change can be summarised in a sentence. It is how anybody will actually consume this. | Prove | M | 2 |
| 280 | **Publish the metrics with the site** <br>needs `edgemetric` | The backlogs are published as pages. The picture's numbers, per world, per release, is the same kind of artefact and it makes the claim checkable by a stranger. | Prove | M | 2 |

## One world is measured; there are a hundred and twenty

`pictureStats(Wcont)` runs on a single continental test world inside one test block, with 14 assertions on it. The catalogue has 120 bodies across seven categories. A detector that samples 0.8% of the catalogue cannot see the class of fault this whole programme is about — the one where 40 worlds looked like Io and nobody noticed for months.

| # | Item | What and why | Kind | E | I |
|---|---|---|---|---|---|
| 281 | **One world is measured; there are a hundred and twenty** <br>gives `fleetrun` <br>needs `headlessgl` | `pictureStats(Wcont)` runs on a single continental test world with 14 assertions on it. That is 0.8% of the catalogue. The fault this whole programme exists to catch — 40 worlds resolving to Io — is invisible at that sample size, and it went unnoticed for months. | Prove | M | 3 |
| 282 | **Generate every catalogue body headlessly** <br>needs `fleetrun` | `auditCatalogueKinds` already walks all 120 through `rulesetFromCatalogue`. Extend it to generate each at N=32 and record whether it completes without throwing — several probably do not, and nobody knows which. | Prove | M | 3 |
| 283 | **A world that fails to generate is a bug nobody sees** <br>needs `fleetrun` | The catalogue has free-floating planets, disintegrating worlds, magma oceans and bodies that are not round. `isNonHydrostatic` exists. Which of the 120 actually run is a fact nobody currently has. | Prove | M | 3 |
| 284 | **Cover the seven categories deliberately** <br>needs `fleetrun` | sol 12, moons 15, temperate 29, furnace 17, giant 21, arch 10, dark 16. A sample that misses giants misses the case where the engine has no heightfield at all. | Prove | S | 3 |
| 285 | **Include the invented rulesets** <br>needs `fleetrun` | Vermis, Selene, Ares and Daisyworld are what most players see first and they are not in the catalogue. The reset test I added covers them; the picture tests do not. | Prove | S | 3 |
| 286 | **Include epochs** <br>needs `fleetrun` | Five era presets on Earth. An Archean render and a Holocene render are different pictures of the same body and both should be measured. | Prove | M | 2 |
| 287 | **Fixed seeds per world** <br>needs `fleetrun` | One seed per body, committed, so a change in the picture is a change in the code rather than a different roll. The landscape pass learned this with openings. | Prove | S | 3 |
| 288 | **Run the fleet at a fixed age** <br>needs `fleetrun` | A world at t=0 and one after a hundred ticks look different. Pick one, state it, and make it the same for every body. | Prove | S | 3 |
| 289 | **A fleet run is a data set** <br>needs `fleetrun` | One hundred and twenty rows of twelve metrics. That is a table you can sort, cluster and plot — and clustering it is how you find the worlds that share a look. | Prove | M | 3 |
| 290 | **Cluster the fleet by appearance** <br>needs `fleetrun` `pairdist` | Compare the clusters against the physical taxonomy from `worldAxes`. Where two worlds are physically distant and visually identical, the appearance path is not reading the physics. | Prove | M | 3 |
| 291 | **Find the worlds nobody has looked at** <br>needs `fleetrun` | Of 120 bodies, a handful have ever been opened. The fleet run is the first time anybody sees the rest, and that alone is worth building it. | Prove | M | 3 |
| 292 | **Rank worlds by how wrong they look** <br>needs `fleetrun` | Sort by metric violations and fix from the top. It converts a vague sense that the catalogue is uneven into a work list. | Prove | M | 3 |
| 293 | **Fleet run cost and cadence** <br>needs `fleetrun` | One hundred and twenty generates plus renders. If it is minutes it runs nightly; if it is an hour it runs on release. Measure, then decide, rather than the reverse. | Prove | M | 3 |
| 294 | **Incremental fleet runs** <br>needs `fleetrun` | A change to Titan's definition should re-measure Titan and whatever shares its ramps. Full runs on demand, targeted runs on commit. | Prove | M | 2 |
| 295 | **The fleet run should also check the sim, not just the picture** <br>needs `fleetrun` | Does every world conserve water, stay inside its temperature bounds, and avoid NaN. Twelve metrics of appearance and half a dozen of physical sanity, from the same pass. | Prove | M | 3 |
| 296 | **Timing per world** <br>needs `fleetrun` | A world that takes ten times longer than its neighbours is doing something wrong. The fleet run is the only place that shows up. | Prove | S | 2 |
| 297 | **Memory per world** <br>needs `fleetrun` `stackbudget` | Especially once the stack lands. An ice giant with a deep column and an ocean world with a deep ocean are the two expensive cases. | Prove | M | 2 |
| 298 | **Keep the fleet result in the repository** <br>needs `fleetrun` | A committed table means a diff. It is the same discipline as `kinds.json`, which is what keeps the Io result from returning. | Prove | S | 3 |
| 299 | **Publish the fleet table** <br>needs `fleetrun` | One hundred and twenty worlds with their axes, their kind, their metrics and their thumbnail. That page is genuinely novel content and it is a by-product of the tests. | Draw | M | 3 |
| 300 | **A fleet run is the acceptance test for this whole programme** <br>needs `fleetrun` | Five backlogs have argued that the catalogue is a set of recolours. The fleet run is what turns that argument into a number and eventually into a refutation. | Prove | M | 3 |

## A measurement nobody looks at is not a guard

The numbers exist and one world asserts eight of them. What is missing is the loop: committed baselines, a budget per metric, a diff on every change, and a failure that names which world got worse. Without it the artefact detector is a script somebody ran once, which is what the realism backlog warned about.

| # | Item | What and why | Kind | E | I |
|---|---|---|---|---|---|
| 301 | **Commit the baselines** <br>gives `baseline` <br>needs `fleetrun` | Every metric, every world, committed as a table. Without a baseline a measurement is a reading; with one it is a guard. `kinds.json` already proves the pattern works in this repository. | Prove | M | 3 |
| 302 | **A budget per metric, not an exact value** <br>needs `baseline` | Rendered output moves slightly for legitimate reasons. A budget with a stated tolerance fails on regressions and tolerates noise, which is the only way a picture test survives contact with a second machine. | Prove | M | 3 |
| 303 | **Fail with the world name and the metric** <br>needs `baseline` | "Europa: axis-aligned runs 0.02 → 0.31" is actionable. "picture test failed" is not, and the difference decides whether the suite gets maintained. | Prove | S | 3 |
| 304 | **Ratchet the budgets down** <br>needs `baseline` | When a metric improves, tighten it. It is what stops a fix from silently eroding, and it converts the backlog into a monotone process. | Prove | M | 3 |
| 305 | **A deliberate change should be easy to accept** <br>needs `baseline` | An art-direction change moves every number. Regenerating baselines must be one command with a reviewable diff, or people will disable the test instead. | Prove | S | 3 |
| 306 | **Separate "different" from "worse"** <br>needs `baseline` | Most metric changes are neutral. The suite needs to distinguish a drift from a violation, or it cries wolf and stops being read. | Prove | M | 3 |
| 307 | **Track the numbers over releases** <br>needs `baseline` | A time series per metric. The surface backlog is 400 claims about numbers that should move; this is where anyone can check whether they did. | Prove | M | 3 |
| 308 | **Guard the field stats too** <br>needs `baseline` | `pictureStats` has 14 assertions on one world with hand-picked thresholds. Baselined across the fleet, the same code becomes a real guard rather than eight sanity checks. | Prove | M | 3 |
| 309 | **Guard the data layer** <br>needs `baseline` `dataratio` | Rows per table, coverage per body, override count, provenance mix, data-to-code ratio. All computable, all currently unwatched, and all in this document as targets. | Prove | M | 3 |
| 310 | **Guard determinism across the fleet** <br>needs `fleetrun` | The golden run covers one world. Generating each of 120 twice and comparing is the fleet-scale version, and it is how the vent leak would have been caught the day it landed. | Prove | M | 3 |
| 311 | **A nightly job, and a fast subset** <br>needs `baseline` | Twelve worlds on every commit, 120 nightly. The fast subset should be chosen to span the categories, not to be the first twelve. | Prove | M | 3 |
| 312 | **Report to somewhere people look** <br>needs `baseline` | A generated page with the contact sheet, the metric table and the deltas since last release. The project already publishes fifteen generated pages; this is the sixteenth and the most operational. | Prove | M | 3 |
| 313 | **Do not let the guard become the goal** <br>needs `baseline` | Every metric here is a proxy. The moment a change is made to move a number rather than to improve a picture, the suite has started doing harm. | Prove | S | 3 |
| 314 | **Test the tests** <br>needs `baseline` | Inject a known artefact — a face seam, a flattened palette, a clipped highlight — and assert the detector catches it. A detector nobody has proven can detect anything is decoration. | Prove | M | 3 |
| 315 | **Record what each metric caught** <br>needs `baseline` | Over a year, which detectors earned their keep. It is how the suite gets pruned rather than accumulating forever. | Prove | S | 2 |
| 316 | **Keep the whole suite runnable offline** <br>needs `baseline` | Every probe in this project runs from the repository with no network. The render suite should keep that property, which rules out fetching reference imagery at test time. | Prove | S | 3 |
| 317 | **A single command for the picture** <br>needs `baseline` | `npm run picture` — render the fleet, compute the metrics, diff the baselines, write the contact sheet, print the table. The review surface for constructs 19 and 20 together. | Prove | M | 3 |
| 318 | **Write the acceptance criteria down** <br>needs `baseline` | What must be true for the picture to ship. Nothing in this project currently states that, which is why every rendering change is a matter of taste and memory. | Prove | M | 3 |
| 319 | **Guard the guards' cost** <br>needs `baseline` | A test suite that takes an hour gets skipped. Track its runtime as one of the metrics. | Prove | S | 2 |
| 320 | **The realism backlog's finding, closed** <br>needs `baseline` `fleetrun` | "The picture has never been measured" has been true through five backlogs. Baselines across the fleet is the sentence that ends it, and nothing smaller does. | Prove | M | 3 |

## Art direction is two lambdas

`applyPlanetLook(rule)` sets exactly two fields: `rule.land` and `rule.ocean`. That is the whole of a world's art direction. Lighting, exposure, atmosphere, sky colour, limb, haze layering, detail grammar, sound, named features and the camera that introduces it are decided elsewhere, by code that does not know which world it is drawing, or not decided at all.

| # | Item | What and why | Kind | E | I |
|---|---|---|---|---|---|
| 321 | **Art direction is two lambdas** <br>gives `artdir` | `applyPlanetLook(rule)` sets exactly `rule.land` and `rule.ocean` — two functions from the 30-entry PAINT table. That is the whole of a world's art direction. Lighting, exposure, sky, limb, haze, detail grammar, sound, named features and the camera that introduces it are decided elsewhere by code that does not know which world it is drawing. | Draw | L | 3 |
| 322 | **One object per world type, decided together** <br>needs `artdir` | Palette, illumination, atmosphere, limb, detail grammar, soundscape, named features, hero camera. The argument for a single object is that these are not independent: a hard limb, a black sky and high-contrast regolith are one decision about an airless world, made three times today in three files. | Draw | L | 3 |
| 323 | **Art direction belongs in the world definition** <br>needs `artdir` `worldrec` | It is the part of construct 16 that was explicitly left out — the worldspace pass's own note says "Look is still code". This is where the two constructs meet and it is the largest single block of code left to move. | Draw | M | 3 |
| 324 | **Direction by class, override by body** <br>needs `artdir` | Airless rock, icy moon, ocean world, lava world, gas giant, ice dwarf. Eight or ten classes with per-body overrides, not 120 art directions — the same inheritance argument that keeps the data table small. | Draw | M | 3 |
| 325 | **A world should read at thumbnail size** <br>needs `artdir` `contact` | The stated test from the worldspace pass: could somebody who knows the Solar System name the body from one frame. Art direction is the discipline that makes that true and the contact sheet is how it is judged. | Draw | M | 3 |
| 326 | **Direction has to survive the god layer** <br>needs `artdir` | A player who terraforms Mars should get a Mars that has been terraformed, not a generic green world. That means direction is a function of state, not a skin applied at generate. | undefined | M | 2 |
| 327 | **Direction has to survive an epoch change** <br>needs `artdir` | Archean Earth and Holocene Earth are the same body and different pictures. If direction is keyed on kind alone it cannot express that, and the worldspace pass wants epochs to be worlds. | Draw | M | 3 |
| 328 | **Say what a world is meant to feel like** <br>needs `artdir` | One sentence per class, in the data, next to the numbers. "Old, still, high-contrast, no air" is a direction; a palette is its consequence. Without the sentence the numbers drift and nobody can say why they are wrong. | Prove | S | 3 |
| 329 | **The unknown world needs a direction too** <br>needs `artdir` | Ninety-four exoplanets whose appearance is entirely extrapolated. "Provisional" is a legitimate art direction — desaturated, softly lit, deliberately unresolved — and it is more honest than confident invention. | Draw | M | 3 |
| 330 | **Avoid the uniform-grey failure** <br>needs `artdir` | Half the Solar System is grey rock or dirty ice and the temptation is one grey. The measured albedo differences between Callisto, Ganymede, the Moon and Mercury are larger than they look and they are published. | Draw | M | 2 |
| 331 | **Direction should name its references** <br>needs `artdir` | Which photograph, which mission, which processing. It is the provenance discipline applied to look rather than to physics, and it is what lets a second person continue the work. | Prove | M | 3 |
| 332 | **A hero camera per world** <br>needs `artdir` `headlessgl` | The angle, distance and sun position that show a world at its best. It is what the picker uses, what the contact sheet uses, and what the render test fixes so comparisons mean something. | Draw | M | 3 |
| 333 | **The first frame is the product** <br>needs `artdir` | The landscape pass made the opening world a choice and the worldspace pass restated it. Which frame of which world you see first is an art-direction decision currently made by a default camera. | Draw | M | 3 |
| 334 | **Direction across zoom rungs** <br>needs `artdir` | A world must stay itself from orbital thumbnail to the ground. The detail grammar is the part of direction that operates below the cell and it is currently shared by every world. | Draw | M | 3 |
| 335 | **Two worlds should be comparable on purpose** <br>needs `artdir` `pairdist` | Europa and Enceladus are both bright ice moons and should look related and distinguishable. That is a decision about a pair, which is why direction cannot be made 120 times in isolation. | Draw | M | 3 |
| 336 | **Direction is where the physics gets a vote** <br>needs `artdir` | The temptation is to art-direct toward the reference photograph and quietly override the model. Where they disagree, the honest move is to say so — the picture is a claim about the model. | Prove | M | 3 |
| 337 | **A style guide for the whole catalogue** <br>needs `artdir` | What this project's planets look like: how saturated, how contrasty, how much detail, how the limb reads. A per-world direction without a house style produces 120 unrelated pictures. | Draw | M | 3 |
| 338 | **Let direction be edited live** <br>needs `artdir` `palview` | The palette editor extended to the whole direction object. It is the difference between art direction as a code change and art direction as a craft. | undefined | M | 2 |
| 339 | **Direction should be diffable and reviewable** <br>needs `artdir` | As data with a thumbnail, a change to Titan's look is a pull request with a picture in it. As 37 lambdas it is a diff of `lerp` constants. | Prove | M | 3 |
| 340 | **The success condition** <br>needs `artdir` | A world's entire look is data, a new world needs no code, and the contact sheet shows 120 distinguishable places. Everything else in this category is a step toward that sentence. | Prove | S | 3 |

## Light, air and the edge of the disc

Mercury at 6.7 solar constants and Pluto at 0.001 are drawn with the same exposure curve. A hard limb means airless, a soft blue one means thin, a layered orange one means Titan — and the limb is one of the two edges the realism pass identified as surviving a thumbnail. None of that is per-world today.

| # | Item | What and why | Kind | E | I |
|---|---|---|---|---|---|
| 341 | **Exposure and tonemap per world** <br>needs `illum` `artdir` | Mercury at 6.7 solar constants and Pluto at 0.001 share a curve today. It is the single largest reason a distant world does not read as distant, and the realism pass built the eye-adaptation machinery it needs. | Draw | M | 3 |
| 342 | **The limb is the world's signature** <br>needs `artdir` | Hard means airless, soft blue means thin, layered orange means Titan. The realism pass identified the limb as one of two edges surviving a thumbnail and it is drawn by one shared shell. | Draw | M | 3 |
| 343 | **Haze in layers** <br>needs `artdir` | Pluto's haze is layered to 200 km and the backlit images are among the most beautiful planetary photographs there are. It is a stack of thin shells and it is cheap. | Draw | M | 3 |
| 344 | **Atmosphere thickness should be visible** <br>needs `artdir` | `scaleHeightKm` is implemented. A puffy hydrogen envelope on a low-gravity world and a thin cold one look completely different at the limb and the number already exists. | Draw | M | 3 |
| 345 | **The sky as seen from the ground** <br>needs `illum` | Rayleigh blue, Mars butterscotch with a blue sunset, Titan orange gloom, Venus a dim yellow-grey. It is half of what a place feels like from the ground and the local view has never shown a sky. | Draw | M | 3 |
| 346 | **A sky with something in it** <br>needs `artdir` | Europa with Jupiter filling a third of the sky; a binary with two suns; a rogue world with nothing. `MOON_PARENTS` knows the geometry and nothing draws it. | Draw | M | 3 |
| 347 | **Rings, and their shadow** <br>needs `artdir` | Saturn is in the catalogue. The rings are the most recognisable structure in the Solar System, they are a Roche-limit consequence, and their shadow band on the cloud tops moves with the season. | Draw | M | 3 |
| 348 | **Aurorae where there is a field** <br>needs `artdir` | `magnetosphere` exists on the ruleset. Jupiter's aurorae are powered by Io's plasma torus rather than the solar wind, which is a nice example of a look that is a physical argument. | Draw | M | 2 |
| 349 | **Night side content** <br>needs `illum` | Lava glow, lightning, aurorae, bioluminescence, city lights. Five emissive sources, four of which exist as fields, and the night side is currently the day side at low brightness. | Draw | M | 3 |
| 350 | **Detail grammar per substrate** <br>needs `artdir` | Sand ripples, lava ropes, ice crevasses, karst fluting, tundra polygons, regolith mottle. The surface backlog's `detailfield` is the mechanism; which grain a world uses is art direction. | Draw | M | 3 |
| 351 | **Cloud character, not just coverage** <br>needs `artdir` | Earth's cloud is structured by fronts and the ITCZ; Venus is a smooth deck; a giant is banded. Same field, three completely different looks. | Draw | M | 3 |
| 352 | **Specular as a material statement** <br>needs `illum` | The glint off Titan's lakes is how they were confirmed. A specular return says "liquid" more clearly than any colour, and the ocean already has a GGX lobe nothing else does. | Draw | M | 2 |
| 353 | **Shadow length and terrain relief** <br>needs `artdir` | Low sun throws long shadows and that is when terrain reads. The exaggeration is per-ruleset — Earth 0.028, others 0.05 — and it is an art-direction constant hiding in the physics. | Draw | M | 3 |
| 354 | **A world seen from its own surface** <br>needs `artdir` | The pitch is "hold a planet, shrink, walk in". What you see when you get there — the sky, the ground, the light, the horizon distance — is a per-world direction that has never been made. | Draw | L | 3 |
| 355 | **Horizon distance is a giveaway** <br>needs `artdir` | On a small moon the horizon is a few kilometres away and visibly curved. It is one of the strongest cues to scale and it follows from the radius. | Draw | M | 2 |
| 356 | **Motion is part of the look** <br>needs `artdir` | A fast rotator, a locked world, a wobbling moon. How a world moves under the camera is direction and it is currently one rotation speed. | Draw | M | 2 |
| 357 | **The picker thumbnail is a different crop** <br>needs `artdir` `contact` | A thumbnail is not a small hero shot; it needs more contrast and a tighter crop. The landscape pass built a nine-globe picker and it is where this is judged. | Draw | M | 2 |
| 358 | **Do not let one shader own every world** <br>needs `artdir` | The surface path is shared and that is right for correctness. Direction has to be able to change what it does per world without forking it, which means uniforms and data rather than branches. | Draw | M | 3 |
| 359 | **Look budget per world** <br>needs `artdir` | Haze layers, cloud shells, emissive passes and detail octaves all cost. A world with rings and aurorae and layered haze is more expensive than a bare rock, and the budget should be stated per class. | Prove | M | 2 |
| 360 | **Assert the look responds to the physics** <br>needs `artdir` `headlessgl` | Halve the insolation and the render should get dimmer and redder. Remove the atmosphere and the limb should harden. Two assertions that prove direction is wired to the model rather than painted on. | Prove | M | 3 |

## A world with no names is a texture

Sputnik Planitia, Caloris, Valles Marineris, the tiger stripes, the Great Red Spot. Every Solar System body has named features that are the reason anyone recognises it, and `W._plateNames` names plates while nothing names a landform. Sound is Earth's everywhere. A place is a name, a sound and a thing worth flying to.

| # | Item | What and why | Kind | E | I |
|---|---|---|---|---|---|
| 361 | **Named features, per world** <br>gives `placenames` | `W._plateNames` names plates and nothing names a landform. Sputnik Planitia, Caloris, Valles Marineris, the tiger stripes, the Great Red Spot — these are the reason anyone recognises a body, they are published for every Solar System world, and they are absent. | Draw | M | 3 |
| 362 | **Generated names for generated worlds** <br>needs `placenames` | The seed-word system names worlds and the life pass names clades. A named continent, range, sea or crater is what turns a heightfield into a place somebody can talk about. | Draw | M | 3 |
| 363 | **A place worth flying to** <br>needs `placenames` `artdir` | Each world gets one signature feature, placed, named and findable, which also gives the camera a destination and the picker a subject. | Draw | M | 3 |
| 364 | **Naming conventions per world** <br>needs `placenames` | The IAU has themed nomenclature per body — Pluto's features are named for explorers of the underworld. Copying the *convention* rather than the names is what makes generated names feel deliberate. | Draw | M | 2 |
| 365 | **Labels that survive a zoom** <br>needs `placenames` | A name at orbital scale, a name at regional scale, a name on the ground. Which names appear at which rung is a cartography problem with a well-known answer. | Draw | M | 2 |
| 366 | **Sound per world** <br>gives `worldsound` | `audio.js` layers a soundscape and it is Earth's everywhere. Wind needs an atmosphere; an airless world is silent; a dense atmosphere carries low frequencies further. It is a strong identity cue and almost nothing uses it. | Draw | M | 2 |
| 367 | **Silence is a legitimate soundscape** <br>needs `worldsound` | On the Moon there is nothing to carry sound. Doing that honestly — and letting the interface say why — is more effective than inventing ambience. | Draw | S | 2 |
| 368 | **Sound from the material** <br>needs `worldsound` | Regolith, ice, sand and rock sound different underfoot. The substrate field already knows which, and the local view is where a footstep would be. | Draw | M | 1 |
| 369 | **Sound from the processes** <br>needs `worldsound` | A geyser field, a lava lake, a storm, a river. The events exist in the chronicle and none of them makes a noise. | Draw | M | 2 |
| 370 | **A world description worth reading** <br>needs `placenames` | The catalogue already carries good prose per body. What is missing is the generated half — what this run of this world actually became — which the life and worldspace passes both produce material for. | Draw | M | 3 |
| 371 | **A world card** <br>needs `placenames` `contact` | Thumbnail, seven axes, signature feature, palette swatches, provenance mix, one sentence. It is the artefact somebody shares and every field on it exists or is in this document. | Draw | M | 3 |
| 372 | **The chronicle should name the place** <br>needs `placenames` | "A flood basalt covered the northern plain" is a sentence; "volcanism event at cell 14822" is a log line. Names are what make the chronicle narrative rather than telemetry. | Draw | M | 3 |
| 373 | **Places should persist across a save** <br>needs `placenames` | A named feature a player has visited must survive a reload, or naming is decoration. The save format has grown to hold terrain and genomes; names are small. | Model | S | 3 |
| 374 | **Let a player name something** <br>needs `placenames` | A player who names a mountain remembers it. It costs almost nothing and it is one of the strongest attachment mechanisms a world simulator has. | undefined | S | 3 |
| 375 | **A place needs a scale cue** <br>needs `placenames` | Valles Marineris is 4,000 km long. Without a stated size a named feature is a label on a texture, and the product has struggled to communicate scale since the first backlog. | Draw | M | 3 |
| 376 | **Real names only where they are real** <br>needs `placenames` | A generated coastline must never carry an Earth name, and an exoplanet must never carry an invented name presented as established. The landscape pass made this rule for terrain and it applies harder here. | Prove | S | 3 |
| 377 | **The named features should be findable from the interface** <br>needs `placenames` | A list per world, click to fly. It is the navigation primitive the catalogue has never had and it makes 120 worlds explorable rather than listed. | Draw | M | 3 |
| 378 | **Features from the landform grammar** <br>needs `placenames` | `landforms.json` has 21 forms. A named feature should be an instance of one, so the name comes with a process and an explanation rather than being decoration. | Model | M | 3 |
| 379 | **Say which names are real** <br>needs `placenames` | Caloris is real. A generated crater name is not. Same provenance discipline, applied to nomenclature, and it is exactly the kind of thing that quietly misinforms. | Prove | S | 3 |
| 380 | **A place is a name, a sound and a reason to go** <br>needs `placenames` `worldsound` | Three cheap things that between them are most of the difference between a rendered sphere and somewhere. None of the three is a rendering problem. | Draw | M | 3 |

## Somebody has to look at all of them

One hundred and twenty worlds, and no view that shows more than one. The contact sheet has been asked for in two previous passes and does not exist. Art direction is not a per-world decision made 120 times in isolation; it is a set of decisions made against each other, which requires seeing them together.

| # | Item | What and why | Kind | E | I |
|---|---|---|---|---|---|
| 381 | **The contact sheet** <br>gives `contact` <br>needs `headlessgl` | One hundred and twenty thumbnails on one page, regenerated on every change. It has been asked for in two previous passes and does not exist. It is the review artefact that makes every claim in this document checkable by eye in five seconds, and it is a by-product of the render harness. | Prove | M | 3 |
| 382 | **Sort the sheet by physics, not by category** <br>needs `contact` | Grouped by the seven axes rather than by how each body was discovered. Worlds that are physically similar should sit together, and the ones that look identical without being similar are the bug. | Prove | M | 3 |
| 383 | **Show the nearest neighbours** <br>needs `contact` `pairdist` | For each world, the five that look most like it. It is how a person finds the Io cluster without being told to look for it. | Prove | M | 3 |
| 384 | **Diff two contact sheets** <br>needs `contact` | Before and after a change, side by side, with the biggest movers first. It turns a rendering change from an assertion into an argument. | Prove | M | 3 |
| 385 | **A sheet per category** <br>needs `contact` | All 21 giants together; all 15 moons together. Within-category sameness is the failure that matters and it is invisible on a mixed sheet. | Prove | S | 3 |
| 386 | **Reference photographs beside the renders** <br>needs `contact` `spectrum` | Twelve Solar System bodies have public imagery. Putting the render next to the photograph on the same sheet is the only external check the picture has. | Prove | M | 3 |
| 387 | **The sheet should carry the metrics** <br>needs `contact` `edgemetric` | A thumbnail with its numbers underneath. Pictures alone become a taste conversation; numbers alone become a metric-gaming exercise. Together they are a review. | Prove | M | 3 |
| 388 | **Publish the sheet** <br>needs `contact` | The project publishes fifteen generated pages. A contact sheet of every world in the catalogue is more compelling than any of them and it costs one more generator. | Draw | M | 3 |
| 389 | **A curation pass is a scheduled activity** <br>needs `contact` | Somebody looks at all 120 and writes down what is wrong. It is not automatable, it is the thing the automation exists to support, and it has never happened. | Prove | M | 3 |
| 390 | **Keep a list of known-bad worlds** <br>needs `contact` | Which bodies currently look wrong and why. It is the work list that the fleet run produces and the contact sheet confirms. | Prove | S | 3 |
| 391 | **Curate the opening set** <br>needs `contact` | The landscape pass built a picker with nine worlds. Which nine, and in what order, is a curation decision that shapes everyone's first impression. | Draw | M | 3 |
| 392 | **A hero shot per world** <br>needs `contact` `artdir` | Higher resolution, best angle, best light. It is what the world card, the README and any sharing surface want, and it is the hero camera from the direction object. | Draw | M | 3 |
| 393 | **The README should show more than Earth** <br>needs `contact` | `site/img/` has four screenshots, all Earth. A project whose argument is that it has 120 worlds shows one. | Draw | S | 3 |
| 394 | **An animated sheet** <br>needs `contact` | Each thumbnail rotating, or stepping through an epoch. Motion reveals the seams and the banding that a still frame hides. | Draw | M | 1 |
| 395 | **Curate across zoom, not just across worlds** <br>needs `contact` | The same world at four rungs, in a row. It is how the zoom contract gets reviewed and it is where detail synthesis succeeds or fails. | Prove | M | 3 |
| 396 | **Show what a world is meant to feel like next to what it does** <br>needs `contact` `artdir` | The direction sentence beside the render. When they disagree, one of them is wrong, and the disagreement is the most useful thing on the page. | Prove | M | 3 |
| 397 | **Let somebody else review it** <br>needs `contact` | Everything in these five backlogs has been produced by looking at the code. A person who knows planetary science looking at the contact sheet would find things no metric will. | Prove | S | 3 |
| 398 | **Track how the sheet changes over releases** <br>needs `contact` `baseline` | A year of contact sheets is the clearest possible record of whether this programme worked. | Prove | M | 2 |
| 399 | **Curation feeds back into the data** <br>needs `contact` `worldrec` | The output of looking at 120 worlds is edits to definitions, not edits to code. That is the loop this entire document is trying to close. | Prove | M | 3 |
| 400 | **One hundred and twenty places** <br>needs `contact` | The stated goal of construct 20. The test is not that the catalogue has 120 entries — it has had that for a year — but that somebody scrolling the sheet sees 120 places and can say what each one is. | Prove | S | 3 |

## Sequencing

1. **Count what is code and print it.** `dataratio` and `newworldcost` are generated by `node scripts/data.mjs`. Per-body `land*`/`stamp*`/`paint*` are gone; named kinds are a table.
2. **Build the render harness.** `headlessgl`. One world, one PNG, one number, end to end. Every ambitious testing plan in five backlogs has been blocked on not having this, and it is the same machine that produces the contact sheet.
3. **Make the contact sheet.** `contact` and `fleetrun`. One hundred and twenty thumbnails on one page. It has been asked for in two previous passes; it is a by-product of the harness; and it is the first time anybody will have seen most of this catalogue.
4. **Then the metrics and the baselines.** `edgemetric`, `pairdist`, `baseline`. Axis-aligned runs, contour steps, per-face discontinuity, palette entropy, and the pairwise distance that finds worlds sharing a look. Commit them, budget them, ratchet them.
5. **Then the world record that joins the tables.** `worldrec`. The five data files plus a join is what turns a vocabulary into a definition. Selection rules rather than assignments, inheritance rather than duplication, and overrides that have to state a reason.
6. **Then move the PAINT table into ramps.** `matramp`, `paintdata`. Thirty kinds, 37 lambdas, 85 `lerp` calls, each a function of four inputs — which is a ramp table. It is the largest single block of appearance code and the one most worth having as data.
7. **Then the illuminant.** `illum`. Reflectance times illuminant, exposure per world, sky as a second light source. `sensory.js` already does this integral for eyes. It is the change that makes a red-dwarf world look like one rather than like Earth with a filter.
8. **Then the per-cell stack.** `colstack`, fixed depth first. Eight layers is 2.2 MB at N=96. The recipe becomes the stamp rather than the answer, `W.substrate` becomes derived, and deposition finally has somewhere to put things.
9. **Then the stack operations.** `stackops`, `deposit`. Six verbs — erode, deposit, intrude, melt, weather, compact — defined once, with rates from `processes.json`, mass balance asserted, and a merge policy that bounds the depth.
10. **Then stratigraphy as history.** Unconformities, ash markers, biological rock, the fossil record in layers, and colour banding that comes from real layers rather than from a classifier threshold. This is where the geology, life and surface passes all meet.
11. **Then art direction as an object.** `artdir`, `placenames`. Palette, illumination, atmosphere, limb, detail grammar, sound, named features and a hero camera, decided together per class with per-body overrides — and moved into the world definition, which is the half of construct 16 that was explicitly deferred.
12. **Then curate, and let the loop close.** Somebody looks at all 120, writes down what is wrong, and the output is edits to data rather than edits to code. That sentence is the whole programme, and every item above exists to make it true.

The through-line: four of the five constructs in this pass are one machine each, and two of those are the same machine. A render harness produces the artefact detector *and* the contact sheet *and* the world cards *and* the picker thumbnails *and* the README images — and until it exists, five backlogs' worth of visual claims are unverifiable by anything except somebody looking at a screenshot, which is how every fault in this programme has been found so far.

The data layer is real and it stopped short of the thing that matters. Five tables describe a vocabulary; what is missing is the join that makes a sentence, and in its absence 64 hand-written functions are still the grammar. The worldspace pass's own note says it plainly: look is still code, stamps are still code. Moving them is the work, and the order is: count what is code, build the harness so the move is checkable, then move the largest block first.

The stack is the one construct here that has not started, and it is the one the most other work waits on — deposition, burial, differential erosion, the fossil record and stratigraphic colour all need a surface that is more than one byte deep. Eight layers is 2.2 MB at play resolution. The recipe that exists today is not wasted: it becomes the thing that stamps the initial condition, and then processes get to change it, which is the entire difference between a description and a history.

