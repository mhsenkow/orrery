# ORRERY — thrive

**611 items.** Generated from `scripts/thrive.mjs` — edit that file, not this one, then run `node scripts/thrive.mjs`.

Life that grows, flourishes and moves into the next thing while you watch it: ecosystems, hunting, swarms, colonisation fronts, settlements that become cities, the in-app drive model that decides how any of it behaves — and the planet itself as something that can regulate, fight, notice, and (when labelled) think.

The measurement this pass starts from, all of it from a twenty-line probe against the shipped code: on the world the app opens with, **300 ticks advance the clock by zero years** and `meanLife` falls 38%. On a 1.7 Gyr deep-time run, **zero beings die**, every survivor is at maximum age, and 49 living lineages are drawn with **4 sprite kinds**. On modern Earth, 300 ticks produce **one settler and no towns**, and 60% of visible life is the reef sprite. `agentsTick` runs in the render loop, so none of the above is in the headless runner, the save file or the test suite — where **1** of 542 assertions mentions a being. Above that layer, `gaiaPolicyTick` is four if-statements on two globals while `gaiaTick` computes a state vector no controller reads, and tipping elements that cannot tip.

Kind: **272** model, **156** picture, **59** verbs, **124** measurement and proof. Effort is S/M/L. Impact is 1–3.

## What the audit found

- **Three hundred ticks advance the clock by zero years.** Measured on `terra` without `deepTime` — the world the app opens with. `advanceClock` clamps `ageYr` to `PRESENT_YR` and then returns `dtYr = 10`, so `W.ageYr` is identical after 300 `simTick` calls. Over those same 300 ticks `meanLife` falls from **0.1463 to 0.0911**, a 38% decline with no forcing applied, and `W.tree.living` stays at 4 — the four lineages `seedHoloceneTree` planted at generate. Nothing can grow before your eyes on the default world because time is not passing and the only trend is downward.
- **The visible life is not in the simulation.** `agentsTick()` is called from the render loop at `vr/main.js:1248`, not from `simTick`. So every being on the globe is outside the simulation: absent from `runHeadless`, absent from `serializeRun` — a save restores the phylogeny and every genome and not one individual — and absent from the test suite, where **1** of 542 `ok(` calls mentions an agent, a city or a behaviour. The layer the player watches is the only layer nothing checks, and its step count depends on frame rate.
- **Nothing is born and nothing dies.** Measured over 300 deep-time ticks — 1.745 Gyr — with agents running: `ENT.n` goes 252 → 489, `dead` is **0**, and `maxAge` is **300**, meaning every being alive at the end had been alive since the first tick. The 237 new beings were not born; `topUpEntities` wrote them into free slots by sampling `life[c]`. Population is a rendering of a scalar field, so it cannot boom, crash, bottleneck, disperse or recover.
- **Nothing hunts anything, and nothing flocks.** `updateFoodWeb` is real work and it is entirely lineage-level: `a.diet` is three lineage ids and Lotka–Volterra runs on `n.censusPop`, **one number per lineage for the whole planet**, so predation has no location, no chase, no kill and no carcass. Grep `vr/` for `flock`, `territory`, `hunger`, `breed`, `offspring`, `colony` or `pheromone`: zero hits between them. `hunt` appears four times and every one is interface copy about a camera.
- **An ecosystem is four scalars.** `W.trophic` is `{prod, herb, carn, decomp}` computed as `nppMean` times 1, 0.1, 0.01 and 0.3, and `W.herbivore` and `W.carnivore` follow from it. So the herbivore load on a rainforest cell equals the herbivore load on an ice cell. `guildDens` in `redox.js` is a genuine per-cell guild field for metabolisms; nothing equivalent exists anywhere above the microbial layer.
- **Three hundred ticks, one settler, zero towns.** Measured on modern Earth with agents running for 300 ticks: 326 beings alive, **1** of kind 5, `meanBuild` 0.00000, `W.cities` empty. `kindForCell` gives a settler a `0.03 + build × 0.2` chance on `earthLike` and `agentsTick` multiplies the build rate by 0.12 there — two undocumented throttles on the same loop. On the same run **196 of 326 beings, 60%, were the reef sprite**.
- **Forty-nine lineages, four visible forms.** Measured on the 1.7 Gyr run: `W.tree.living.length` is 49 and the beings on screen span **4 distinct sprite kinds** — canopy, grass, sparse plant, reef. `morphTileOf` stamps expressed plans into a 20-slot atlas with 16 hand-drawn fallbacks. The modelled diversity is an order of magnitude past what the picture can show, and the cap is in the atlas rather than in the model.
- **Gaia is four if-statements.** `gaiaPolicyTick` in `god/observe.js` is the whole autopilot: too cold raise solar, too hot lower it, CO₂ low inject, CO₂ high draw down. Four thresholds on two globals, writing `W.solar` and `W.gases.CO2` directly rather than through the fifteen modules of god verbs. Meanwhile `gaiaTick` computes resilience, seven tipping elements with hysteresis, rate stress, feedback gain and a Medea score — a rich state vector that no controller reads. A second controller, `thermostatTick`, can pin temperature by cheating solar at the same time.
- **The planet does not notice you.** `notice.js` is civilisation: awareness from `interventionLog`, worship styles, a prayer queue. It returns early unless `meanBuild` is high, so a Precambrian world cannot notice a god at all. `playStyle` and `restraintStats` already classify the player; `forecastAct` already predicts a tool; Gaia reads none of them. A sentient planet that cannot tell a brush stroke from weather is not looking.
- **Seventy-two overlays and not one is behaviour.** The overlay table runs from temperature through vorticity, ENSO, sea state, crust age and the technosphere. Its four biosphere entries — `npp`, `guild`, `diversity`, `range` — are all state. None of the 72 shows a rate, a flow, a movement or an interaction. Storms move on that globe; life does not. Tipping proximity, predation pressure and colonisation fronts are not in the table.
- **The food web is being truncated in every long run.** Measured: after 300 deep-time ticks `W.foodWeb.links.length` is exactly **200**, the value of `links.slice(0, 200)`. Nothing sorts by flux before cutting and nothing reports how many were dropped, so a truncated web reads as a complete one.
- **Tipping elements that cannot tip.** `TIPPING` lists seven organs. `W._amoc` is written `W.conveyor ?? W._amoc ?? 0.7` and never diagnosed; `_monsoon` is a constant 0.5; amazon reads a global forest fraction; coral reads `meanTemp`. Six of seven are geography and none of that geography is connected. A vital-sign list that cannot trip is a lie a planetary mind would be built on.

## Where the living layer actually is

- **The biology under all of this is good, and it is not connected to anything that moves.** The life pass shipped a genome that is data, 1.6 × 10²⁸ distinguishable bodies, Eigen thresholds, Kleiber densities, chirality gating trophic transfer, twelve morphological incompatibility rules with costs, and `sensory.js` deciding band by band what a world delivers to an eye. That is a serious model. What reads it at runtime is `agentsTick`, which uses `plan.stride` and a sprite index. The gap this backlog addresses is not the biology — it is everything between the biology and the screen.
- **The two-level architecture exists by accident.** Fields carry biomass and beings are sampled from them; beings then write back into `build[c]` and `h[c]`. Neither direction is designed, `topUpEntities` invents individuals from a scalar, and `node.pop` counts cells while `node.censusPop` integrates a differential equation, so the tree and the visible population can disagree with nothing noticing. Writing the contract down is the cheapest high-value item in this document.
- **The performance answer is already in the repo.** `rebuildBuckets` builds a per-cell linked list of beings every tick — the right spatial index for this grid. `gpgpu/` runs the climate in shaders with a CPU fallback. `worker.js` exists. A 78-weight policy at 1400 beings is 109,000 multiply-adds, under a tenth of a millisecond. Nothing in this backlog is blocked on speed; the constraint is the stated 12 ms tick budget, and most of the value here is arithmetic.
- **Agentic AI here means an evolved policy, not a language model in a tick.** The honest design is a small drive vector per being, perception gated on `sensory.js`, actions scored from an authored table, and the weights that do the scoring carried in the genome — so behaviour is selected by the same process that already shapes bodies. A generative model belongs at authoring time, compiled to frozen data through the pattern `lifegrammar.mjs` proved, with a provenance tag and a review gate. Both halves are buildable and only one of them is novel.
- **A planetary mind is the same architecture one level up.** `gaiaTick` is already a sensory stream. `W.tips`, `rateStress`, `medeaScore` and `disequilibrium` are already drives. The god layer is already fifteen modules of actuators on a budget with receipts. `gaiaPolicyTick` ignores all of that and writes two globals; `thermostatTick` can cheat a third. Sentience here is not a new soul — it is wiring those parts, labelling the mode so Daisyworld stays a proof, and letting the planet notice the hand the way `notice.js` already lets a civilisation notice.
- **The measurement discipline works, and it has never been pointed here.** Five hundred and forty-two assertions cover Eigen, Kleiber, chirality, genome round-trips and a 24-tick biosphere that must not collapse. One mentions a being. Every measured fact in the audit above was found in an afternoon with a twenty-line probe, which is simultaneously the strongest argument for committing the probe and the reason the zero-city and zero-death results survived this long.

## The critical path

The capabilities the largest number of other items are waiting on.

| Capability | Item | Unblocks |
|---|---|---|
| `pdrive` | 432. A drive vector for the planet | 27 items |
| `gaiaface` | 592. Show the metabolism, not a resilience number | 26 items |
| `tipgeo` | 492. Paint tipping state on the cells they name | 25 items |
| `metab` | 452. Show carbon breathing as a pulse on the land | 24 items |
| `planetmood` | 552. Mood is a projection of the state vector | 24 items |
| `homeo` | 472. The silicate weathering thermostat, properly | 23 items |
| `alienmind` | 572. Gate Gaia on air, water and a biosphere | 23 items |
| `entsim` | 21. Move beings inside the simulation tick | 22 items |
| `immune` | 512. The player is a symbiont or a pathogen | 22 items |
| `planetsee` | 532. Notice is civilisation; the planet needs its own | 21 items |
| `gaiapolicy` | 301. Four if-statements are the whole autopilot | 20 items |
| `planetclaim` | 412. Name the three modes and refuse a fourth | 20 items |
| `group` | 102. A group as an object | 17 items |
| `popbook` | 63. A population book per lineage | 15 items |
| `hunt` | 81. A hunt that happens on a cell | 15 items |
| `guildfield` | 141. A trophic field, not three multiplications | 14 items |
| `drivevec` | 42. A drive vector, not a behaviour string | 13 items |
| `lifelearn` | 281. Learning inside one lifetime | 12 items |
| `utility` | 242. Utility scoring as the base mechanism | 11 items |
| `behgenome` | 244. A behaviour genome, inherited and mutated | 11 items |

## Three hundred ticks, zero years

Measured: on `terra` without `deepTime` — the world the app opens with — 300 ticks advance `W.ageYr` by **exactly zero years**, because `advanceClock` clamps `ageYr` to `PRESENT_YR` and then sets `dtYr = 10`. Over those same 300 ticks `meanLife` falls from 0.1463 to 0.0911, a 38% decline, and `W.tree.living` stays at 4 — the four lineages `seedHoloceneTree` planted. Nothing grows before your eyes on the default world because time is not passing and the only trend is downward.

| # | Item | What and why | Kind | E | I |
|---|---|---|---|---|---|
| 1 | **Unpin the present** <br>gives `unpin` | `advanceClock` clamps `ageYr` to `PRESENT_YR` and returns `dtYr = 10` on the calibration Earth, so the world the app opens with cannot advance. Measured: 300 ticks, zero years elapsed. The calibration Earth has to stay reproducible, so the fix is a second mode — Earth at the present that is allowed to keep going — not a change to the pinned one. | Model | M | 3 |
| 2 | **A biology clock that is not the geology clock** <br>gives `biotime` <br>needs `unpin` | `adaptiveTickYears` gives one `dtYr` to tectonics, carbon, phylogeny and behaviour alike. At 5 Myr a tick, a generation is invisible; at 10 years a tick, an orogeny is. Life needs its own rate — generations per tick — advanced by the same wall clock but scaled separately, which is what makes evolution watchable without freezing the mountains. | Model | L | 3 |
| 3 | **Generations, not years** <br>needs `biotime` | The unit that matters for watching evolution is the generation, and it is species-specific: a microbial mat turns over in hours, a tree in a century. Derive generation time from body mass through the same Kleiber relation `kleiberDensity` already uses, and let each lineage accumulate generations at its own rate inside one tick. | Model | M | 3 |
| 4 | **Four times is not fast** <br>gives `livespeed` | `simAcc += dt * (W.fastForward ? 4 : 1)` — fast-forward is a 4× multiplier on an accumulator that is already gated at 0.09 s and abandoned above 12 ms. So the maximum observable rate is fixed by a frame budget rather than chosen. A speed control that means something needs sub-stepping the cheap systems many times per expensive one. | Model | M | 3 |
| 5 | **Substep life inside one climate tick** <br>needs `livespeed` `biotime` | Climate, ocean and tectonics are the expensive systems and the slow ones. Ecology, behaviour and population are cheap and want to run often. Run life N times per climate tick with N from the speed control, and the same frame budget buys an order of magnitude more biological time. | Model | M | 3 |
| 6 | **Say what the rate is, on screen** <br>needs `livespeed` | `W.dtYr` exists and the interface shows an age, not a rate. Print generations per second and years per second beside the clock, because the single most common confusion in a deep-time game is not knowing whether nothing is happening or nothing is being shown. | Show | S | 3 |
| 7 | **A speed dial with named stops** <br>gives `speeddial` <br>needs `livespeed` | Live, seasonal, generational, millennial, geologic. Each stop states its `dtYr`, which systems are sub-stepped and which are frozen, so the player is choosing a regime rather than a multiplier. `fixedDtYr` already exists as the override hook. | Play | M | 3 |
| 8 | **Never advance time without advancing something visible** <br>needs `speeddial` | The measured failure is 300 ticks with a still picture. Any tick that changes no visible field is a tick the player experiences as a hang. Assert it: over 100 ticks at every speed stop, at least one of life, population, range or build must change by a stated amount. | Prove | M | 3 |
| 9 | **The declining default** | Measured: `meanLife` 0.1463 → 0.0911 over 300 ticks on the default world, a 38% fall with no forcing applied. Something in the modern-Earth path is a net sink. Find it before building anything on top, because every growth feature will be fighting it. | Prove | S | 3 |
| 10 | **A growth budget per tick** <br>needs `biotime` | `bioTick` grows at `hab * frontier + nl * hab * 0.14` and dies at 0.035 or 0.06 per tick. Those are per-tick constants in a world whose `dtYr` spans 10 to 5,000,000. Rates have to be per-year and integrated, or the same model is a bloom at one speed and a collapse at another. | Model | M | 3 |
| 11 | **Make the fast-forward halt conditions biological** | `shouldHaltFF` stops the fast clock on anomalies. Add the biological ones: first predation, a lineage crossing 10% of the surface, a population crash over 50%, a settlement reaching city stage. Fast time should stop for the things worth watching. | Play | M | 3 |
| 12 | **Seasons that life can feel** | `nppField` has a green wave — `phenology` from `W.season` and latitude — and it is the only seasonal thing life reads. Migration, breeding windows, dormancy and mast years all hang off a seasonal phase that already exists and is used once. | Model | M | 2 |
| 13 | **A day, for the things that keep to one** | `isOutNow` in `present.js` already gates a being on a diel cycle and `pickBehav` reads it. That is the seed of a real activity budget: nocturnal, diurnal, crepuscular, tidal — each with a metabolic and a predation consequence rather than a rest-probability. | Model | M | 2 |
| 14 | **Tie generations to the tide where it matters** | `tideRange` and `intertidal` are computed fields and the intertidal is where spawning clocks come from on the real planet. A moon that sets a breeding cycle is a physical parameter with a visible biological output, which is this product at its best. | Model | M | 2 |
| 15 | **A run that is meant to be watched, not left** | The deep-time path is designed for a probe: 300 ticks, 1.7 Gyr, print a summary. The watched path needs the opposite defaults — slower time, faster life, more events per minute. State which of the two any given ruleset is for. | Model | S | 2 |
| 16 | **Catch up rather than drop** | `noteDroppedTicks` dumps the accumulator when a tick exceeds 12 ms, so a heavy frame silently loses simulated time. For behaviour that is fine; for population it is a leak. Mark which systems must be conservative under a dropped tick and integrate them on the wall clock instead. | Model | M | 2 |
| 17 | **One frame, one seed, one answer** | `rngOf(W, 'rngAgents')` gives behaviour its own stream, which is right. But `agentsTick` runs off the render loop, so the number of behaviour ticks depends on frame rate — the same seed on a fast machine produces a different world. Moving beings into `simTick` fixes it; until then it should be stated. | Prove | S | 3 |
| 18 | **Let the player scrub back** | `forkRun` exists and `serializeRun` is at version 5. A biosphere you can rewind ten thousand years to watch a radiation again is the single strongest way to make evolution legible, and the save path is most of the machinery. | Play | L | 3 |
| 19 | **The chronicle as a timeline, not a log** | `W.chron` is an append-only list rendered as text. The same data on a horizontal axis, with lineage bands, extinction pulses and settlement founding marked, turns a scrolling log into the shape of a history. | Show | M | 3 |
| 20 | **A stated eons-per-minute target** | Pick the number and defend it: the Cambrian in a minute, the Phanerozoic in ten, a civilisation in five. Every rate constant in this backlog then has a target to be tuned against instead of being tuned by feel. | Prove | S | 3 |

## The visible life is not in the simulation

Landed (partial). `agentsTick()` now runs inside `simTick` in `vr/world.js`, so beings participate in headless runs and the thrive test slice. Save **v8** stores build, settlements and every live individual (`packEntities` / `restoreEntities`). The suite has a dedicated beings / settlements / fire block — not one assertion. Still open: resolution remap, behaviour genome in saves, energy/hunger, birth/death as objects.

| # | Item | What and why | Kind | E | I |
|---|---|---|---|---|---|
| 21 | **Move beings inside the simulation tick** <br>gives `entsim` | Landed. `agentsTick()` runs from `simTick` in `vr/world.js`, not the render loop. Beings are part of the world clock, headless runs and saves. | Model | M | 3 |
| 22 | **Beings in the headless runner** <br>needs `entsim` | Landed. `runHeadless` ticks the full sim; `scripts/thrive-probe.mjs` and `vr/sim/livemetric.js` report population, settlements and fire. | Prove | S | 3 |
| 23 | **Beings in the save** <br>gives `entsave` <br>needs `entsim` | Landed (v8). `serializeRun` stores build, cities and `entities.list`; `loadRunMeta` restores clock, build and population. Resolution change still wipes beings. | Model | M | 3 |
| 24 | **A being count that survives a resolution change** <br>needs `entsim` | `changeResolution` reallocates every field and `respawnEntities` wipes and refills from scratch, so changing detail level kills the entire population. With beings holding cell indices this needs a remap, which is exactly the kind of thing that is invisible until somebody asserts it. | Model | M | 2 |
| 25 | **One assertion, today** <br>needs `entsim` | Landed (first slice). `vr/sim/test.mjs` has a beings block: population in `simTick`, determinism, generate reset, settlements, fire, herds, plumes, save round-trip. Birth/death objects and twenty assertions remain. | Prove | S | 3 |
| 26 | **A reset that actually resets** <br>needs `entsim` | The generate-reset test in the world-space pass found a vent field carrying 173 vents from one planet to the next. `ENT` is a module-level singleton with `_idSeq` never reset and `resetMorphAtlas` called only from `respawnEntities`. Assert that generating a new world leaves nothing behind. | Prove | S | 3 |
| 27 | **Beings are a view of the population, or they are the population** <br>needs `entsim` | Right now they are neither: `topUpEntities` samples `life[c]` to invent individuals, and those individuals then edit `build[c]` and `h[c]`. Pick one architecture. The honest one for this product is a two-level model — fields carry biomass, beings are sampled representatives that can also act — and it has to be written down. | Model | M | 3 |
| 28 | **A stated contract between field and being** <br>needs `entsim` | If `life[c]` is biomass and a being is a sample of it, then the number of beings on a cell is a function of that biomass and the beings' actions feed back into it. Both directions exist by accident today. Write the contract, assert conservation across it, and the two-level model stops being a leak. | Model | M | 3 |
| 29 | **Stop respawning the world every time it changes** | `respawnEntities` is called on generate and again from `main.js:439`, and it calls `resetMorphAtlas` and rebuilds from zero. Any long-lived individual — a named settler, a matriarch, a lineage founder — is destroyed by a routine event. Incremental repopulation is the requirement, not the optimisation. | Model | M | 3 |
| 30 | **Frame-rate independence, asserted** <br>needs `entsim` | Landed. Same seed + same `simTick` count → identical population signature in the beings block. | Prove | S | 3 |
| 31 | **A budget line for beings** <br>needs `entsim` | `W._msSim` records the sim tick and `agentsTick` is currently outside it, so the cost of the population is invisible in the only performance number the app keeps. Time it separately and show it, because every item in this backlog spends from that line. | Prove | S | 3 |
| 32 | **Cities in the tick, not on the frame** <br>needs `entsim` | `settleCities(W)` is called from `agentsTick` every 40 years of `W.year`, which means from the render loop. A settlement is a simulation object; it belongs on the simulation clock, and it needs to exist in headless runs before anything can be claimed about urban growth. | Model | S | 3 |
| 33 | **The population is a field too** <br>needs `entsim` | `rebuildBuckets` already builds a per-cell linked list of beings each tick. Keeping a `Uint16Array` count per cell alongside it gives every other system — predation, disease, grazing pressure, settlement demand — an O(1) query it currently cannot make. | Model | S | 3 |
| 34 | **A being belongs to a lineage, and the lineage should know** <br>needs `entsim` | `m.popId` points at a tree node and `lineageAt` resolves the reverse. But `node.pop` and `node.censusPop` are computed from cells, not from beings, so the tree and the visible population can disagree without anything noticing. One of them has to be the truth. | Model | M | 3 |
| 35 | **Kill the second cap** | `MAX_ENT` is 1400, `capForWorld` returns 560 on Earth-like worlds, `respawnEntities` computes a stride from the cap, and `topUpEntities` refuses to run above 85% of it. Four interacting limits decide how alive a world looks, and none of them is a stated design choice. | Model | S | 2 |
| 36 | **What happens to a being when the world ends** | `extinctionTick` removes lineages and `noteImpact` can sterilise a planet. Beings die only from `life[c] < 0.04` with a 0.12 roll. An extinction event should visibly kill the individuals it kills — that is the whole emotional content of an extinction. | Model | M | 3 |
| 37 | **Beings under fast-forward** <br>needs `entsim` | At 5 Myr a tick, individual behaviour is meaningless and individual rendering is expensive. Define the degradation: above some `dtYr`, beings become a density and a few named exemplars, and the transition is explicit rather than a frame-rate artefact. | Model | M | 3 |
| 38 | **Sprites are not the population either** | `morphTileOf` stamps unique plans into a 20-slot atlas with 16 hand-drawn Path2Ds as fallback. Measured on a 1.7 Gyr run: 49 living lineages, **4 distinct sprite kinds on screen**. The visible diversity is capped far below the modelled diversity and the cap is in the atlas. | Show | M | 3 |
| 39 | **Reef is not sixty percent of Earth** | Measured on modern Earth, 300 ticks: 196 of 326 beings were kind 14, the reef sprite. `kindForCell` returns 14 for any submerged cell with `reef > 0.25` or a life signal above 0.35, and most of the planet is ocean. The kind assignment needs to sample what is actually abundant, not what matches first. | Show | S | 3 |
| 40 | **A probe for the living layer** <br>gives `liveprobe` <br>needs `entsim` | Landed. `scripts/thrive-probe.mjs` plus `npm run probe` / `probe-fire`; thrive slice in `test.mjs`. | Prove | S | 3 |

## A being with no inside

An individual is 8 floats — position, scale, kind, three colour channels — plus a meta object holding `age`, `behav`, `plan`, `popId` and a name. `behav` is one of five strings picked by `pickBehav`, which is a probability roll against ash, dust, storm and ice. There is no energy, no hunger, no target, no memory of anywhere it has been, no relationship to any other being. Grep the whole of `vr/` for `hunger`, `territor`, `breed`, `offspring`, `colony` or `pheromone`: zero hits.

| # | Item | What and why | Kind | E | I |
|---|---|---|---|---|---|
| 41 | **Energy, so a life has a cost** <br>gives `energy` <br>needs `entsim` | The first missing quantity and the one everything else hangs off. A being has a store, spends it on movement, thermoregulation and growth, gains it by eating, and dies at zero. Kleiber already gives the metabolic rate from body mass and `plan.size` is already expressed from the genome, so the constants are not invented. | Model | M | 3 |
| 42 | **A drive vector, not a behaviour string** <br>gives `drivevec` <br>needs `energy` | `m.behav` is one of five strings chosen by a probability roll. Replace it with a small set of continuous drives — hunger, thirst, fear, heat stress, urge to breed, crowding — each a number that grows and is discharged by an action. Behaviour then falls out of which drive is loudest, which is both cheaper to reason about and richer to watch. | Model | M | 3 |
| 43 | **Perception before decision** <br>gives `percept` <br>needs `drivevec` | A being currently reads `life`, `moist`, `ice`, `ash`, `dust` and `stormField` on its own cell and four neighbours, with perfect knowledge and no range. `sensory.js` already decides what a world delivers to which receptor. Gate what a being knows on what it can actually sense, and a blind burrower and a hawk stop making the same decision. | Model | M | 3 |
| 44 | **A target, held across ticks** <br>needs `drivevec` | Every tick recomputes the best of five cells from scratch, so a being cannot cross a desert to reach a lake. One remembered destination and a heading turn Brownian shuffling into travel, and travel is the thing that reads as intent on screen. | Model | S | 3 |
| 45 | **Memory of a few places** <br>gives `memory` <br>needs `drivevec` | Three or four remembered cells per being — where food was, where water is, where the danger was — at a few bytes each. This is what produces trails, home ranges, seasonal returns and avoidance, all from one small array. | Model | M | 3 |
| 46 | **Age classes, so a body has a stage** <br>gives `agestage` <br>needs `energy` | `m.age` increments and is read only for a `Math.max`. Juvenile, adult, senescent — with different size, different diet, different vulnerability and different behaviour. `creatureDraw.js` already has a juvenile notion; nothing in the model does. | Model | M | 3 |
| 47 | **Size that grows with age** <br>needs `agestage` | `writeEnt` sets scale from `plan.size` and a random factor, once, at birth, and never again. A being that visibly grows is the cheapest possible signal that time is passing and that this individual has a history. | Show | S | 3 |
| 48 | **Thermoregulation as a real cost** <br>needs `energy` | `W.temp[c]` and the genome thermal axis both exist. An endotherm pays continuously to hold its temperature and an ectotherm pays in lost activity when it is cold — which is the actual reason the two strategies partition the planet, and it needs no new fields. | Model | M | 2 |
| 49 | **Water, separately from food** <br>needs `energy` | `moist`, `precip`, lakes and rivers are all computed. Thirst is the drive that makes a waterhole a place, concentrates animals, creates the ambush that predators want and makes drought lethal before it is visible in the biomass field. | Model | M | 2 |
| 50 | **Fear, and the cost of being afraid** <br>needs `drivevec` `percept` | A prey animal that will not feed in the open is a prey animal that starves slowly. That trade — the landscape of fear — restructures grazing pressure far more than predation deaths do, and it is a single drive plus a lookup of nearby predators. | Model | M | 3 |
| 51 | **Crowding, so a place can be full** <br>needs `drivevec` | `rebuildBuckets` already gives beings per cell. Making crowding a drive gives dispersal a cause, territory a meaning and cities a limit, and it is the term that stops every population from piling onto the single best cell. | Model | S | 3 |
| 52 | **Death with a stated cause, always** | `m.cause` is set to `ice`, `heat` or `starved` from a coin flip on `life[c] < 0.04`. Every death should have a cause that came from the model — starved, predated, frozen, drowned, diseased, old — because the causes of death are the honest summary of what a world is like to live in. | Model | S | 3 |
| 53 | **A body plan that constrains behaviour** | `planOf` expresses habitat, skeleton, limbs, senses and locomotion, and the only thing `agentsTick` reads from it is `stride`. A sessile plan should not walk, a gilled plan should not cross land, a phototroph should not chase. Twelve of those rules already exist as `morphMult` penalties on fitness; the same table should gate action. | Model | M | 3 |
| 54 | **Locomotion that costs what it should** <br>needs `energy` | Moving uphill, through snow, against a current or in a storm all cost more, and `h`, `ice`, `current` and `stormField` are all there. Movement cost is what makes terrain matter to an animal rather than only to a renderer. | Model | M | 2 |
| 55 | **One being, inspectable** <br>gives `beinginspect` <br>needs `drivevec` | `beingAtLocalPixel` already resolves a click to an individual. Show it: name, lineage, age class, energy, current drive, what it is doing and why, what it last ate, where it was born. This single panel is how a player learns that any of the rest of this exists. | Show | M | 3 |
| 56 | **Follow one for its whole life** <br>needs `beinginspect` | `followTarget` returns the first named settler. A follow camera that stays with one animal from birth to death, with a small caption when something happens to it, is the strongest thing this product could do with the layer it already renders. | Play | M | 3 |
| 57 | **Names that mean something** | `nameFrom` builds a name from two syllable tables for settlers and ice fauna. Names should be earned — the first of a lineage, the founder of a settlement, the largest, the oldest — because a named thing the player did not choose is noise. | Show | S | 2 |
| 58 | **A being state budget in bytes** <br>needs `drivevec` | Eight floats per being today. Energy, drives, target, memory and age class fit in roughly another twelve to sixteen, so 1400 beings costs about 130 KB total. State that number before building, and structure it as parallel typed arrays rather than growing the meta object. | Prove | S | 3 |
| 59 | **Behaviour that reads at a glance** <br>needs `drivevec` | The current five behaviours produce, measured: 216 resting, 92 foraging, 17 travelling, 1 fleeing out of 326. Three quarters of the visible population is doing nothing. Whatever replaces it should be asserted against a distribution the designer chose. | Prove | S | 3 |
| 60 | **The five strings, deleted** <br>needs `drivevec` | The end state for this category is that `pickBehav` no longer exists: no probability table, no string, no `roll < 0.18`. An action is chosen by scoring drives against perception, and the test is whether the code can be deleted rather than wrapped. | Model | M | 3 |

## Nothing is born and nothing dies

Measured over 300 deep-time ticks — 1.745 Gyr — with agents running: `ENT.n` goes 252 → 489, `dead` is **0**, and `maxAge` is 300, meaning every being alive at the end had been alive since the first tick. The 237 new beings were not born; `topUpEntities` wrote them into free slots from `life[c]`. Population is a rendering of a scalar field, so it cannot boom, crash, bottleneck, disperse or recover.

| # | Item | What and why | Kind | E | I |
|---|---|---|---|---|---|
| 61 | **Birth, so a population can grow** <br>gives `birth` <br>needs `energy` | Nothing is ever born. `topUpEntities` writes new beings into free slots from `life[c]` — measured, 252 → 489 beings over 1.7 Gyr with zero deaths and every survivor at max age. A birth needs a parent, a cost in energy, a place and a time, and it is the single change that turns a rendering into a population. | Model | M | 3 |
| 62 | **Death, from something** <br>gives `death` <br>needs `energy` | Measured `dead: 0` over 300 ticks. Death from starvation when energy hits zero, from predation, from exposure, from disease and from old age — each logged with its cause so the mortality profile of a world is legible. | Model | M | 3 |
| 63 | **A population book per lineage** <br>gives `popbook` <br>needs `birth` `death` | `node.pop` counts occupied cells and `node.censusPop` is a Lotka–Volterra scalar. Neither is a count of anything that exists. Keep births, deaths, immigration and emigration per lineage per tick, and every population claim becomes an accounting identity that can be asserted. | Model | M | 3 |
| 64 | **Reproductive strategy from the genome** <br>needs `birth` | The genome has axes for size, dispersal and defence, which is most of an r-versus-K position. Many cheap offspring with high mortality, or few expensive ones with care — that choice changes the shape of a population curve and it is already latent in the trait vector. | Model | M | 3 |
| 65 | **Parental care, and what it buys** <br>needs `birth` `agestage` | Juvenile survival as a function of an adult staying nearby. It is a small behaviour with a large demographic consequence and it produces the visual the player wants: a small one following a big one. | Model | M | 2 |
| 66 | **A carrying capacity a being can feel** | `carryingCapacityNPP` returns a per-cell number that only the field integrator reads. Beings should feel it through competition for food, not through a cap on a scalar, or the population curve will be smooth in a way real ones never are. | Model | M | 3 |
| 67 | **Boom and crash** <br>needs `popbook` | A predator that overshoots its prey and starves, a mast year, a die-off after a good decade. Overshoot needs a lag between resource and population, which needs birth and death rather than a logistic term. This is the payoff item for the whole category. | Model | M | 3 |
| 68 | **A bottleneck that shows in the genome** <br>needs `popbook` | `Ne = 0.35 × census` and drift is variance already. Once census comes from counted individuals, a crash leaves a real signature — reduced diversity, fixed alleles, a founder line — and the Lab tree can show the pinch afterwards. | Model | M | 3 |
| 69 | **A population pyramid in the readout** <br>needs `agestage` `popbook` | Juveniles, adults, old. The shape says whether a population is growing, stable or collapsing at a glance, and it is three numbers to compute and a small bar chart to draw. | Show | S | 2 |
| 70 | **Minimum viable population, per lineage** | `updateFoodWeb` uses `mvp = 2` for every lineage on every world. MVP depends on body size, reproductive rate and range. A whale and a bacterium do not have the same floor and the constant is doing real work in deciding who goes extinct. | Model | S | 3 |
| 71 | **The last individual** <br>needs `popbook` | When a lineage falls to one being, say so, name it, and mark it on the globe. The endling is the most affecting event a simulation like this can produce and it costs a threshold check. | Show | S | 3 |
| 72 | **Immigration and emigration, counted** <br>needs `popbook` | `m.cell` changes and nothing records that a being left one population and joined another. Counting the flow is what makes a metapopulation a metapopulation rather than a set of independent numbers. | Model | M | 2 |
| 73 | **Disease as a population process** | `W.plague` is a global scalar that multiplies `life[c]` by 0.55 with probability `plague * 0.03`. A transmissible disease needs a host density, a contact rate and a recovery — an SIR on the being buckets, which is a dozen lines and a genuinely different dynamic from a global multiplier. | Model | M | 3 |
| 74 | **Density-dependent everything** <br>needs `popbook` | Birth rate, disease, aggression, dispersal and stress all scale with local density, and density is the per-cell being count. One field, five consequences, and it is the mechanism that produces cycles without any cycling code. | Model | M | 3 |
| 75 | **A refuge that actually refuges** | `declareRefuge` exists as a god verb. With counted individuals a refuge becomes a source population that recolonises after a crash, which is what refugia do and what makes them worth declaring. | Play | M | 2 |
| 76 | **The population graph, live** <br>needs `popbook` | One line per major lineage, time on the x axis, live and scrolling. Every ecology textbook opens with this picture, no readout in the product draws it, and it is the fastest way to see a food web working. | Show | M | 3 |
| 77 | **Assert the accounting** <br>needs `popbook` | Population at t+1 equals population at t plus births minus deaths plus immigration minus emigration, exactly, every tick, for every lineage. If that identity holds, the demography is real; if it cannot be written, the model is still a rendering. | Prove | M | 3 |
| 78 | **Two lineages, one resource, a hundred ticks** <br>needs `popbook` | The smallest test that proves competition: seed two lineages with different efficiencies on one resource, run it, assert one wins and that which one wins follows from the traits. This is the ecology equivalent of the two-species predation test that already exists. | Prove | M | 3 |
| 79 | **Do not let the tree outrun the world** | Measured: 49 living lineages after 1.7 Gyr with 489 beings, so a typical lineage is represented by ten individuals — and 4 sprite kinds are shown. Either the tree is too fine for the population or the population is too small for the tree, and the ratio should be a stated design target. | Prove | S | 3 |
| 80 | **A world that is allowed to be empty** | The counterweight to everything in this category: an early Earth should look sparse, a snowball should look nearly dead, and `topUpEntities` currently guarantees a floor of visible activity. Emptiness has to be renderable or abundance means nothing. | Show | S | 3 |

## Nothing hunts anything

`updateFoodWeb` is real work and it is entirely lineage-level: `a.diet` is up to three lineage ids, `predation` and `preyAvail` are scalars on a tree node, and the Lotka–Volterra step runs on `n.censusPop` — **one number per lineage for the whole planet**. Predation therefore has no location, no chase, no kill, no carcass and no failure. The string `hunt` appears in `teach.js`, `god/tips.js`, `main.js` and `localview.js` — all of it interface copy about a camera.

| # | Item | What and why | Kind | E | I |
|---|---|---|---|---|---|
| 81 | **A hunt that happens on a cell** <br>gives `hunt` <br>needs `entsim` `percept` | `updateFoodWeb` decides that lineage A eats lineage B and applies a Lotka–Volterra term to a single planet-wide `censusPop`. A hunt is a predator on a cell, a prey within sense range, a pursuit that can fail, an energy transfer on success and a carcass. All of it needs the per-cell being buckets `rebuildBuckets` already builds. | Model | L | 3 |
| 82 | **Predation pressure as a field** <br>gives `preyfield` <br>needs `hunt` | Kills per cell per unit time, decayed. It is the field that makes prey avoid a place, makes a predator return to one, drives the landscape of fear, and gives the globe its first behavioural overlay. One `Float32Array` and an accumulate. | Model | M | 3 |
| 83 | **A pursuit the player can see** <br>needs `hunt` | Two sprites, a closing distance, an outcome. On the local grid this is the single most legible piece of behaviour possible, and it needs the predator and prey to be interpolated between cells — which `presentAgents` already does for movement. | Show | M | 3 |
| 84 | **Hunts that fail** <br>needs `hunt` | A success probability from the difference in speed, size, cover and the prey being aware. If every hunt succeeds, predators are a mortality constant. If they fail most of the time, the whole system gains the texture it should have and predator energy becomes a real constraint. | Model | M | 3 |
| 85 | **A carcass, and everything that comes to it** <br>gives `carcass` <br>needs `hunt` | `W.detritus` already accumulates from a biomass rain. A discrete carcass is a resource at a place: scavengers arrive, a decomposer bloom follows, nutrients enter the soil at that cell. It is the cheapest way to make a food web feel like a web rather than a graph. | Model | M | 3 |
| 86 | **Grazing that eats the plants that are there** <br>needs `hunt` | A herbivore should remove biomass from `life[c]` on the cell it stands on. Today `W.herbivore` is `nppMean × 0.2`, planet-wide, so grazing has no location and cannot overgraze anything. Local grazing produces the grass–grazer oscillation, bare patches and the reason herds move. | Model | M | 3 |
| 87 | **Overgrazing, and recovery** <br>needs `hunt` | A cell eaten below a threshold recovers slowly, so a herd that stays kills its own pasture. This is the mechanism behind migration, rotational grazing and desertification, and all three are things this product wants to show. | Model | M | 3 |
| 88 | **Ambush, pursuit, filter, graze, browse, scavenge** <br>needs `hunt` | Six feeding modes, each with a different cost, success curve and preferred terrain, selected from the genome axes that already exist. `node.diet` currently expresses only *what*; a mode expresses *how*, and how is what makes two predators of the same prey different animals. | Model | M | 3 |
| 89 | **Prey defence that is used, not just costed** | The genome has a defence axis and `ecologyTick` escalates it by 0.0005 a tick in an arms race. Nothing spends it: no failed attack, no deterrence, no armour that stops a bite. Defence should reduce a hunt success probability at the moment of the hunt. | Model | M | 3 |
| 90 | **Cover, so terrain decides the outcome** <br>needs `hunt` | Forest hides prey, open ground favours pursuit, water is a refuge for something and a trap for something else. `biome`, `life` and `h` are all per-cell, so terrain-conditioned hunt success is a lookup rather than a new system. | Model | M | 2 |
| 91 | **Predator territory** <br>needs `hunt` `memory` | A large predator holds an area, excludes rivals and returns to a core. That single behaviour spaces predators out, caps their density far below what food alone would allow, and creates the mosaic that everything smaller lives inside. | Model | M | 3 |
| 92 | **Cooperative hunting, and when it pays** <br>needs `hunt` `flock` | Group hunting beats solo hunting only above a prey size threshold, and this is one of the few places in biology where a clean quantitative rule exists. It is also the most watchable behaviour a simulation can show. | Model | M | 3 |
| 93 | **Trophic cascade, demonstrated** <br>needs `preyfield` | Remove the top predator, watch herbivores rise, watch vegetation fall, watch a river change. This is the canonical result the field is famous for. With local grazing and local predation it should emerge; a scenario that runs it is the proof. | Prove | M | 3 |
| 94 | **Who is eating whom, right now** <br>needs `hunt` | A readout of live trophic flows — kills per lineage pair per interval — rather than the static `foodWeb.links` list. The web the player cares about is the one that is currently carrying energy. | Show | M | 3 |
| 95 | **The two-hundred-link cap is being hit** | Measured: after 300 deep-time ticks `W.foodWeb.links.length` is exactly 200, the value of `links.slice(0, 200)`. So the food web is being truncated in every long run and nothing says which links were dropped or on what basis. Sort by flux before truncating, and report the count that was cut. | Prove | S | 3 |
| 96 | **A food web drawn as a web** | `foodWeb.links` has predator, prey and weight — enough for a layered graph with trophic level on the vertical axis and link thickness from flux. Nothing draws it. It is the picture that makes an ecosystem comprehensible. | Show | M | 3 |
| 97 | **Omnivory, and switching** | `a.diet` is a fixed top-three by weight, recomputed every tick from traits. A real consumer switches to whatever is abundant, which stabilises webs and is the reason generalists survive extinctions. Prey availability should reorder the diet. | Model | M | 2 |
| 98 | **Parasites and disease as trophic links** | A parasite is a consumer that does not kill its host, and by biomass it may be most of the links in a real web. It needs no new machinery beyond a negative term on the host and a dependence on host density. | Model | M | 2 |
| 99 | **Detritivores get a seat** | `W.trophic.decomp` is `nppMean × 0.3` and `W.detritus` is a real per-cell field that nothing consumes. Most of the energy in most ecosystems goes through decomposition, and here it goes through a constant. | Model | M | 2 |
| 100 | **Assert one kill** <br>needs `hunt` | A predator and a prey on adjacent cells, one tick, one hunt: prey population falls by one, predator energy rises by the prey mass times an efficiency, a carcass appears, a chronicle line is written. Five assertions and the whole category has a floor. | Prove | S | 3 |

## The word flock does not appear anywhere

Grep `vr/` for `flock`: zero hits. What exists is a cohesion-plus-separation nudge in `agentsTick` for exactly four of sixteen kinds — 6, 7, 14, 15 — which averages up to eight same-kind neighbours within `cellDot > 0.92` and steps one cell toward the centroid with probability 0.25. There is no alignment term, so the group has no heading; no leader; no size; no name; and no reason to exist rather than disperse.

| # | Item | What and why | Kind | E | I |
|---|---|---|---|---|---|
| 101 | **Flocking with an alignment term** <br>gives `flock` <br>needs `entsim` | The existing code averages neighbour positions and steps toward the centroid, with a separation nudge when beings share a cell. That is two of the three classical rules; without alignment a group has no shared heading, so it clumps rather than moves. `m.heading` exists and is written on every move and read nowhere. | Model | M | 3 |
| 102 | **A group as an object** <br>gives `group` <br>needs `flock` | Today a flock is an emergent side effect with no identity, so it cannot be named, counted, followed, split, merged or shown. A group record — members, centroid, heading, size, kind, leader — costs almost nothing and unlocks everything else in this category. | Model | M | 3 |
| 103 | **Flocking for more than four kinds** | The nudge applies to kinds 6, 7, 14 and 15. Whether a body plan schools, herds, swarms, flocks or is solitary should come from the genome — sociality is a trait — not from a hardcoded list of four sprite indices. | Model | M | 3 |
| 104 | **Sociality as a genome axis** <br>needs `group` | Solitary, pair, family, troop, herd, colony, superorganism. It is a categorical axis of exactly the kind `lifeGrammar.js` already carries, with real consequences for predation risk, disease, foraging efficiency and the path to a settlement. | Model | M | 3 |
| 105 | **Group size that is selected, not set** <br>needs `group` | Bigger groups see predators sooner and eat their patch faster. That trade produces an optimum that depends on predation pressure and productivity, both of which are fields the world already has, so group size becomes a readout of the environment. | Model | M | 3 |
| 106 | **A leader, and following** <br>needs `group` `memory` | One member holds the target and the rest weight its heading. This is how a herd crosses a barren stretch, which a centroid-averaging flock can never do, and it is the difference between milling and migrating. | Model | M | 3 |
| 107 | **Groups that split and merge** <br>needs `group` | Fission–fusion when a group exceeds a size or a patch runs out. It gives the visible population a rhythm of its own, independent of the slow biomass field, and it is a few conditions on an existing record. | Model | M | 2 |
| 108 | **Swarm as a rendered mass, not N sprites** <br>needs `group` | A locust swarm is a million individuals. Draw the group as a density blob with a few resolved individuals at the edge — which is also the answer to the performance question, since one group of ten thousand costs one record and one sprite pass. | Show | L | 3 |
| 109 | **A swarm on the globe** <br>needs `group` | From orbit a swarm is a dark moving smudge. Render groups above a size as a globe-scale mark that moves — the first thing on that sphere that moves for a biological reason rather than a meteorological one. | Show | M | 3 |
| 110 | **Migration, along a route** <br>needs `group` `memory` | A seasonal round trip between two remembered regions, triggered by the seasonal phase `nppField` already computes for the green wave. A migration is the most legible large-scale animal behaviour there is and the globe is the right canvas for it. | Model | L | 3 |
| 111 | **Migration routes that persist and can be broken** <br>needs `group` | A route is a remembered path. Put a mountain across it, drop the sea level, remove a stopover, and the population that used it crashes. That is a god verb with an ecological consequence, which is the intersection this product is built on. | Play | M | 3 |
| 112 | **Colonies and nests** <br>needs `group` | A group anchored to a place rather than moving through it: a rookery, a mound, a reef colony, a mat. It is the biological ancestor of a settlement and it should use the same record so the transition is continuous. | Model | M | 3 |
| 113 | **A superorganism that engineers its cell** <br>needs `group` | `ecologyTick` already lets reefs raise the seafloor and life build soil. A colony that changes moisture, soil, albedo or height at its own cell is the same mechanism aimed at something visible at the scale a player is looking. | Model | M | 2 |
| 114 | **Trails, and the field that carries them** <br>needs `memory` | A shared, decaying per-cell field written by movement and read as attraction. It is the cheapest known route to emergent structure — paths, foraging columns, worn tracks — and `noteWear` already writes exactly this shape of field for a different purpose. | Model | M | 3 |
| 115 | **Worn paths that the renderer shows** <br>needs `memory` | `paintWear` in the local view already draws a wear texture. Feeding movement into it turns the grid of squares into a place with desire lines, which is a small change with a disproportionate effect on how inhabited a tile looks. | Show | S | 3 |
| 116 | **Predator response to a group** <br>needs `group` `hunt` | A group is harder to attack and more rewarding to find. Confusion effect, dilution, mobbing — the reasons grouping evolved. Without them, grouping is decoration; with them, it is a strategy the simulation selected. | Model | M | 3 |
| 117 | **Groups in the census line** <br>needs `group` | `viewCensus` counts beings by kind. It should count groups, name the largest, and say what it is doing — grazing, moving, spawning — so the readout describes a scene rather than a tally. | Show | S | 2 |
| 118 | **Assert the three rules** <br>needs `flock` | Twenty beings, one kind, no predators: assert the mean nearest-neighbour distance falls and stabilises, the heading variance falls, and no two beings occupy the same position. Three numbers that catch every flocking bug there is. | Prove | S | 3 |
| 119 | **A group budget** <br>needs `group` | Neighbour search is the cost. `eachNearby` already walks a cell and its four neighbours through a linked list, which is the right structure. State the per-tick cost at 1400 beings and at the 768-cell resolution before adding an alignment pass. | Prove | S | 3 |
| 120 | **The word flock should appear** | Grep `vr/` for `flock`, `herd`, `swarm`, `colony`, `territory` or `pheromone`: zero hits between them outside a technology name and a grammar string. The vocabulary of animal behaviour is entirely absent from a product about life, and naming the concepts is the first step to having them. | Prove | S | 2 |

## A range with no front

Colonisation in `bioTick` is `max` over the four neighbours: if a neighbour is above 0.2 the cell grows at 0.28 instead of 0.1. That is a diffusion, and it produces the one thing a spreading biosphere should never look like — an isotropic stain. No corridor, no barrier, no dispersal distance, no founder effect at the edge, no invasion that arrives from somewhere and outcompetes what was there.

| # | Item | What and why | Kind | E | I |
|---|---|---|---|---|---|
| 121 | **A colonisation front with a shape** <br>gives `front` | `bioTick` grows a cell faster when a neighbour is above 0.2 — an isotropic diffusion that produces a stain. A front has a direction, a speed that depends on the terrain ahead, a leading edge that is sparse and a wake that fills in. Front speed is the most watchable thing a spreading biosphere does. | Model | M | 3 |
| 122 | **Dispersal with a distance** <br>gives `disperse` <br>needs `front` | Growth today is strictly nearest-neighbour, so nothing can cross a barrier and nothing can arrive from far away. A dispersal kernel with a mean and a fat tail — wind, water, rafting, hitching a ride — is what actually decides which islands have what. | Model | M | 3 |
| 123 | **Barriers, from the terrain that is already there** <br>needs `front` | Mountains, deserts, straits, ice and salinity fronts are all computed fields. The life backlog names `barrier` as unblocking six items and it is still a token. A front that stops at a real barrier is the picture that explains biogeography. | Model | M | 3 |
| 124 | **Corridors, and what opens them** <br>needs `front` | A land bridge at low sea level, a river valley, a green corridor across a wet Sahara. Corridors open and close on the climate clock, and each opening is an exchange event with winners and losers — the Great American Interchange as a mechanism rather than a name. | Model | M | 3 |
| 125 | **Founder effects at the edge** <br>needs `disperse` | The population that crosses is small and unrepresentative. `Ne` and drift already exist in the genome layer, so a founder event at the leading edge produces real divergence and is the honest mechanism behind allopatric speciation, which `evolveTick` currently accumulates as an isolation scalar. | Model | M | 3 |
| 126 | **Invasion, with an outcome** <br>gives `invade` <br>needs `front` | `transplantClade` moves a clade and can mark it invasive. What is missing is the contest: the arrival competes with the incumbent for the same resource on the same cells, and either establishes, fails or explodes. That is a scenario the player should be able to run in a minute. | Model | M | 3 |
| 127 | **Range as a thing that moves** | The `range` overlay paints the occupied cells of the dominant clade — a static mask. Range shift under warming, range collapse under drought, poleward creep: the derivative is the interesting quantity and it is one subtraction away. | Show | S | 3 |
| 128 | **Islands, and why they are strange** <br>needs `disperse` | Small area, isolated, few lineages, weird ones. Island biogeography is a quantitative theory with a species–area relationship this simulation could reproduce and be checked against, and the world generator already makes islands. | Model | M | 2 |
| 129 | **Refugia during a bad epoch** | When the world contracts, populations survive in a few cells and expand out again afterwards. That produces the genetic signature of every real glacial cycle, and this simulation runs glacial cycles already. | Model | M | 2 |
| 130 | **Vicariance, when a continent splits** | `tectonicsTick` moves plates. A range cut in two by a rifting continent is speciation with a stated cause and a date, and the phylogeny could carry that cause on the node. | Model | M | 3 |
| 131 | **A front rendered as a front** <br>needs `front` | On the globe: an animated leading edge, brighter than the interior, moving at a visible speed. This is the single most direct answer to *life growing before your eyes* and it needs a gradient of the occupancy field, not a new simulation. | Show | M | 3 |
| 132 | **The green wave, made visible** | `nppField` computes a phenology term from season and latitude and nothing shows it. Spring sweeping poleward is the most beautiful thing a living planet does at globe scale and it is already in the model. | Show | S | 3 |
| 133 | **Succession after a disturbance** <br>gives `succession` | Fire, ash, lava, ice retreat and a drained lake all leave bare ground, and `W.ash` and `W.lava` are real fields. Pioneers, then mid-succession, then a climax community — a sequence with a clock, which is growth the player can watch on one tile. | Model | M | 3 |
| 134 | **Fire as a process life participates in** | Fuel from biomass, ignition from lightning or volcanism, spread with the wind, and a burnt scar that then succeeds. `stormField`, `wind` and `life` are all there. Fire is the fastest visible ecological process on a planet with plants and oxygen. | Model | L | 3 |
| 135 | **Treeline, and where it moves to** | `ecologyTick` already substitutes alpine biomes above a height and below a temperature. Making it a moving line rather than a threshold gives a warming world an unmistakable visible signal at a scale the local grid can show. | Model | S | 2 |
| 136 | **Sea-level change as habitat change** | `seaLevel` moves, `intertidal` exists and `city.js` already computes a `drowned` flag. Coastal habitat gained and lost, on the globe, with the populations that depended on it — the clearest way to make a slow forcing feel consequential. | Model | M | 2 |
| 137 | **Assert a front speed** <br>needs `front` | Seed one cell on a uniform world, run 100 ticks, measure occupied radius against time. It should be linear, its slope should scale with productivity, and it should be zero across a barrier. Three assertions that pin the entire spread model. | Prove | M | 3 |
| 138 | **A species–area check** <br>needs `disperse` | Count lineages against island area across a generated archipelago and fit the exponent. Real values sit near 0.25. This is a rare case where an emergent ecological result has a published number to be measured against. | Prove | M | 2 |
| 139 | **Seed a front and watch it, as a verb** <br>needs `front` | `seedLife` paints a disc of biomass at 0.7 and above instantly. A seed that starts as one cell and has to spread is a completely different experience of the same button, and it is how the player learns what the terrain means. | Play | S | 3 |
| 140 | **A time-lapse of a colonisation** <br>needs `front` | Record occupancy every N ticks and replay it fast. The product already captures moments in `maybeCaptureMoment`; a front replay is the same idea applied to the one process this whole backlog is about. | Show | M | 2 |

## An ecosystem is four scalars

`W.trophic` is `{prod, herb, carn, decomp}` computed as `nppMean`, `× 0.1`, `× 0.01`, `× 0.3` — a global mean multiplied by three constants, from which `W.herbivore` and `W.carnivore` follow. So the herbivore load on a rainforest cell equals the herbivore load on an ice cell. `guildDens` in `redox.js` is a genuine per-cell guild field for metabolisms; nothing equivalent exists above the microbial layer.

| # | Item | What and why | Kind | E | I |
|---|---|---|---|---|---|
| 141 | **A trophic field, not three multiplications** <br>gives `guildfield` | `W.trophic` is `{nppMean, ×0.1, ×0.01, ×0.3}` and `W.herbivore`/`W.carnivore` follow from it, so a rainforest cell and an ice cell carry identical herbivore load. Producers, herbivores, carnivores and decomposers need to be per-cell — four `Float32Array`s — and `redox.js` already proves the pattern with `guildDens`. | Model | L | 3 |
| 142 | **Transfer efficiency that varies** <br>needs `guildfield` | Ten percent is a textbook average, not a law: it is higher in aquatic webs, lower in terrestrial ones, and depends on what is being eaten. Making it depend on the biome and the prey type is what makes ocean and land pyramids differ. | Model | M | 2 |
| 143 | **A pyramid that can be the wrong shape** <br>needs `guildfield` | Inverted biomass pyramids are real — plankton support more fish biomass than they hold at an instant, because turnover is fast. A model that computes standing stock from a fixed ratio can never show that, and it is one of the few genuinely counterintuitive facts in ecology. | Model | M | 2 |
| 144 | **Guilds above the microbial layer** <br>needs `guildfield` | `GUILDS` in `redox.js` is a real metabolic guild table and it stops at chemistry. Grazers, browsers, filterers, ambush predators, pursuit predators, scavengers, detritivores, pollinators — a table of the same shape, one level up, and the interface already has a palette for it. | Model | M | 3 |
| 145 | **Niche partitioning, so coexistence has a reason** <br>needs `guildfield` | `updateFoodWeb` gives competitors a flat `compete += 0.04` when their trophic levels are within 0.08. Real coexistence comes from differing along an axis — size, time of day, depth, prey size, tolerance. Partitioning is what lets a cell hold twenty lineages instead of one. | Model | M | 3 |
| 146 | **A keystone that can be removed** <br>gives `keystone` <br>needs `guildfield` | Identify the lineage whose removal changes the most, by simulating its removal. Then let the player remove it. This is the most instructive single experiment in ecology and the product already has `cullClade`. | Play | M | 3 |
| 147 | **Succession as a community sequence** <br>needs `succession` `guildfield` | Not a scalar rising toward a cap: a sequence of communities where each changes the conditions the next needs — nitrogen fixers, then grasses, then shrubs, then canopy. `W.soil` already accumulates with life, which is the first link of that chain. | Model | M | 3 |
| 148 | **Ecosystem engineers, on purpose** | `ecologyTick` has two hardcoded cases: reefs raise the seafloor and life builds soil. Beavers, burrowers, termites, mats, forests that make their own rain — the mechanism is one line each and the effect is a cell that a lineage has visibly changed. | Model | M | 3 |
| 149 | **Mutualism, which the model has none of** | Every interaction in `updateFoodWeb` is negative — predation or competition. Pollination, mycorrhizae, gut symbionts, cleaner fish and lichens are all positive, and a web with no positive links cannot produce the two most important transitions in the life pass, endosymbiosis and land plants. | Model | M | 3 |
| 150 | **Symbiosis that becomes an organelle** | The endosymbiotic step is already a transition flag. Reaching it through a mutualism that got tighter, rather than through a threshold, is the difference between a checkbox and a story — and the genome layer can already move an organ between lineages via HGT. | Model | L | 3 |
| 151 | **Decomposition that closes the loop** | `W.detritus` accumulates and decays at 0.97 a tick into nothing. Decomposers should consume it and return nutrients to `nutrientN` and `nutrientP` on that cell, which is the actual nutrient cycle and the reason a forest is not a desert. | Model | M | 3 |
| 152 | **Nutrient limitation a being can experience** | Redfield limitation already shapes ocean NPP. On land, nitrogen and phosphorus limits should decide which lineage wins where — and nitrogen fixers should be worth being, which is a guild with a specific, visible geographic advantage. | Model | M | 2 |
| 153 | **Diversity that comes from somewhere** | `shannonDiversity` measures the tree. Local diversity should follow from productivity, disturbance, area and heterogeneity — the intermediate disturbance hypothesis is a testable prediction this simulation could exhibit rather than assume. | Model | M | 2 |
| 154 | **The ecotone means something** | `W.ecotoneFrac` is 0.676 on modern Earth — measured — meaning two thirds of land cells have a dominant biome weight below 0.7. Either that is a real mosaic or the membership function is too soft. Ecotones should be where diversity peaks and edges matter; today they are only a colour. | Model | M | 2 |
| 155 | **A biome is a community, not a colour** | `BIOMES` is fifteen ids and `classifyBiome` picks one from temperature and precipitation. A biome should be a set of guilds with abundances, so *tundra* means a specific community that can be depleted, invaded or replaced rather than a palette entry. | Model | L | 3 |
| 156 | **Biome bistability, done properly** | The forest–savanna flip in `ecologyTick` is a hardcoded window on moisture that also overwrites `W.biome`. Bistability should come from the feedback — trees make rain, rain makes trees — with real hysteresis, which the tipping-element machinery in `gaia.js` already knows how to express. | Model | M | 2 |
| 157 | **An ecosystem panel worth opening** <br>needs `guildfield` | For the cell under the cursor: NPP, the four trophic stocks, the guilds present with abundances, the dominant lineages, what limits growth here, and what would happen if it warmed. Every one of those numbers either exists or is one addition away. | Show | M | 3 |
| 158 | **Assert the pyramid** <br>needs `guildfield` | Producers exceed herbivores exceed carnivores on every cell, energy in equals energy out plus respiration plus burial across the biosphere, and no trophic stock is negative. Three invariants that make a spatial trophic model trustworthy. | Prove | M | 3 |
| 159 | **Assert a cascade** <br>needs `keystone` | Remove the top carnivore, run 200 ticks, assert herbivores rise then vegetation falls, and assert the effect decays with distance from the removal. If it does not, the trophic field is not coupled to space. | Prove | M | 3 |
| 160 | **Ten guilds, one cell, a hundred ticks** <br>needs `guildfield` | The smallest interesting test: seed ten guilds on one productive cell and check how many coexist. If the answer is always one, competition is too strong; if always ten, it is too weak. The number itself is a design decision that should be written down. | Prove | M | 3 |

## The grid of squares

The local view is the one place a player is close enough to see behaviour, and it draws life from two arrays: `life[c]` and `lifeClass[c]`. Ten `stamp*` functions — mats, stromatolites, daisies, ocean life, ice life, flora, ambient fauna, buildings — key off a scalar and a class index. `stampAmbientFauna` calls `stampBug`. Nothing on that grid is doing anything; the tile is a still life that changes slowly.

| # | Item | What and why | Kind | E | I |
|---|---|---|---|---|---|
| 161 | **The tile is a still life** <br>gives `tilelife` | `drawLocalView` unwraps a 17 × 17 patch around the focus cell and paints each square from `life[c]` and `lifeClass[c]` through ten `stamp*` functions. Nothing in that picture is doing anything: `stampFlora` places fixed fronds, `stampAmbientFauna` calls `stampBug`, and the only motion is `presentAgents` interpolating a being between two cells. The grid of squares is the one view close enough to show behaviour and it shows state. | Show | M | 3 |
| 162 | **Draw the act, not the actor** <br>gives `tileact` <br>needs `tilelife` `drivevec` | A being that is foraging, drinking, hunting, fleeing, nesting, grazing or resting should look different in the square — a posture, a direction, a small mark. One glyph per drive is a day of drawing and it is what turns 289 squares into a scene. | Show | M | 3 |
| 163 | **Face the direction of travel** | `m.heading` is computed on every move as an eastward component and never read by the renderer, so everything faces the same way forever. Orientation is the cheapest possible cue that a thing is going somewhere. | Show | S | 3 |
| 164 | **A gait** | `entityGait` exists in the local view. Tie its phase to actual displacement so a walking animal walks, a resting one breathes and a fleeing one is fast, rather than every sprite sharing one animation clock. | Show | M | 2 |
| 165 | **Eating, visibly** <br>needs `tileact` | A grazer at a plant, a predator at a carcass, a filter feeder in the current. If eating has a location and a duration in the model, it can have a picture, and eating is the interaction a player understands without being told. | Show | M | 3 |
| 166 | **Plants that are individuals where it matters** | `stampFlora` draws a texture keyed to `life[c]`, so a forest is a fill pattern. At the tightest zoom a tree should be a thing with an age and a size that grows over the run — the same argument as for animals, applied to the 60% of the biosphere that does not move. | Show | M | 2 |
| 167 | **Vegetation structure, in layers** | Canopy, understorey, ground cover, litter. Four strata drawn in order give a tile depth and give animals places to be — and the stratum an animal occupies is already implied by the genome habitat axis. | Show | M | 2 |
| 168 | **Seasonal appearance on the tile** | `nppField` computes a phenology term. Leaf-out, senescence, snow on the ground, a dry-season canopy — a tile that changes with the season is the fastest visible proof that time is running. | Show | M | 3 |
| 169 | **The tile after a disturbance** <br>needs `succession` | Burnt ground, ash fall, a lava flow cooling, ice retreating and bare soil colonising. `W.ash` and `W.lava` are real fields the local view barely reads, and recovery on one tile over a few thousand years is growth at exactly the scale a player can follow. | Show | M | 3 |
| 170 | **A nest, a den, a mound** <br>needs `group` | A structure a being built, on a specific square, that persists and can be destroyed. It is the smallest form of the same idea that ends in a city, and it gives the tile a history. | Show | M | 2 |
| 171 | **Trails on the ground** <br>needs `memory` | `paintWear` already draws a wear texture from `noteWear`, which movement already calls. Persisting and rendering it as paths gives the tile the single strongest signal that something lives here and goes somewhere. | Show | S | 3 |
| 172 | **A tile census that describes a scene** | `viewCensus` ranks cover shares and counts beings by kind through `KIND_CENSUS`. It should also say what is happening: how many are feeding, whether a hunt is in progress, whether the population here is rising, which lineage dominates and what limits it. | Show | M | 3 |
| 173 | **Zoom levels with different jobs** | `mapFidelity(cellPx)` already grades detail by cell size and `localFrameLabel` names the frame. Make the job explicit: at the widest, the ecosystem mosaic; in the middle, groups and movement; at the tightest, individuals with visible behaviour. Each level needs its own answer to what is being shown. | Show | M | 2 |
| 174 | **The net view as an ecology map** | `drawNetView` draws all six cube faces as a flat net of squares. That is the whole planet as a grid, and it currently paints height and a few fields. It is the natural home for a biosphere-wide picture of ranges, fronts and populations that the sphere cannot show without rotation. | Show | M | 3 |
| 175 | **Cell picking that returns life, not terrain** | `cellAtLocalPixel` and `beingAtLocalPixel` both exist. Clicking a square should offer both readings — this ground, and this animal — because the player asking about a tile is usually asking about what is on it. | Show | S | 2 |
| 176 | **Do not draw more than is there** | A tile with `life[c] = 0.1` should look sparse. The stamps currently scale a texture rather than a count, so low biomass reads as a thinner fill rather than as fewer things — which is why an early Earth and a modern one look like the same picture at different brightness. | Show | M | 3 |
| 177 | **A hunt, framed** <br>needs `hunt` `tileact` | When a hunt starts within the patch, mark it. `huntGlance` already exists as a camera hint in the local view — it should have something to glance at. | Show | M | 3 |
| 178 | **Sound for the tile** | `audio.js` layers one soundscape and it is Earth's everywhere. A tile with a dense biosphere should sound different from a bare one, and the ambient layer is a mix of three or four loops weighted by what is present. | Show | M | 1 |
| 179 | **Assert something moved** <br>needs `tileact` | Render a patch twice, ten ticks apart, on a living world, and assert the pixels differ by more than noise. The world-space pass established that nothing in the suite renders anything; this is the smallest useful pixel assertion and it targets exactly the failure this backlog exists for. | Prove | M | 3 |
| 180 | **Count the frames a scene costs** | Ten stamp functions per square across 289 squares, plus weather overlays, rivers, plumes and beings. Measure it now, because every item in this category adds to that number and the tick budget is 12 ms for everything. | Prove | S | 3 |

## Seventy-two overlays and not one is behaviour

The overlay table runs from temperature through vorticity, upwelling, ENSO, sea state, crust age and the technosphere. It has `npp`, `guild`, `diversity` and `range` — four biosphere fields, all of them state. None of the 72 shows a rate, a flow, a movement or an interaction: no migration, no predation pressure, no colonisation front, no settlement growth. The globe can say where life is and never what it is doing.

| # | Item | What and why | Kind | E | I |
|---|---|---|---|---|---|
| 181 | **A behaviour overlay** <br>gives `behovl` <br>needs `preyfield` | Seventy-two overlays and every biological one is a state: `npp`, `guild`, `diversity`, `range`. Add the rates — predation pressure, grazing pressure, birth rate, mortality, net population change. A globe that shows where life is *changing* is a different object from one that shows where it is. | Show | M | 3 |
| 182 | **Population change, signed** <br>needs `popbook` | Red falling, blue rising, black steady. One overlay, computed from the population book, and it answers the question a player of a life simulator asks first and cannot currently ask at all. | Show | S | 3 |
| 183 | **Groups and swarms on the sphere** <br>gives `globeswarm` <br>needs `group` | Discrete marks that move, sized by group size, coloured by lineage. Nothing on that globe currently moves for a biological reason. Storms move; life does not. | Show | M | 3 |
| 184 | **Migration arcs** <br>needs `globeswarm` | A route drawn as a great-circle arc with a moving head, appearing seasonally. The storm layer already draws tracks as a gold trail, so the rendering pattern exists and is proven. | Show | M | 3 |
| 185 | **The colonisation front, animated** <br>needs `front` | A bright moving edge on the occupancy field. This is the picture that the phrase *life growing before your eyes* actually describes and it is a gradient plus a shader term. | Show | M | 3 |
| 186 | **Night lights that grew** | `cityLights` returns a single scalar from city count, mean build and a log of population, fed to `technoLights`. The interesting version is per-cell and cumulative, so the player can watch a lit coastline appear over centuries. | Show | M | 3 |
| 187 | **Clade colour on the surface** | `cladeRGB(node.id)` already tints beings by lineage. Painting the surface by dominant clade makes biogeography visible at globe scale — the map that shows which lineage owns which continent. | Show | S | 3 |
| 188 | **Biomass, not occupancy** | `life[c]` is drawn as a green tint and reads as coverage. A biomass overlay with a real scale — with the ocean actually dark and the rainforest actually bright — is a different and more honest picture, and `biosphereWatts` already computes a global total to calibrate against. | Show | S | 2 |
| 189 | **Grazing pressure** <br>needs `guildfield` | Where herbivores are eating hardest. It is the field that explains why a productive place can look bare, and it is one of the four trophic arrays. | Show | S | 2 |
| 190 | **The tree of life on the globe** | Select a node in the Lab tree, see its range light up; select a region, see which nodes live there. The `range` overlay is half of this and the linkage is the part that makes both halves useful. | Show | M | 3 |
| 191 | **An extinction, rendered as an event** | `extinctionTick` and the tipping machinery produce events that appear only as chronicle lines. A pulse on the globe where the deaths are, fading over a few thousand years, gives an extinction a place as well as a date. | Show | M | 3 |
| 192 | **Fires, visible from orbit** <br>needs `front` | Bright points on the night side, smoke on the day side. Fire is the most visible biological process on Earth from space and this simulation has the fuel, the ignition and the wind. | Show | M | 2 |
| 193 | **Reef, mangrove, kelp — the structures** | `W.reef` is a real field with bleaching. Coastal biogenic structures are narrow, bright, geographically specific and they read at globe scale, which makes them unusually good value per pixel. | Show | S | 2 |
| 194 | **A biosphere seen from far away** | `disequilibrium` computes an O₂–CH₄ biosignature and shows it as a number. Presenting the planet as a distant observer would see it — a spectrum, a red edge, a seasonal signal — connects the whole biosphere model to the reason anyone cares about it. | Show | M | 3 |
| 195 | **The overlay legend should say the units** | Seventy-two overlays with tooltips that describe meaning and rarely the scale. A behavioural overlay without units is a mood; with units it is a measurement, and this product is explicitly in the business of the latter. | Show | S | 2 |
| 196 | **One overlay for the whole story** | A default biosphere view that combines biomass, dominant clade and change, tuned so a single glance reads as *this planet is alive and this is what it is doing*. Seventy-two specialist views and no summary is a diagnostic tool, not a game. | Show | M | 3 |
| 197 | **Do not paint the ocean green** | A recurring artefact class from the realism pass. Any life overlay has to handle the ocean on its own scale — a productive ocean is not a productive forest — and the axis-aligned-edge detector should be run on every new overlay added here. | Prove | S | 2 |
| 198 | **Overlays that survive a red dwarf** | `lifeColour.js` holds 76 hardcoded colour triples. A biosphere overlay under a 2,560 K star should not be Earth green, and the illuminant work in the open-world pass is the mechanism. | Show | M | 2 |
| 199 | **Assert the overlay is not the same picture** <br>needs `behovl` | For each new behavioural overlay, assert its per-cell correlation with `life[c]` is below a stated bound on a test world. An overlay that is a recolour of biomass is a wasted slot, and there are already 72 slots. | Prove | M | 3 |
| 200 | **Frames per overlay** | Every overlay is a per-cell pass at up to 3.5 M cells. Measure the cost of the behavioural ones specifically, because they update every tick rather than every generate, and `gpgpuClimateTick` shows where the fast path is if they need it. | Prove | S | 3 |

## Three hundred ticks, one settler, zero towns

Measured on modern Earth with agents running for 300 ticks: 326 beings alive, **1** of kind 5, `meanBuild` 0.00000, `W.cities` empty. `kindForCell` gives a settler a `0.03 + build × 0.2` chance on `earthLike`, and `agentsTick` scales the build rate by 0.12 there, so the loop that is supposed to raise towns is throttled to roughly nothing. On the same run 196 of 326 beings — 60% — were reef sprites.

| # | Item | What and why | Kind | E | I |
|---|---|---|---|---|---|
| 201 | **Unthrottle the settlement loop** <br>gives `citysim` | Measured on modern Earth, 300 ticks: 326 beings, **one** settler, `meanBuild` 0.00000, zero cities. `kindForCell` gives a settler a `0.03 + build × 0.2` chance on `earthLike` and `agentsTick` multiplies the build rate by 0.12 there. Two independent throttles on the same loop, neither documented, and together they make urban growth unobservable. | Model | S | 3 |
| 202 | **A settlement is an entity, not a scan** <br>needs `citysim` | `settleCities` flood-fills the `build` field every 40 years and rebuilds the whole list, capped at 48 settlements and 40 cells each. So a town has no continuous identity: it cannot have a founding date, a history, a name it keeps, or a population that persists between scans. | Model | M | 3 |
| 203 | **Population that is counted, not formulated** <br>needs `citysim` `popbook` | `pop` is `(80 + build × 4000) × (0.35 + npp × 0.5 + soil × 0.2 + moist × 0.15) × (0.6 + tech × 0.8) × n`. Five factors and six constants producing a number nobody can check. A settlement population should be people who were born, who eat, and who can leave. | Model | M | 3 |
| 204 | **A hinterland that feeds it** <br>gives `hinterland` <br>needs `citysim` | A settlement draws food from the cells around it, and the size it can reach depends on their NPP, the soil and how far a cart goes. `npp` at the centre cell is read once as a multiplier. A real catchment is what makes river valleys and deltas produce the first cities. | Model | M | 3 |
| 205 | **Food surplus, and what it buys** <br>needs `hinterland` | Surplus is the precondition for specialisation, which is the precondition for everything the technosphere models. Making it an explicit quantity ties the civilisation ladder to the ecology instead of to `unlockedClass`. | Model | M | 3 |
| 206 | **Roads between settlements** <br>gives `roads` <br>needs `citysim` | A path over terrain between two settlements, cheapest by slope and water crossings, drawn on the globe and the grid, wearing in with use. `noteWear` is already the field for it. Roads are the most legible artefact of a civilisation at any zoom. | Model | M | 3 |
| 207 | **Trade, and what it moves** <br>gives `trade` <br>needs `roads` | Grain from the hinterland, ore from the mountains, salt from the coast — `W.ore` exists per cell. Trade turns 48 independent dots into a network with hubs, and hubs are why some settlements become cities and others do not. | Model | M | 3 |
| 208 | **Migration between settlements** <br>needs `trade` `popbook` | People leave a failing place for a growing one. That single flow produces urbanisation, abandonment, refugees after a disaster, and it is the same immigration accounting the population book already needs. | Model | M | 3 |
| 209 | **Sites chosen for a reason** <br>needs `citysim` | `city.js` already computes `harbour` from tide range and `coastal` from height, which is exactly the right instinct. Extend it: a confluence, a defensible height, a pass, a ford, a spring, a good anchorage. Then the map of settlements is a readout of the terrain. | Model | M | 3 |
| 210 | **A settlement that can fail** <br>needs `citysim` | `drowned` is computed and used only to sort the list. Salinisation, drought, exhausted soil, a shifting river, a drowned harbour, a plague — an abandoned town with a date and a cause is worth more than four thriving ones. | Model | M | 3 |
| 211 | **Growth stages with visible thresholds** | Camp, village, town, city from `build` at 0.3, 0.55 and 0.85, logged to the chronicle. The stages are right and the trigger is one scalar. Tie them to population, surplus and trade so that crossing a threshold is an event with a cause. | Model | M | 2 |
| 212 | **Buildings that accumulate** | `stampBuildings` draws structures from `build[c]` in the local view. A settlement should visibly add structures over time — the same growth argument as for a body or a forest, applied to the thing players most want to watch grow. | Show | M | 3 |
| 213 | **A settlement panel** <br>needs `citysim` | Name, founded, population and its trend, food balance, what it trades and with whom, what limits it, what would kill it. `W.cities` already carries nine fields; this is the presentation the model has been waiting for. | Show | M | 3 |
| 214 | **Land use around a town** <br>needs `hinterland` | Cropland, pasture, managed forest, quarry — each a change to the cell that the biosphere then has to live with. `technoTick` has a land-use notion at planet scale; at settlement scale it is the thing that makes a civilisation visible on the surface. | Model | M | 3 |
| 215 | **The cost of a settlement to the biosphere** <br>needs `hinterland` | Cleared forest, hunted-out large fauna, a changed fire regime, nutrient runoff. `medeaScore` exists as a self-harm metric. A civilisation that grows without a measured cost is a decoration on the ecology rather than part of it. | Model | M | 3 |
| 216 | **Forty-eight is a rendering limit, not a world limit** | `W.cities = cities.slice(0, 48)` and the flood fill stops at 40 cells. State which limits are for drawing and which are for modelling, and let the model exceed the drawing. | Model | S | 2 |
| 217 | **Names that persist** | `name` falls back to `${stage}-${best}` — a stage plus a cell index, so a town is renamed whenever it grows or the scan shifts. A settlement should be named once, at founding, and keep it. | Show | S | 3 |
| 218 | **Watch one settlement for a thousand years** <br>needs `citysim` | A time-lapse of one place: huts, a wall, fields spreading, a harbour, a road, a fire, a rebuild. The product has a moment-capture system and a local view; this is the two of them pointed at the thing the player asked to see grow. | Play | M | 3 |
| 219 | **Assert a town grows** <br>needs `citysim` | On a productive world, over N ticks, assert at least one settlement crosses each of the three stage thresholds, and assert none does on a barren one. This is the assertion whose absence let the measured zero-city result stand. | Prove | S | 3 |
| 220 | **Assert a town starves** <br>needs `hinterland` | Remove the hinterland productivity and assert the population falls and the settlement is abandoned with a stated cause. A growth model that cannot shrink is a counter. | Prove | M | 3 |

## A ladder of eight envelopes

`LIFE_CLASSES` is eight rows from prokaryote to mammal, each an O₂/temperature/moisture envelope, and `W.unlockedClass` is one integer for the planet. Everything downstream reads it: which sprite a cell gets, whether a settler may exist, how fast a settler builds. A civilisation is therefore a number between 0 and 7 that the whole world shares, and `technoTick` hangs a technosphere off it.

| # | Item | What and why | Kind | E | I |
|---|---|---|---|---|---|
| 221 | **Retire the single planetary ladder** <br>gives `bandtociv` | `W.unlockedClass` is one integer 0–7 for the whole planet, read by `kindForCell` to decide whether settlers may exist and by `agentsTick` to decide how fast they build. So every region of every world is at the same stage at the same moment, and a civilisation is a global variable. | Model | L | 3 |
| 222 | **Culture as a regional thing** <br>needs `bandtociv` | Several groups on one planet at different stages, in contact, exchanging or displacing. That is the shape of every real history and it needs the stage to live on a group rather than on the world. | Model | L | 3 |
| 223 | **A technology that has prerequisites** <br>needs `bandtociv` | `technoTable.js` exists as authored data. A tech reached because a population had a surplus, a material and a need — rather than because `unlockedClass` incremented — is the difference between a tech tree and a timer. | Model | M | 3 |
| 224 | **Domestication** <br>needs `bandtociv` | A lineage from the phylogeny becomes a crop or an animal, with its traits changed by selection the settlement applies. It is the most direct possible link between the evolution model and the civilisation model, and both already exist. | Model | M | 3 |
| 225 | **What the planet gave them to work with** <br>needs `bandtociv` | Whether there is a domesticable large animal, a cereal, smeltable ore, navigable water or wood at all depends on the world. `W.ore` and the genome both exist. This is the mechanism that makes two habitable planets produce different histories. | Model | M | 3 |
| 226 | **Energy from somewhere specific** | `technoTick` computes a technosphere and `W.energyIncome` is `0.5 + health × 1.5 + meanLife`. Firewood, then coal from buried carbon, then hydro from real discharge, then fission from real ore — the carbon model already tracks burial, so the fossil fuel is genuinely there or genuinely not. | Model | M | 3 |
| 227 | **The technosphere as a consumer in the food web** | A civilisation eats. Putting its consumption into the trophic accounting is the honest way to model it, and it makes the biosphere cost of a growing population fall out of the same arithmetic as everything else. | Model | M | 3 |
| 228 | **Population that can crash** <br>needs `popbook` | `civPop` is a sum over city populations from a formula. A civilisation should be able to lose a third of its people to a plague, a famine or a war and take two centuries to recover, because that is what the historical record is mostly made of. | Model | M | 3 |
| 229 | **Conflict, at the resolution the world has** | Two groups, one valley. Contested territory, displacement, a wall, an abandoned frontier. Handled at the group level it is cheap; handled at the individual level it is a different game. State which and stay there. | Model | M | 2 |
| 230 | **Knowledge that can be lost** <br>needs `culture` | A technology held by one settlement that dies with it. Loss is the mechanism that makes a tech tree a history rather than a ratchet, and it is the same Muller ratchet argument the genome layer already makes. | Model | M | 2 |
| 231 | **A civilisation that notices its planet** <br>needs `gaiapolicy` | The population reads the climate it is changing and responds — or does not. That is the loop this whole product is about and it currently has no representation at all beyond a `medeaScore`. | Model | L | 3 |
| 232 | **The Anthropocene as a measured transition** | `transitions` already tracks the major evolutionary steps as flags with dates. A technosphere transition — when biological and technological energy use cross — is the same kind of dated, defensible marker, and `biosphereWatts` gives one side of it. | Model | M | 3 |
| 233 | **A non-humanoid civilisation** | `unlockedClass` runs to `mammal` and the settler sprite is a humanoid. Nothing in the model requires either. A civilisation built by something eusocial, aquatic or sessile is a genuinely different picture and the genome grammar can already describe the builder. | Model | L | 3 |
| 234 | **What a civilisation looks like from orbit** | Lights, geometry, straight lines, changed albedo, a changed atmosphere. `technoLights` produces one scalar. The visible fingerprint of a technosphere is a set of specific artefacts and each is worth drawing on its own terms. | Show | M | 3 |
| 235 | **A history you can read** <br>needs `bandtociv` | The chronicle already logs foundings. A civilisation timeline — populations, technologies, disasters, migrations, wars — is the readout that makes the last thousand ticks comprehensible. | Show | M | 3 |
| 236 | **The player as a god they can perceive** | `god/` has 15 modules of verbs. A population that notices intervention — that builds where you raised land, that abandons where you struck — closes a loop the god layer currently only writes into. | Play | M | 3 |
| 237 | **Do not let the ladder outrun the biology** | Measured: `unlockedClass` reaches 7 on modern Earth within 300 ticks while `meanLife` falls 38% and no settlement exists. The ladder is decoupled from the biosphere it is supposed to be growing out of, and an assertion should tie them. | Prove | S | 3 |
| 238 | **Assert two worlds diverge** <br>needs `bandtociv` | Same seed, one world with a domesticable large animal and one without: assert their histories differ in a stated way. If they do not, the civilisation model is not reading the planet. | Prove | M | 3 |
| 239 | **A stated ceiling** | What is the highest stage this product models, and what happens there. Without an answer, the ladder grows a rung every time somebody has an idea, and `LIFE_CLASSES` plus `technoTable` are already two ladders that do not agree. | Model | S | 2 |
| 240 | **The cost of a civilisation in milliseconds** | Roads, trade, hinterlands and migration are all graph work on up to 48 settlements, which is small. Say so with a number, because the reason none of this exists is a suspicion that it is expensive. | Prove | S | 2 |

## What agentic AI honestly means at sixty hertz

The ask is real and the honest version is specific: a language model cannot run inside a tick, but a **drive vector** can — a small utility model per being, evaluated against what it can perceive, scored, and acted on. Utility AI, GOAP, behaviour trees and a forty-weight network evolved by the same selection that already shapes bodies all fit in the budget. `vr/sim/gpgpu/` already exists, so thousands of tiny evaluations per frame is a solved shape here.

| # | Item | What and why | Kind | E | I |
|---|---|---|---|---|---|
| 241 | **State what the in-app model can be** <br>gives `aidesign` | The ask is agentic life. The constraint is 12 ms a tick shared with climate, tectonics and rendering, in a browser, offline. Inside that: a per-being utility model, a compiled behaviour tree, a GOAP planner over a handful of actions, or a network of order 100 weights. Outside it: anything that calls a server, and anything that runs a language model per being per tick. Write that boundary down before writing code, because it is the decision everything else in this category inherits. | Model | S | 3 |
| 242 | **Utility scoring as the base mechanism** <br>gives `utility` <br>needs `drivevec` `percept` | For each candidate action, score `drive × expected gain × feasibility` and take the best. It is the cheapest agentic architecture that produces legible behaviour, it degrades gracefully under a frame budget, and every term in it is a number the player can be shown — which is what makes it debuggable at all. | Model | M | 3 |
| 243 | **An action table, authored as data** <br>gives `actiontable` <br>needs `utility` | Eat, drink, flee, hide, hunt, graze, travel, rest, court, nest, tend, build, follow, disperse. Each with a cost, a precondition, a duration and an effect, in a JSON file compiled and validated the way `lifegrammar.mjs` compiles the life grammar. Behaviour becomes reviewable data rather than branches in `pickBehav`. | Model | M | 3 |
| 244 | **A behaviour genome, inherited and mutated** <br>gives `behgenome` <br>needs `utility` | The weights that turn drives into action preferences are heritable numbers, so they mutate, they are selected, and a lineage develops a temperament. This is the honest version of *AI-based life creation* in this product: the same selection that already shapes bodies shapes behaviour, and no external model is involved. | Model | L | 3 |
| 245 | **A forty-weight network as the alternative** <br>gives `tinynet` <br>needs `behgenome` | Six perceptual inputs, a hidden layer of six, five action outputs: roughly 78 weights, evaluated with 78 multiply-adds. At 1400 beings that is 109,000 operations a tick — under a tenth of a millisecond. Whether the policy is a weight matrix or a scored table is an implementation choice; both are affordable and the network is more surprising. | Model | L | 3 |
| 246 | **Evolve the policy, do not train it** <br>needs `behgenome` | There is no gradient here and there does not need to be one: selection is already running. A policy that survives is a policy that worked, and evolution strategies on a hundred weights across a thousand individuals over a thousand generations is exactly the regime where this works well. | Model | L | 3 |
| 247 | **Behaviour that speciates with the body** <br>needs `behgenome` | `morphTick` already applies module events and developmental locking. Behaviour modules under the same mechanism means a split lineage diverges in what it does as well as in what it looks like — which is what makes two similar animals different species in practice. | Model | M | 3 |
| 248 | **The GPU path, if it is needed** <br>needs `tinynet` | `vr/sim/gpgpu/` runs the climate tick in shaders already. A policy evaluation for thousands of beings is the same shape of problem. Do not build it first — measure the CPU version and only move it if the number says so. | Model | L | 2 |
| 249 | **Determinism, non-negotiable** <br>needs `utility` | `rngOf(W, 'rngAgents')` gives a seeded stream. Any policy has to be a pure function of state and that stream, or the fork, the save and the reproducible run all break — and those three are load-bearing for the entire product. | Prove | M | 3 |
| 250 | **A drive budget per tick** <br>needs `utility` | Perception, scoring and acting per being per tick, times up to 1400 beings, inside a 12 ms budget shared with everything else. State the target — a millisecond is generous — and assert it, because a behaviour system that misses it will be silently throttled by `noteDroppedTicks`. | Prove | S | 3 |
| 251 | **Level of detail for minds** <br>needs `utility` | Beings near the camera think every tick; distant ones every tenth, or not at all, updated as a density. This is the standard answer and it needs to be designed in rather than retrofitted, because the transition is where the visible artefacts live. | Model | M | 3 |
| 252 | **Explain a decision** <br>gives `explainai` <br>needs `utility` | Click a being: this is what it perceived, these were its drives, these were the candidate actions and their scores, this is what it chose and why. An agentic system nobody can interrogate is indistinguishable from a random one, and this panel is also the only practical way to debug it. | Show | M | 3 |
| 253 | **A temperament readout** <br>needs `behgenome` | Bold or cautious, social or solitary, generalist or specialist — derived from the behaviour genome and shown on the species page next to the body. It makes the invisible half of a lineage visible and it is a projection of numbers that already exist. | Show | M | 2 |
| 254 | **Behaviour that is wrong for the world it is in** <br>needs `behgenome` | `morphMult` already penalises a gilled land animal. A behaviour inherited from an ancestor that is maladaptive now — a migration route to a place that dried up — is the same idea and it is one of the most poignant things a simulation can show. | Model | M | 2 |
| 255 | **Do not let every being be the same being** <br>needs `behgenome` | Individual variation around the lineage mean, so a population has bold and shy members. It costs a few bytes, it is the substrate selection acts on, and it is why a crowd looks like a crowd rather than a clone army. | Model | S | 3 |
| 256 | **Author the starting policies, then let them go** <br>needs `actiontable` | A hand-authored default policy per guild as the seed, mutated from there. It gives the first minute of a run competent behaviour and gives evolution somewhere to start, which is the same argument the life pass made for keeping the named-body functions as fixtures. | Model | M | 3 |
| 257 | **A policy is data, so it can be saved** <br>needs `behgenome` `entsave` | The behaviour genome goes in the save next to the body genome. `serializeRun` is at version 5 and already stores every genome as JSON, so this is an added field rather than a new mechanism. | Model | S | 3 |
| 258 | **Assert a policy learns the world** <br>needs `behgenome` | Run two worlds — one where food is clustered, one where it is uniform — and assert the evolved policies differ in a stated direction: more travel in the clustered one. If they do not diverge, selection is not reaching behaviour and the whole category is decoration. | Prove | M | 3 |
| 259 | **Assert the cheap version is not worse** <br>needs `tinynet` | Compare the scored table against the network on the same worlds by a stated fitness measure. If the table wins, ship the table. This is the assertion that stops an interesting architecture from being chosen over a working one. | Prove | M | 2 |
| 260 | **No network calls, ever, in the tick** | The product runs from a static site and the whole appeal of an in-app model is that it is in the app. State it as an invariant with a lint, because it is the kind of thing that gets violated once and then is load-bearing. | Prove | S | 3 |

## A brain that costs something

The organ table has a brain and `phasedArray` is gated on it, and `sensory.js` decides band by band what a world delivers to an eye. So the pieces of a mind that is *earned* rather than assumed are already here: perception from physics, an organ with a metabolic price, and a genome that can gain or lose it. What is missing is the step where having a brain changes what a being does.

| # | Item | What and why | Kind | E | I |
|---|---|---|---|---|---|
| 261 | **A brain that has to be paid for** <br>gives `brainorg` <br>needs `energy` | The organ table already carries a brain and `phasedArray` is gated on it. Give it a metabolic cost proportional to size, so intelligence is a strategy with a price rather than a free upgrade — which is the actual reason most of the biosphere is not intelligent. | Model | M | 3 |
| 262 | **What a brain buys, precisely** <br>needs `brainorg` `utility` | More remembered places, longer planning horizon, more candidate actions considered, better prey estimation, the ability to learn at all. Each is a number scaled by brain size, so the trade is quantitative and a player can see both sides of it. | Model | M | 3 |
| 263 | **Perception range from the sense the animal has** <br>needs `percept` | `sensory.js` computes, band by band, what a world delivers to which receptor, and it is the best-grounded module in the biosphere. Wiring it to what a being can actually detect is the single highest-value connection available in this category. | Model | M | 3 |
| 264 | **A world model that can be wrong** <br>needs `percept` `memory` | A being acting on a stale memory — going to a waterhole that dried, avoiding a predator that left — is the difference between an agent and a lookup. It also costs nothing extra, since the memory is already stale. | Model | M | 3 |
| 265 | **Attention, so perception has a limit** <br>needs `percept` | A being tracks a few things, not everything in range. It is a performance measure and a behavioural mechanism at the same time, and it produces the failure mode that makes ambush predation work. | Model | M | 2 |
| 266 | **Communication, with a channel that exists** <br>needs `percept` | An alarm call needs the acoustic band `sensory.js` already evaluates; a pheromone trail needs a diffusing field; a display needs a visual band the star actually delivers. A signal that must travel through modelled physics is a signal that differs between worlds. | Model | M | 3 |
| 267 | **Signals that can lie** | Deception, mimicry, bluffing and warning colours that are honest or not. It is a small extension of a signalling system and it is where behaviour starts to be interesting rather than merely functional. | Model | M | 2 |
| 268 | **Tool use as an unlock with prerequisites** <br>needs `brainorg` | Requires a manipulator from the organ table, a brain above a threshold and a reason. It is the bridge between the animal layer and the civilisation layer and it should be crossed by a lineage, at a date, in a place. | Model | M | 3 |
| 269 | **Sleep, and what it costs to skip** | `isOutNow` already gates activity on a diel cycle. Making rest a requirement with a debt gives predators a window, gives the day a structure, and gives the local view a reason to look different at night. | Model | S | 2 |
| 270 | **Play, which is how you know it is a mind** | Juveniles that do things with no immediate payoff. It is cheap, it is only worth having if there are age classes, and it is the single most effective animation for making a creature read as alive. | Show | M | 2 |
| 271 | **An encephalisation quotient, measured** <br>needs `brainorg` | Brain mass against body mass has a published scaling exponent, so a lineage can be plotted against the real relation and the outliers named. This is the kind of check the life pass used for Kleiber and it is available here for free. | Prove | S | 2 |
| 272 | **Convergent intelligence, allowed and priced** <br>needs `brainorg` | Cephalopod, corvid, primate — three routes to the same capability with different bodies. The morphology rules already allow convergence and charge for it, which is exactly the right treatment. | Model | M | 2 |
| 273 | **What a mind is like on a dark world** <br>needs `percept` | Under 15 km of Europan ice no photon band survives, and `sensory.js` already ranks electroreception and flow sensing first there. A mind built on electric sense would model the world differently, and that is a genuinely alien result derived from physics rather than invented. | Model | M | 3 |
| 274 | **The sense overlay, from a specific animal** | The `sense` overlay shows the globe as the dominant lineage perceives it, which is one of the best ideas in the product. Point it at an individual instead, in the local view, and it becomes the strongest teaching tool here. | Show | M | 3 |
| 275 | **Do not give everything a mind** | Most of the biosphere by mass is bacteria, plants and fungi. The drive model must degrade to nothing for a mat, a tree or a sponge, and the fact that it does should be visible rather than a special case buried in a condition. | Model | S | 3 |
| 276 | **A mind that the tree remembers** <br>needs `brainorg` | When a lineage crosses a cognitive threshold, log it as a transition with a date and a cell, the way `transitions` logs photosynthesis. The first tool, the first teaching, the first signal — these are the milestones a run should be able to list afterwards. | Show | S | 3 |
| 277 | **A brain in the body plan drawing** | `drawCreature` builds a silhouette from the expressed plan. A large-brained animal should look like one — head proportion, eye size, posture — so the cognitive axis is visible in the same picture as everything else. | Show | M | 2 |
| 278 | **Assert the brain is not free** <br>needs `brainorg` | On a low-productivity world, assert large-brained lineages are selected against; on a variable one, assert they are favoured. That is the published explanation for why intelligence is rare and it is a testable prediction of this model. | Prove | M | 3 |
| 279 | **Assert perception gates behaviour** <br>needs `percept` | A blind lineage and a sighted one on the same world should produce measurably different movement statistics. If they do not, `sensory.js` is not connected to anything that acts. | Prove | M | 3 |
| 280 | **One page on what a mind is here** | The glossary already carries definitions. Cognition is the area where a simulation most easily overclaims, so the page should say exactly what is modelled, what is asserted and what is a sprite — the same honesty the provenance tags apply to materials. | Prove | S | 3 |

## Learning within a life, culture between lives

Evolution in this product is genetic and generational. The two faster loops are absent: an individual that changes its behaviour because of what happened to it, and a behaviour that passes from one individual to another without passing through a genome. Both are cheap, both are visible in seconds rather than eons, and both are what makes a watched world feel inhabited rather than simulated.

| # | Item | What and why | Kind | E | I |
|---|---|---|---|---|---|
| 281 | **Learning inside one lifetime** <br>gives `lifelearn` <br>needs `utility` `memory` | A small update to a being's own policy from what happened: this place had food, that place had a predator, this prey fought back. Reinforcement on a handful of weights, bounded, decaying. It is the loop that operates in seconds rather than eons, which is exactly what a watched world needs. | Model | M | 3 |
| 282 | **Habituation and sensitisation** <br>needs `lifelearn` | The two simplest forms of learning, present in animals with almost no nervous system, and both are a single decaying weight. Cheap enough to give to nearly everything, which is what makes a whole population respond to a repeated event. | Model | S | 3 |
| 283 | **Imprinting, and what it fixes** <br>needs `lifelearn` | What a juvenile learns early it keeps. It creates lineages of preference within a species — natal site fidelity, host preference — and it is one of the recognised routes to speciation without geographic isolation, which `evolveTick` currently reaches only through an isolation scalar. | Model | M | 2 |
| 284 | **Learned aversion, from a real event** <br>needs `lifelearn` | A defended prey that is tasted once and avoided afterwards. It makes the genome defence axis pay off through behaviour rather than through a fitness multiplier, and it is the mechanism behind warning colouration. | Model | M | 2 |
| 285 | **Knowing where things are** <br>needs `memory` `lifelearn` | Spatial learning is the highest-value learning in a world with a heterogeneous resource map, and this world has one. It converts a random walk into a route and it is the substrate for both migration and territory. | Model | M | 3 |
| 286 | **Imitation between individuals** <br>gives `culture` <br>needs `lifelearn` | A behaviour copied from a neighbour without passing through a genome. It is the second inheritance channel, it spreads orders of magnitude faster than mutation, and it is the mechanism that makes a population feel like it has a shared way of doing things. | Model | L | 3 |
| 287 | **Teaching, which costs the teacher** <br>needs `culture` | An adult that spends time transferring a behaviour to a juvenile at a cost to itself. Rare in nature, expensive, and the precondition for anything cumulative — which is the precondition for the civilisation ladder to mean anything. | Model | M | 3 |
| 288 | **A tradition, held by a group** <br>needs `culture` `group` | A behaviour a group has and a neighbouring group does not. Traditions drift, they can be lost when a group dies, and they can be traced — which makes a cultural phylogeny alongside the genetic one, drawn by the same tree code. | Model | M | 3 |
| 289 | **Cumulative culture, and the threshold for it** <br>needs `culture` | When transmission fidelity times population size passes a threshold, behaviours can accumulate rather than being reinvented. There is a real quantitative argument for that threshold, it depends on population size, and it is the honest bridge from animal to civilisation. | Model | L | 3 |
| 290 | **Dialects, drawn on the globe** <br>needs `culture` | A learned signal that diverges between regions. It is a visible map of cultural isolation, it uses the same clade-colour machinery as `cladeRGB`, and it is one of the few cultural phenomena that renders naturally at globe scale. | Show | M | 2 |
| 291 | **Culture that can go extinct without the population** <br>needs `culture` | A tradition lost while the species survives. It is a distinct kind of loss from extinction, it is measurable, and it gives the chronicle a class of event it does not currently have. | Model | M | 2 |
| 292 | **The Baldwin effect, if it earns its place** <br>needs `culture` `behgenome` | Learned behaviour changing which genomes are selected. It is a real and much-argued mechanism; if the model shows it, that is a genuine result worth a chronicle entry, and if it does not, that is worth knowing too. | Model | L | 2 |
| 293 | **A learning rate that is a trait** <br>needs `lifelearn` `behgenome` | How fast an individual learns is heritable and costly — a fast learner is also a fast mis-learner. Making it a genome axis puts the plasticity–specialisation trade under selection instead of into a constant. | Model | M | 2 |
| 294 | **Show what an animal has learned** <br>needs `lifelearn` `beinginspect` | In the inspect panel: the places it knows, the things it avoids, what it learned most recently. It is the only way a player will believe learning is happening rather than being claimed. | Show | M | 3 |
| 295 | **A learning event in the chronicle** <br>needs `culture` | The first tool, the first taught behaviour, the first tradition to spread beyond its group. `maybeCaptureMoment` already captures firsts; these are the behavioural ones and they are the milestones this backlog is aiming at. | Show | S | 3 |
| 296 | **Bound the learning so it cannot run away** <br>needs `lifelearn` | Unbounded reinforcement on a shared policy produces degenerate behaviour within a few thousand ticks — every being doing the one thing that scored best once. Clamp, decay and inject noise, and assert the diversity of behaviour does not collapse. | Prove | M | 3 |
| 297 | **Assert learning beats not learning** <br>needs `lifelearn` | Two identical lineages on a variable world, one with learning enabled. Assert the learner has higher survival, and assert that on a uniform world it does not — because learning should only pay where there is something to learn. | Prove | M | 3 |
| 298 | **Assert a tradition spreads and stops** <br>needs `culture` | Seed a behaviour in one group and measure its spread: faster than genetic spread, blocked by the same barriers, lost when the group is culled. Three assertions that separate culture from a global flag. | Prove | M | 3 |
| 299 | **The cost of learning in milliseconds** <br>needs `lifelearn` | A policy update per being per tick is a few multiply-adds, so the honest answer is that it is nearly free and the expensive part is the perception it depends on. Measure both separately so the attribution is right. | Prove | S | 2 |
| 300 | **Say which of the two channels a behaviour came from** <br>needs `culture` | Genetic or learned. It matters for what happens when the population crashes, and a readout that confuses them will teach the player something false about how inheritance works. | Show | S | 3 |

## Gaia is four if-statements

`gaiaPolicyTick` in `god/observe.js` is the whole autopilot: if too cold raise solar, if too hot lower it, if CO₂ is low inject some, if CO₂ is high draw it down. Four thresholds on two globals. Meanwhile `gaiaTick` computes resilience, tipping elements with hysteresis, rate stress, feedback gain and a Medea score — a rich state vector that no controller reads. The instrumentation for a planetary drive exists and nothing drives. A second controller, `thermostatTick` in `god/climate.js`, pins `meanTemp` by cheating solar and stamps the run as not counting. Two thermostats, zero minds.

| # | Item | What and why | Kind | E | I |
|---|---|---|---|---|---|
| 301 | **Four if-statements are the whole autopilot** <br>gives `gaiapolicy` | `gaiaPolicyTick` reads `meanTemp` and `gases.CO2` and applies four threshold nudges to solar and CO₂. Meanwhile `gaiaTick` computes resilience, seven tipping elements with hysteresis, rate stress, feedback gain, a Medea score and habitability-versus-inhabitance. A rich state vector exists and the controller reads two scalars of it. | Model | M | 3 |
| 302 | **A controller that reads the whole state** <br>needs `gaiapolicy` | Resilience, tipping proximity, rate stress and biosphere trend, weighted into an objective. It is still a small amount of arithmetic and it is the difference between a thermostat and something that appears to have a view about the planet. | Model | M | 3 |
| 303 | **Say what it is optimising** <br>needs `gaiapolicy` | Maximum biomass, maximum diversity, maximum stability and maximum complexity are different objectives that produce visibly different planets. Naming the objective is what makes a planetary controller honest rather than mystical. | Model | M | 3 |
| 304 | **Act before the tipping point, not after** <br>needs `gaiapolicy` | `TIPPING` gives seven elements with thresholds and hysteresis, so proximity is computable. A controller that anticipates is qualitatively different to watch from one that reacts, and the data it needs is already on `W.tips`. | Model | M | 3 |
| 305 | **Look ahead by running the model** <br>needs `gaiapolicy` | `forkRun` exists. A controller that forks, runs a few hundred cheap ticks under two candidate actions, and picks the better is model-predictive control — and it is the most defensible form of planetary intelligence this product could actually ship. | Model | L | 3 |
| 306 | **Keep Gaia honest about what it is** | `W.gaiaMode` is already set to `survivorship` or `transient` and the wording in the code is careful about selection rather than purpose. Any controller has to be labelled: a player autopilot, a tutorial feedback loop, or an explicitly fictional planetary mind. The product's credibility rests on that label. | Model | S | 3 |
| 307 | **Three named Gaia dispositions** <br>needs `gaiapolicy` | Gardener, which maximises life; regulator, which minimises variance; experimenter, which perturbs to see what happens. Selectable, visibly different, and each a different weighting of the same objective — which teaches what a control objective is. | Play | M | 3 |
| 308 | **The Medea option** | `medeaScore` already computes biosphere self-harm. A controller that pursues it is the counterweight to Gaia and it is a real hypothesis in the literature, not a joke setting. | Play | M | 2 |
| 309 | **A log that says why** | `W.gaiaLog` keeps 60 entries of the form `raised solar — too cold`. With a real objective the log can state the cost, the alternative and the expected effect, which turns the autopilot into a demonstration of control theory rather than a black box. | Show | S | 3 |
| 310 | **A voice for the biosphere** <br>gives `gaiavoice` <br>needs `gaiapolicy` | One sentence at a time, in the chronicle: what the biosphere is doing, what is limiting it, what it is about to lose. The narration should be generated from the state vector by templates over real numbers, never invented — and it is what makes a planet feel like it has a drive. | Show | M | 3 |
| 311 | **Do not let the autopilot cheat** <br>needs `gaiapolicy` | `gaiaPolicyTick` writes `W.solar` and `W.gases.CO2` directly. A controller should act through the same verbs the player has — the god layer has 15 modules of them — with the same costs and the same receipts, or it is not a player of the game. | Model | M | 3 |
| 312 | **The energy budget as the constraint** | `W.energy`, `energyCap` and `energyIncome` exist and `god/economy.js` and `god/receipt.js` are already built. A controller on a budget has to choose, and choosing is what makes its behaviour interesting. | Model | M | 3 |
| 313 | **Gaia versus the player** <br>needs `gaiapolicy` | Let the controller run while the player interferes, and show the two sets of actions in the same log. It is a game mode, a tutorial and a demonstration of feedback all at once. | Play | M | 3 |
| 314 | **Regulation that fails visibly** <br>needs `gaiapolicy` | A controller pushed past what its verbs can fix should visibly lose — because that is the actual lesson of planetary regulation, and a controller that always wins teaches the opposite of the intended thing. | Model | M | 3 |
| 315 | **Daisyworld as the honest tutorial** | The daisyworld path already exists with N species, `gaiaMode = tutorial-feedback` and a rising sun. It is the one place regulation is genuinely emergent rather than controlled, and it should be presented as the reference case any controller is compared against. | Show | M | 2 |
| 316 | **A planetary dashboard** | Resilience, feedback gain, rate stress, tipping proximity, biosphere trend, and what the controller intends to do next. Six numbers that already exist, in one place, is the difference between an instrumented planet and a green sphere. | Show | M | 3 |
| 317 | **Feedback gain that is measured, not fitted** | `W.feedbackGain` is `weatheringFlux × 20 − rateStress`, clamped. Gain should be measured by perturbing the system and observing the response — which the fork machinery makes possible and which turns a fitted constant into an experiment. | Prove | M | 3 |
| 318 | **Assert the controller helps** <br>needs `gaiapolicy` | Same seed, same forcing, controller on and off: assert the objective it names is measurably better with it on. An autopilot nobody has measured is a set of four thresholds somebody guessed. | Prove | M | 3 |
| 319 | **Assert it cannot save everything** <br>needs `gaiapolicy` | Under a large enough forcing the controller must fail, and the failure should be graceful and legible. Assert the failure mode, because an unbounded controller will eventually be found holding a planet in a state the physics does not allow. | Prove | M | 3 |
| 320 | **One page on what Gaia means here** | Survivorship, not purpose — the code says it and the interface should too. The distinction between a biosphere that regulates because regulators persist and a biosphere that intends to regulate is the single most misunderstood idea this product touches. | Prove | S | 3 |
| 321 | **Two thermostats, pick one** | `gaiaPolicyTick` nudges solar and CO₂ when `W.autopilot` is on. `thermostatTick` in `god/climate.js` pins `meanTemp` by cheating solar and marks the run as not counting. They can both run. A planet with two independent temperature controllers is not regulating; it is being held. Delete or fuse, and make the cheat remain a cheat. | Model | S | 3 |
| 322 | **`forecastAct` exists and Gaia never calls it** <br>needs `gaiapolicy` | `god/receipt.js` already forecasts a tool at 10, 100 and 1,000 years. That is the lookahead the controller was going to invent. Wire it, or the fork-and-roll item is reinventing a function that is sitting unused one directory over. | Model | M | 3 |
| 323 | **`playStyle` exists and Gaia never reads it** <br>needs `gaiapolicy` | `playStyle` and `restraintStats` already classify the god from the intervention log. A planetary controller that does not know whether it is dealing with a vandal or a gardener cannot choose a response, and those two functions are the classification. | Model | S | 3 |
| 324 | **Control on three timescales** <br>needs `gaiapolicy` | Clouds and ice answer in years, vegetation in decades to millennia, silicate weathering in 100 kyr to 1 Myr. A single 0.002 solar nudge per tick has no timescale. Split the controller into fast, slow and geologic verbs, or it will fight the carbon cycle with a dimmer switch. | Model | L | 3 |
| 325 | **The carbon cycle is the nervous system** <br>needs `gaiapolicy` | Photosynthesis, respiration, weathering, burial, outgassing — the signals a planetary mind would actually have. `W.carbon` already carries fluxes. Reading `meanTemp` instead of those fluxes is listening to a fever without taking a pulse. | Model | M | 3 |
| 326 | **Disequilibrium is what it feels like to be alive** | `W.disequilibrium` is `sqrt(O2 × CH4) × 200`, the one scalar that means something here is fighting entropy. A controller that does not read it is ignoring the biosignature this product already computes, and a voice that does not mention it is not talking about life. | Model | S | 3 |
| 327 | **N-species Daisyworld with mutable albedo** | Lenton and Lovelock 2000: let albedo mutate and the regulated temperature range widens because the system keeps finding new species to do the job. `daisyNSpeciesTick` exists. Mutable albedo is a small change and it is the answer to the standard objection that Daisyworld is a toy with two paint colours. | Model | S | 2 |
| 328 | **The Gaian bottleneck, as a catalogue claim** | Chopra and Lineweaver: most biospheres fail to establish regulation before the planet runs away. Run the 120-world catalogue with the controller off, count how many ever regulate, and you have an empirical claim this product can actually make rather than a quotation. | Prove | M | 2 |
| 329 | **Kirchner's critique, playable** | Homeostasis can be a coincidence of forcings, not a biosphere doing work. A scenario that turns life off and shows the same temperature path would falsify Gaia on that world. If the product cannot run that experiment it cannot claim the hypothesis. | Prove | M | 3 |
| 330 | **Do not let the policy replace the physics** <br>needs `gaiapolicy` | If the controller is the only reason temperature is stable, the planet is not a Gaia world — it is a clamped slider. Assert that with autopilot off, weathering, ice-albedo and clouds still oppose a perturbation, or the sentience layer is a cheat code wearing a myth. | Prove | M | 3 |
| 331 | **A closed demo: perturb, wait, photograph** <br>needs `gaiapolicy` | Double CO₂, run 200 years of presentation time, export before and after discs, with the controller on and off. This is the modern SimEarth trailer and it is also the test that the face, the metabolism and the policy are the same event. | Play | M | 3 |

## What it would mean for a planet to be sentient

Watson and Lovelock never claimed a mind. The code already knows this: `W.gaiaMode` is `survivorship`, `transient` or `tutorial-feedback`. Sentience here is a labelled mode with the same four parts a being gets — perception, drives, memory, action — running on the planetary state vector, acting through the player's verbs, on the player's budget, able to be wrong and able to die. Anything else is a thermostat with a face drawn on it.

| # | Item | What and why | Kind | E | I |
|---|---|---|---|---|---|
| 412 | **Name the three modes and refuse a fourth** <br>gives `planetclaim` | Survivorship, tutorial-feedback, and an explicitly fictional planetary mind. `W.gaiaMode` already holds the first two. A fourth — a mind that is implied but not labelled — is how the product would lose the argument Lovelock actually won. The switch is a ruleset flag and a sentence in the HUD, and it has to be on before any voice, face or drive is allowed to sound like intent. | Model | S | 3 |
| 413 | **A mind is four parts, the same four a being gets** <br>needs `planetclaim` `drivevec` | Perception, drives, memory, action. `gaiaTick` is perception; the missing drive vector is this backlog; memory is the climate record nobody keeps; action is the god verbs. Writing "sentient" without those four is drawing a face on a thermostat, which is what `gaiaPolicyTick` already is. | Model | M | 3 |
| 414 | **Distributed regulation is not a mind** <br>needs `planetclaim` | Daisyworld regulates because black and white daisies have different temperature optima, not because anything decides. Presenting that as sentience is the exact confusion the tutorial exists to prevent. Keep Daisyworld as the proof that regulation needs no foresight, and keep the fictional mind in a different ruleset so they cannot be mistaken for each other. | Model | S | 3 |
| 415 | **Autopilot is a player, not a planet** <br>needs `planetclaim` `gaiapolicy` | `W.autopilot` is a checkbox that acts with god verbs. That is a gardener-on-rails, and it is a good mode. It is not the biosphere waking up. The interface has to say *you handed the tools to a script*, or players will believe the daisies learned to work the solar slider. | Play | S | 3 |
| 416 | **The fiction switch, on the world, in the save** <br>needs `planetclaim` | A planetary mind is a property of a run, stored next to `autopilot`, visible on the shelf entry, and stripped from any scenario that claims to be a science result. `shelfEntryFromWorld` already stores `disequilibrium`; storing the claim is the same field. | Model | S | 3 |
| 417 | **Four minds can share one sphere** <br>needs `planetclaim` | The biosphere (survivorship), the civilisation (`notice.js`), the player, and the autopilot. They have different sensors, different timescales and different verbs. The interesting game is the argument between them, and today only the player has a body. | Play | M | 3 |
| 418 | **When the civ becomes the planet's cortex** <br>needs `planetclaim` `bandtociv` | A scientific civilisation with a carbon model is a planetary mind made of people. SimEarth's late game was this. `civ.worship === 'scientific'` is a string assigned from a style heuristic; it should be the start of a control loop that competes with Gaia rather than a flavour label. | Model | L | 3 |
| 419 | **Superorganism versus organism, stated** <br>needs `planetclaim` | A reef colony, an ant nest, a planet. The group record this backlog already asks for is the same object at three scales. Write down which scale is allowed to have a drive vector, or every mat will get a hunger and the word will mean nothing. | Model | S | 3 |
| 420 | **A being that is the whole sphere** <br>needs `planetclaim` `pdrive` | One ENT-shaped record for the planet: energy as the free-energy disequilibrium, cell as undefined, lineage as the tree, name as the world's. Inspecting the planet with the same panel as a creature is the joke that teaches the architecture, and it is also a real debug view of the controller. | Show | M | 2 |
| 421 | **Sentience as inhabitance of the control loop** <br>needs `planetclaim` | `W.inhabitance` is meanLife × 1.2. A better split: inhabitance is biomass, sentience-mode inhabitance is whether anything is actually closing a loop. A green sterile-looking desert of cyanobacteria can regulate; a lush world with the controller off and the tips unconnected cannot. | Model | M | 3 |
| 422 | **The hard problem is a glossary page** <br>needs `planetclaim` | `glossary.js` already carries definitions. Cognition is where a simulation most easily overclaims. The page says what is modelled (a controller), what is presented (a voice, a face), and what is not claimed (phenomenal experience). Same honesty the provenance tags apply to materials. | Prove | S | 3 |
| 423 | **Watson and Lovelock never claimed a mind** <br>needs `planetclaim` | The original Daisyworld paper is a proof about feedback, and the product owns a working Daisyworld. Quote it in the tutorial, then show the fictional mode as a separate toy. Borrowing their authority for a talking planet is the one way to make the science layer look like marketing. | Prove | S | 3 |
| 424 | **A planet that can be wrong about itself** <br>needs `planetclaim` `pdrive` | Acting on a stale memory — raising solar because last era was cold, fighting an AMOC that already recovered — is the difference between a lookup and a mind. `W._prevMeanTemp` is the entire climate memory today. A ring buffer of a few eras is the cheapest possible inner life. | Model | M | 3 |
| 425 | **Death of a mind** <br>needs `planetclaim` | Snowball, moist-greenhouse and sterilising impact already set `W.state`. The fictional mind should go quiet with them: no voice, no face, `gaiaMode = 'gone'`. A corpse that keeps chatting is a worse fiction than a silent rock. | Show | S | 3 |
| 426 | **Information that is actually integrated** <br>needs `planetclaim` | Seven tipping elements, a carbon vector, a tree, a disequilibrium scalar. A cheap integration score — how many of those streams are coupled this tick — is a better "how awake is this planet" meter than `W.health`, and it uses no new physics. | Model | M | 2 |
| 427 | **A mind that thinks in Myr cannot talk in seconds** <br>needs `planetclaim` `biotime` | Weathering answers in 100 kyr. A sentence every frame about what Gaia "wants" is lying about timescale. Voice rate has to track the biology clock, or the planet sounds frantic on a world whose only move this century is a millimetre of soil. | Show | M | 3 |
| 428 | **Do not call Daisyworld sentient** <br>needs `planetclaim` | `gaiaMode = 'tutorial-feedback'` is already the right label. Guard the voice, the face and the drive vector behind `gaiaMode === 'mind'` (or whatever the flag is named), and assert Daisyworld never emits a sentence of intent. | Prove | S | 3 |
| 429 | **The label has to match the mechanism** <br>needs `planetclaim` | If the HUD says mind, the controller must be reading more than two scalars and acting through verbs. If it says survivorship, no voice of intent. Assert the pairing, because a mismatched label is how this feature becomes an embarrassment rather than a mode. | Prove | S | 3 |
| 430 | **Kirchner on a switch** <br>needs `planetclaim` | A scenario toggle that kills the biosphere and keeps the geology, so the player can see which part of the temperature path was life. That experiment is the whole debate, and `W.life` can already be zeroed. The missing piece is the before/after and a sentence that does not editorialize the result. | Play | M | 3 |
| 431 | **A run that is allowed to stay a rock** <br>needs `planetclaim` | Most catalogue worlds should never wake. The default is silence. Sentience is a rare, expensive, labelled event — the same rarity argument this backlog already makes for brains in animals. If every sphere talks, none of them are a planet. | Play | S | 3 |

## The same drive architecture, one level up

A being is supposed to get a drive vector, perception gated on `sensory.js`, and actions scored from a table. The planet already has the analog of all three and uses none of them: `gaiaTick` is the sensory stream, `W.tips` / `rateStress` / `medeaScore` / `disequilibrium` are the drives, and the god layer is fifteen modules of actuators. `gaiaPolicyTick` ignores the stream and writes two globals. This category is the wiring, not a new invention.

| # | Item | What and why | Kind | E | I |
|---|---|---|---|---|---|
| 432 | **A drive vector for the planet** <br>gives `pdrive` <br>needs `gaiapolicy` `planetclaim` | Hunger, heat, cold, fear (tip proximity), pain (`rateStress`), crowding (inhabitance vs habitability), Medea. Continuous, decaying, discharged by verbs. This is `drivevec` at radius 6,370 km, and it is what lets the four if-statements die without being replaced by a larger pile of if-statements. | Model | M | 3 |
| 433 | **Hunger is whatever is limiting** <br>needs `pdrive` | Light, water, phosphate, nitrogen, carbon, space. The model computes each per cell and never says which is binding. The loudest planetary hunger is that binding constraint, averaged with a stated rule, and it is the sentence a metabolism would actually utter. | Model | M | 3 |
| 434 | **Pain is rate stress** <br>needs `pdrive` | `W.rateStress` already exists from dT/dt. Map it onto a drive and a fast anthropogenic pulse hurts more than a slow Milankovitch one, which is the published reason ecosystems die of speed rather than of climate. No new field. | Model | S | 3 |
| 435 | **Fear is tipping proximity** <br>needs `pdrive` `tipgeo` | Distance-to-threshold on each `TIPPING` row, the nearest one the loudest. A controller that is afraid of the Amazon and not of the AMOC is a controller that has a geography, which is the whole point of treating tips as places. | Model | M | 3 |
| 436 | **Heat and cold, still, but not as the whole mind** <br>needs `pdrive` | The current four ifs are two drives. Keep them as the loudest pair — temperature is the variable Daisyworld was built to regulate — and stop letting them drown carbon, ice, diversity and disequilibrium. | Model | S | 3 |
| 437 | **Perception is the state vector, named** <br>needs `pdrive` | meanTemp, iceFrac, gases, carbon fluxes, tips, disequilibrium, resilience, inhabitance, habitability, medeaScore, dTempDt. Write it as a struct the controller reads and the inspect panel prints. Today those numbers live on `W` and the policy peeks at two of them. | Model | S | 3 |
| 438 | **Memory of climates, a ring of eras** <br>needs `pdrive` | `W._prevMeanTemp` is one float. Keep a small ring — last season, last century, last glacial, last burial pulse — so the planet can return to a place, fear a place, and be wrong about a place. Same shape as the being memory item, same byte argument. | Model | M | 3 |
| 439 | **Attention: which basin is in trouble** <br>needs `pdrive` `tipgeo` | A being tracks a few things, not everything in range. The planet should track a few regions — the dying forest, the stalled conveyor, the bleaching reef — and put the camera and the verbs there. `shouldHaltFF` already stops for anomalies; this is the same idea aimed at organs. | Model | M | 3 |
| 440 | **Actuators are the god verbs, all of them** <br>needs `pdrive` | Solar, CO₂, albedo paint, aerosol, seed, refuge, trip conveyor, local weather, magnetosphere. `gaiaPolicyTick` uses two. A mind with two muscles is a neck. The budget and the receipts already price the rest. | Model | M | 3 |
| 441 | **Cannot think every tick** <br>needs `pdrive` | `forkRun` and `forecastAct` are expensive. Planetary deliberation is every N years of `dtYr`, with a stated staleness, the way beings get LOD. A mind that re-plans at 60 Hz on a 10 kyr tick is the same bug as agents on the render loop. | Model | M | 3 |
| 442 | **The same utility scorer as a being** <br>needs `pdrive` `utility` | Candidate verbs scored against drives, pick the best that the budget allows. One mechanism, two scales. If the planetary scorer is a new pile of thresholds, the architecture thesis of this backlog is decoration. | Model | M | 3 |
| 443 | **Explain a planetary decision** <br>needs `pdrive` `explainai` | Click the planet: these were the drives, these were the candidate verbs and their scores, this is what it chose, this is what it expected, this is what happened. `W.gaiaLog` currently stores `{ t, acts }` strings. Replace the string with the score row. | Show | M | 3 |
| 444 | **A temperament for a world** <br>needs `pdrive` | Gardener, regulator, experimenter, Medea — the dispositions already named in gaiamind — as a weighting of the same drives, stored on the ruleset, visible on the catalogue card. Different worlds wake differently, which is the point of having 120 of them. | Play | M | 3 |
| 445 | **Thinking costs energy** <br>needs `pdrive` | `thermoCost` already prices tools. Forking a run to look ahead should cost more than a reflex nudge. A mind that plans for free will always plan, which is not how metabolisms work and not how this economy was designed. | Model | S | 3 |
| 446 | **LOD: coarse when the player is on the ground** <br>needs `pdrive` | Full deliberation at orbit, a slow pulse at regional, frozen when the player is inside a tile watching a hunt. The interesting event at ground scale is the animal; a planet muttering about CO₂ over it is the wrong narrator. | Model | M | 2 |
| 447 | **Determinism, same stream** <br>needs `pdrive` | `rngOf(W, 'rngGod')` already serves notice and prayers. Planetary policy uses that stream or a dedicated `rngGaia`, never `Math.random`, or the fork, the save and the catalogue experiment all break. | Prove | S | 3 |
| 448 | **Save the planetary policy** <br>needs `pdrive` `entsave` | Drive weights, memory ring, current attention, last decision. `serializeRun` is at version 5. A reload that forgets why the planet was raising solar is a reload that breaks the character. | Model | S | 3 |
| 449 | **Assert drives change with forcing** <br>needs `pdrive` | Pulse freshwater, fear of AMOC should rise. Dim the sun, cold should rise. If the vector is static under a stated forcing, it is a skin on the four ifs and this category has failed. | Prove | M | 3 |
| 450 | **Delete the four if-statements when this ships** <br>needs `pdrive` | The end state is that `gaiaPolicyTick` no longer exists as four thresholds. Same deletion test this backlog already applies to `pickBehav`. If the function is still there wrapped, the mind did not land. | Model | M | 3 |
| 451 | **A millisecond budget for the planet** <br>needs `pdrive` `entbudget` | One scorer, a handful of forecasts, no per-cell loop. State the cost and assert it. Planetary thought that scans `NC` to "feel" is how the 12 ms budget dies, and `gaiaTick` already did the reduction to scalars. | Prove | S | 3 |

## A metabolism you can see

Earth as a face is fluxes: carbon in, carbon out, albedo up, ice back, a northern inhale every summer. `W.health` is a blend of diversity, meanLife, temperature error and rate stress, and the disc does not change when it moves. A sentient planet that does not breathe on screen is a number. The carbon module, NPP, ice, haze and `disequilibrium = sqrt(O2 × CH4) × 200` are already the lungs; they are not drawn as lungs.

| # | Item | What and why | Kind | E | I |
|---|---|---|---|---|---|
| 452 | **Show carbon breathing as a pulse on the land** <br>gives `metab` <br>needs `gaiaface` | The Keeling curve is the inhale of the north. `nppField` already has a phenology term from `W.season` and latitude. A subtle darkening of northern green in summer and a browning in winter is the same pulse. Do not make it neon; make it true. That pulse is the first proof the planet is a metabolism rather than a texture. | Show | M | 3 |
| 453 | **Albedo as the clothing the planet puts on** <br>needs `metab` | Ice, cloud, canopy, desert, daisy bands, `albedoPaint`. The controller already wants to raise and lower temperature; clothing is how a real Gaia would do it, and `paintAlbedo` is already a god verb. Draw the Bond albedo as a disc average next to the face so a white world is a choice you can see. | Show | M | 3 |
| 454 | **Ice–albedo as a visible runaway, not a floor** <br>needs `metab` `homeo` | `iceTick` floors `iceLand` above 0.86 and `iceSea` above 0.88 so the caps cannot die. A snowball should be a thing you watch: white grows, albedo rises, white grows. The clamp hides the most famous Gaia story and it makes a sentient planet unable to freeze itself. | Model | M | 3 |
| 455 | **Weathering as a slow green-to-pale of mountains** <br>needs `metab` `homeo` | The Walker thermostat is a CO₂ leak from warm wet land. The picture is mountains going dull and rivers going milky. If CO₂ drops with no change on the ground, the thermostat is a cheat code and the metabolism is off-screen. | Show | M | 2 |
| 456 | **Oxygen as a colour of the sky, late** <br>needs `metab` | Ozone from O₂ is why twilight is blue. Deep time should have a pale, ozone-poor limb until the GOE, then the ring appears. That is Gaia as a history of the photograph, and `uOzone` is already a mix factor waiting to be a real absorption. | Show | M | 3 |
| 457 | **Disequilibrium as the living smell** <br>needs `metab` | `W.disequilibrium = sqrt(O2 × CH4) × 200`. Plot it, speak it, colour a thin limb with it. A world that is chemically at equilibrium is a world that has stopped fighting, which is the honest synonym for dead. | Show | S | 3 |
| 458 | **NPP as the heartbeat on the disc** <br>needs `metab` | `W.npp` is per cell and the overlay exists. Animate it as a slow glow whose integral is the pulse, not as a static false-colour. A heartbeat you can count is how a child reads a body; the same trick works on a planet. | Show | M | 3 |
| 459 | **Cloud as a thermostat the player can see** <br>needs `metab` | Clouds raise albedo and they are already a field. A controller that seeds clouds (`seedClouds` exists) should leave a visible deck, and a world cooling by cloud should go milky from orbit, not just drop a number. | Show | M | 2 |
| 460 | **Dust as a fever** <br>needs `metab` | `gases.dust` and the dust storm path already exist. A planet fighting heat with haze should look sick — brown, dim, the sun a smeared disc — because that is what a fever looks like from outside. | Show | M | 2 |
| 461 | **The anti-greenhouse haze as a veil with a cause** <br>needs `metab` | `hazeAntiGreenhouse` reddens and cools. Titan and Archean Earth should go orange for a reason you can point at: a photochemical haze that also changes the limb. The veil is the metabolism, not a tint. | Show | M | 2 |
| 462 | **Draw the carbon budget on the planet** <br>needs `metab` | In, out, burial, outgassing, player injection. `W.carbon` has fluxes. A small Sankey or a pair of opposing arrows on the Lab Gaia card is the difference between a health number and a breathing body. | Show | M | 3 |
| 463 | **Connect `W.health` to something you can see** <br>needs `metab` `gaiaface` | `health` is already in the calibrate snapshot. If it is a function of ice, life and temperature, the disc should look sick when it is low: more desert, more ice, or a bleach. A healthy number on a lime continent is the old bug. | Show | M | 3 |
| 464 | **A sick Earth should look sick in true colour first** <br>needs `metab` `gaiaface` | Snowball, hothouse, anoxic — the photograph changes before the gauge. If Health is low and the disc still looks like a Blue Marble, the face is lying and the mind is a HUD. | Show | M | 3 |
| 465 | **Phosphorus as the slow pulse** <br>needs `metab` | Productivity limited by the scarcest nutrient is already an evolution-backlog item. At planetary scale it is the metabolic rate the mind cannot hurry. Show the bottleneck on the face as a long, reluctant beat, not as a missing green. | Model | M | 2 |
| 466 | **Water as blood** <br>needs `metab` | Close the water budget and the planet has a circulation the way a body has one. Sea level, ice volume, vapour and soil moisture are the reservoirs. A mind that regulates temperature while leaking water mass is a mind ignoring a haemorrhage. | Model | M | 3 |
| 467 | **Heat transport as circulation you can feel** <br>needs `metab` | The currents backlog is the cardiovascular system. AMOC is a pulse in the North Atlantic. Until that pulse is a diagnosed streamfunction, the planetary mind is claiming an organ it does not have — which this document already called a fake vital sign. | Model | L | 3 |
| 468 | **Core sampler as a metabolic history** <br>needs `metab` | The core already reads banded iron, coal, till, ejecta. Those are breaths the planet took. Labelling the layers as metabolic events — GOE, burial pulse, snowball dust — turns a geology toy into the planet's medical record. | Show | M | 2 |
| 469 | **Assert fluxes, not stocks** <br>needs `metab` | A face that only shows amounts cannot show breathing. Assert that the displayed pulse is a derivative of carbon or NPP, and that it moves under a seasonal forcing on a world with obliquity. | Prove | M | 3 |
| 470 | **CLAW only if it earns a flux** <br>needs `metab` | Dimethyl sulphide, cloud condensation, cooling — a famous Gaia loop and an easy one to fake. Do not add a DMS slider. If marine NPP already modulates cloud, show that coupling; if it does not, say the hypothesis is off, because a sentient planet should not get free magic. | Prove | M | 2 |
| 471 | **Fire as a regulator, visible** <br>needs `metab` | Fire recycles nutrients, sets albedo and caps biomass. If the model grows vegetation without a loss term that looks like fire in the dry season, the metabolism is missing an organ animals have been watching for a million years. | Show | M | 2 |

## Homeostasis that is physics, not a checkbox

The Walker thermostat in `gaia.js` is a CO₂ leak of `0.0000004` from warm wet land, skipped entirely when `W.carbon` exists. `iceTick` floors land ice above 0.86 and sea ice above 0.88 so the caps cannot die. Daisyworld is the one place regulation is genuinely emergent, and it is a selectable planet rather than the tutorial it should be. A mind that cheats the physics it claims to inhabit is not a mind.

| # | Item | What and why | Kind | E | I |
|---|---|---|---|---|---|
| 472 | **The silicate weathering thermostat, properly** <br>gives `homeo` | Walker, Hays and Kasting: weathering rate rises with temperature and runoff, drawing down CO₂, on a 100 kyr–1 Myr timescale. `gaiaTick` has this as one line with a magic 0.0000004, skipped when `W.carbon` exists. Give it explicit T and runoff dependence and it becomes the negative feedback that keeps a planet habitable for four billion years — the nervous system, not a leak. | Model | M | 3 |
| 473 | **Do not floor the ice** <br>needs `homeo` | Caps that cannot die cannot runaway. Remove the 0.86 / 0.88 floors or gate them to a tutorial, and let snowball be a reachable, reversible, hysteretic state — which `W.state` already names and `iceTick` currently forbids. | Model | M | 3 |
| 474 | **Cloud feedback with a sign** <br>needs `homeo` | Low cloud cools, high cloud warms, and the net sign is the argument. A single translucent shell with no height cannot regulate. Typing cloud by regime is a rendering item and a homeostasis item at once; without the sign, seeding clouds is a coin flip. | Model | L | 3 |
| 475 | **Vegetation–albedo, Charney's loop** <br>needs `homeo` `metab` | Dry a savanna, raise albedo, dry it more. The Sahel is the textbook. Canopy already darkens land in the shader by guesswork (`greenDom`); driving albedo from cover fraction makes a desert a decision the biosphere can make and unmake. | Model | M | 3 |
| 476 | **Oxygen is not a painted constant** <br>needs `homeo` | Photosynthesis and burial already move O₂ in the chemistry. Regulation of pO₂ — fire plus burial plus respiration — is a real debate. A planetary mind that can inject CO₂ and cannot touch oxygen is missing the gas that made animals possible. | Model | M | 3 |
| 477 | **Two carbon timescales, both present** <br>needs `homeo` | Organic cycle fast, silicate cycle slow. A controller that only sees `gases.CO2` will use the wrong muscle. Expose both fluxes on the perception struct, or every "drew down CO₂" log line will be a lie about which reservoir moved. | Model | M | 3 |
| 478 | **Water vapour, the fast one** <br>needs `homeo` | The strongest greenhouse gas and the one the four ifs never mention. If H₂O is tracked, the moist-greenhouse path is a feedback the mind should fear; if it is not, `W.state === 'moist-greenhouse'` is a name without a mechanism. | Model | M | 3 |
| 479 | **Daisyworld remains the reference experiment** <br>needs `homeo` | N species, rising sun, no atmosphere, flat world. Demote it from the catalogue to the first five minutes, then compare every later controller against it: does this world regulate for a reason you can point at, the way the daisies do? | Play | M | 3 |
| 480 | **A world without life still has geology** <br>needs `homeo` | Weathering, ice-albedo, silicate outgassing. Assert that an abiotic twin still opposes a temperature pulse, weaker and slower. If it does not, Gaia is being asked to invent physics, which is Kirchner's objection made into a bug. | Prove | M | 3 |
| 481 | **Nitrogen fixation as a valve** <br>needs `homeo` | Life invented a way to unlock the air. That valve is a planetary control point with a date, a guild and an energy cost. Treat it as one, and the GOE-adjacent story gains a second act that is not oxygen. | Model | M | 2 |
| 482 | **Methane as a young planet's thermostat** <br>needs `homeo` | Archean warmth is a methane problem. `gases.CH4` exists and disequilibrium already multiplies it. A faint-young-sun world that cannot raise CH₄ cannot stay liquid, and a mind that only injects CO₂ is using the wrong era's tool. | Model | M | 2 |
| 483 | **Hysteresis is the lesson, not the bug** <br>needs `homeo` | Once a snowball locks, removing the cause must not undo it. The prototype reproduced this accidentally on Ares. Make it intentional, labelled on the face as a scar, and teach it in the tutorial after Daisyworld. | Model | M | 3 |
| 484 | **Gain measured by perturbation, in the suite** <br>needs `homeo` | `W.feedbackGain` is `weatheringFlux × 20 − rateStress`. Pulse solar by a stated amount, measure dT, report the ratio. That number replaces the fitted constant and it is what "the planet is regulating" means in a lab. | Prove | M | 3 |
| 485 | **Autopilot off is the honest test of Gaia** <br>needs `homeo` | The physics must oppose a perturbation without `W.autopilot`. The fictional mind is allowed to help. Mixing the two in a screenshot is how a trailer lies. Tag every capture with the flag. | Prove | S | 3 |
| 486 | **Fire, ice, cloud, rock: name the four loops** <br>needs `homeo` | A player should be able to list the negative feedbacks this world actually has. If the list is "the autopilot", the product has failed the SimEarth thesis. A Lab card of loops with on/off and gain is the teaching. | Show | M | 3 |
| 487 | **Assert a pulse is opposed** <br>needs `homeo` | Same seed, +CO₂, 200 ticks, controller off: temperature rise then partial return, ice loss then partial return, weathering up. If temperature steps and stays, there is no Gaia to be sentient with. | Prove | M | 3 |
| 488 | **Phosphate, the brake you cannot talk past** <br>needs `homeo` | A mind that wants more life and has no phosphate is a mind that has to wait for weathering. That wait is character. Do not let the controller spawn biomass from a slider. | Model | M | 2 |
| 489 | **Runaway terminal states with their own faces** <br>needs `homeo` `gaiaface` | Snowball and moist-greenhouse are named in `W.state`. They need looks, sounds and a silenced voice, or losing is a log line. SimEarth understood that losing should look like something. | Show | M | 3 |
| 490 | **The faint young sun, as a scenario** <br>needs `homeo` | Start under a dimmer star with a methane-rich air and ask whether the loops hold. It is the original Gaia problem and a better tutorial than four ifs on a modern Earth that cannot even advance the clock. | Play | M | 3 |
| 491 | **Do not skip Walker when carbon exists** <br>needs `homeo` | `if (!W.carbon) { weather += … }` means the better module turns the famous thermostat off. Either `W.carbon` implements Walker or the backup stays on. A silent `if` is how regulation vanished from the default path. | Model | S | 3 |

## Tipping elements as places

`TIPPING` lists iceSheet, amoc, amazon, boreal, permafrost, coral, monsoon. Six of seven are geography. `W._amoc` is written `W.conveyor ?? W._amoc ?? 0.7` and never diagnosed; `_monsoon` is a constant 0.5; amazon reads `_forestFrac` as forest-over-land for the whole planet; coral reads `meanTemp`. A list that cannot trip is a vital-sign lie, and a sentient planet whose organs cannot fail is a cartoon.

| # | Item | What and why | Kind | E | I |
|---|---|---|---|---|---|
| 492 | **Paint tipping state on the cells they name** <br>gives `tipgeo` <br>needs `gaiaface` | A pale Amazon, a still North Atlantic, a dark reef, a brown boreal, a bubbling permafrost coast, a monsoon that has not come. `W.tips[id] = { on, val, label }` is a boolean per name. Geography is the missing field. | Show | M | 3 |
| 493 | **AMOC as a colour of the North Atlantic** <br>needs `tipgeo` | When the conveyor is strong, Europe is green in winter relative to Labrador. When it is weak, the difference collapses. That is the photograph of a tipping element. It needs ocean heat transport, and it needs `_amoc` to be measured rather than `?? 0.7`. | Model | L | 3 |
| 494 | **Do not call it Gaia if the AMOC cannot die** <br>needs `tipgeo` | `W._amoc = W.conveyor ?? W._amoc ?? 0.7` is written every tick from a conveyor that may not exist. Either wire the currents backlog into this gauge or remove the tipping element. A fake vital sign is worse than none, and a mind that fears a ghost organ is a farce. | Model | S | 3 |
| 495 | **Amazon dieback as a browning you cannot miss** <br>needs `tipgeo` | `_forestFrac` is forest over land. The Amazon is a place. If tropical forest drops, that basin goes tan and the ITCZ should feel it. A global mean going from 0.4 to 0.3 is not a dieback and cannot be a drive. | Show | M | 3 |
| 496 | **Coral as a bleaching event with a recovery clock** <br>needs `tipgeo` | `refreshColours` already flashes reef white at `temp > 0.74`. Hold the white for presentation-weeks, then either green or bone. The tipping element `coral` reads `meanTemp`, which is the wrong geography. | Show | S | 2 |
| 497 | **Permafrost as a darkening and a bubble** <br>needs `tipgeo` | Thaw is thermokarst texture plus methane the redox tower can take. The list item exists; the ground does not change. A northern coast falling into lakes is the picture, and the methane is the drive. | Model | M | 2 |
| 498 | **A monsoon that reverses** <br>needs `tipgeo` | `_monsoon` is permanently 0.5. A monsoon is a seasonal reversal driven by land–ocean heat capacity. Half the human population lives under one. A planetary mind that cannot feel it is missing a cranial nerve. | Model | M | 3 |
| 499 | **Boreal as a marching treeline** <br>needs `tipgeo` | `_borealStress = clamp(meanTemp - 0.4, 0, 1)` is a global scalar. The picture is a treeline walking north, and the drive is the area of that walk, not a clamp on the mean. | Show | M | 2 |
| 500 | **Ice sheets as objects that can die** <br>needs `tipgeo` `homeo` | Greenland and West Antarctic analogues with a mass and a grounding line, not `iceFrac` against 0.55. `TIPPING[0]` is `iceSheet` reading `iceFrac`. A mind cannot mourn a mean. | Model | L | 3 |
| 501 | **Hysteresis as a scar on the map** <br>needs `tipgeo` | Once the Amazon browns, it stays brown past the original threshold. A recovery slower than the collapse is the lesson; a slider snapping back is not. Paint the scar until the hysteresis gap is actually recrossed. | Show | M | 3 |
| 502 | **A map of proximity, not a list of trips** <br>needs `tipgeo` | Distance-to-threshold per organ, in the overlay table that currently has seventy-two entries and zero behaviour. This is the fear drive made visible, and it is the first overlay that would show what the planet is *about to* do. | Show | M | 3 |
| 503 | **Named places in the chronicle when an organ fails** <br>needs `tipgeo` | `Tipping: Polar ice sheets` is a system name. `The northern cap has gone` is a place. The chronicle already has a cell argument; use it. | Show | S | 3 |
| 504 | **A god verb that trips one organ on purpose** <br>needs `tipgeo` | `tripOceanConveyor` exists. Give each organ a surgical verb with a receipt, so the player can examine hysteresis the way a physiologist examines a reflex. The mind should treat those verbs as injuries. | Play | M | 3 |
| 505 | **Cascades, because organs share blood** <br>needs `tipgeo` | Ice melt freshens the conveyor; a dead conveyor dries the monsoon; a dead monsoon browns a forest. If tips are independent booleans, the interesting failure of a planetary body cannot happen. A stated coupling table, small, reviewed, compiled. | Model | L | 3 |
| 506 | **Do not list what cannot trip** <br>needs `tipgeo` | A UI row for monsoon at 0.5 forever trains the player to ignore the list. Hide or mark `n/a` until the field is live. The same honesty as provenance tags. | Show | S | 3 |
| 507 | **Geography from the actual fields** <br>needs `tipgeo` | Tropical forest from `life` + `moist` + latitude, not a global `_forestFrac`. Reef from `reef[c]`. Ice sheet from polar `iceLand`. The diagnosis should be a reduction of fields that already exist, or the organ is a fiction. | Model | M | 3 |
| 508 | **Europa does not have an Amazon** <br>needs `tipgeo` `alienmind` | Per-world tip tables. Ice-shell worlds have different organs. Pasting Lenton's Earth list onto a catalogue body is the lava-world category error in miniature. | Model | M | 3 |
| 509 | **Assert AMOC can trip once it is real** <br>needs `tipgeo` | After the currents work lands, a freshwater pulse must be able to flip `_amoc` through the threshold and hold it past hysteresis. Until then, assert the element is disabled. A test that a constant 0.7 cannot fail is not a test. | Prove | M | 3 |
| 510 | **A closed demo of one organ** <br>needs `tipgeo` | Script: trip coral, watch the flash, wait the recovery clock, photograph. One organ, one film, no HUD. If that film is not readable, the face is not ready. | Play | S | 3 |
| 511 | **The mind attends the nearest organ** <br>needs `tipgeo` `pdrive` | Attention drive from proximity. Camera offer, chronicle line, overlay pulse. `shouldHaltFF` stops for anomalies; stopping for a named organ is how a body would interrupt you. | Play | M | 3 |

## The planet fights back

`god/disaster.js` already has `releasePathogen` and `pathogenTick`. `god/sculpt.js` has `resistTick`. Impacts write craters. None of this is an immune system: a wound with a recovery clock, a fever that is a greenhouse overshoot, antibodies that are weathering and ice and storms answering a hand, scars that are hysteresis. The player is either a symbiont or a pathogen. The planet has to be able to tell the difference, and to lose.

| # | Item | What and why | Kind | E | I |
|---|---|---|---|---|---|
| 512 | **The player is a symbiont or a pathogen** <br>gives `immune` <br>needs `planetsee` `pdrive` | `playStyle` already classifies vandal versus gardener. Map that onto an immune stance: tolerate, inflame, wall off, attack. A planet that treats every hand as weather cannot be in a relationship, and sentience without a relationship is a screensaver. | Play | M | 3 |
| 513 | **Impact as a wound with a clock** <br>needs `immune` | `strikeImpact` already writes a crater and a climate pulse. Add a recovery clock and a scar: ejecta that weathers, a dust fever that breaks, a chronicle that dates the injury. A mind that does not notice Theia is not home. | Model | M | 3 |
| 514 | **Fever is a greenhouse overshoot** <br>needs `immune` | Temperature above target is fever if it is a response, heat if it is a forcing. Label the difference from whether the controller or the player raised it. A body that cannot tell those apart will treat summer as disease. | Model | M | 2 |
| 515 | **Antibodies are weathering, ice and storms** <br>needs `immune` `homeo` | The loops that oppose a perturbation are the immune system. Show them answering a brush stroke the way a wound weeps, then closes. If the only answer is `gaiaPolicyTick` moving solar, the antibodies are a cheat. | Show | M | 3 |
| 516 | **`pathogenTick` is a start** <br>needs `immune` | `releasePathogen` and `pathogenTick` exist in `disaster.js`. Wire them to host density and to the planetary pain drive, and a plague is a fever the mind can fight rather than a global `W.plague` multiplier on `life[c]`. | Model | M | 3 |
| 517 | **An invasive clade as an infection** <br>needs `immune` | `transplantClade(..., invasive = true)` exists. The planet should raise crowding, predation pressure and a chronicle of the front. That is immunity as ecology, which is the honest version, not a white-blood-cell sprite. | Model | M | 2 |
| 518 | **Nuclear winter as immune overreaction** <br>needs `immune` | Aerosol injection that saves nothing and starves everything. The Medea stance in miniature. If the controller can choose that verb, it needs a cost and a regret in the log, or it is a slapstick god. | Play | M | 2 |
| 519 | **Autoimmune is Medea** <br>needs `immune` | `medeaScore` already estimates biosphere self-harm. Oxygen catastrophe, anoxia, a planet that poisons its own air. Make it a stance the controller can enter, labelled, reversible only through hysteresis, never as a joke setting without physics. | Model | M | 3 |
| 520 | **Scar tissue is hysteresis** <br>needs `immune` `tipgeo` | A recovered Amazon that is not the old Amazon. A conveyor that stays weak. The immune log should keep scars as first-class as wounds, because that is how a body remembers. | Show | M | 2 |
| 521 | **`resistTick` as local flinch** <br>needs `immune` | `god/sculpt.js` already resists the brush. Use it: the ground that pushes back, the life that dies under the finger then creeps in from the edge. The planetary immune system at the scale of a hand. | Play | S | 3 |
| 522 | **Inflammation is rate stress, drawn** <br>needs `immune` `metab` | A hot rim around a wound cell, a restless cloud deck, a flicker on the face. `rateStress` is the number; the picture is a planet that cannot settle. Do not add a new field for redness. | Show | S | 2 |
| 523 | **Wall off a basin** <br>needs `immune` | A controller that seeds a desert belt or a cold current to quarantine a blight is using geography as skin. Expensive, visible, and a use of verbs that is not "raise solar". | Play | M | 2 |
| 524 | **A small perturbation that trains** <br>needs `immune` `lifelearn` | Vaccination as metaphor: a mild forcing that leaves the loops tighter. If feedback gain rises after a survived pulse, say so. If it does not, do not claim the planet learned. | Model | L | 1 |
| 525 | **Chronic illness is slow CO₂** <br>needs `immune` | A civilisation that leaks carbon is a low-grade fever for megayears. The mind should not treat it like an impact. Timescale matching is the whole of diagnosis. | Model | M | 3 |
| 526 | **Surgery versus infection, in the receipt** <br>needs `immune` | Player tools tagged as wound, medicine or food. `issueReceipt` already stores intent. The immune stance reads the tag. Without it every miracle is an invasion. | Model | S | 3 |
| 527 | **A planet that refuses to heal** <br>needs `immune` | Medea stance, or a wound past a stated mass. The gardener has to change strategy. A body that always recovers teaches the wrong biology and the wrong game. | Play | M | 3 |
| 528 | **Assert a wound closes** <br>needs `immune` | Crater plus 1,000 ticks: dust down, temperature back, scar remains. Three assertions. If dust never falls the fever never breaks. | Prove | M | 3 |
| 529 | **Assert a large wound kills** <br>needs `immune` | Theia-scale or moist-greenhouse: voice off, mind gone, `gaiaMode = 'gone'`. The controller must not keep raising solar on a magma ocean. | Prove | S | 3 |
| 530 | **The fight in view, not in the log** <br>needs `immune` `gaiaface` | Raise CO₂ and watch ice, cloud and green answer over presentation-minutes. If the answer is only a receipt, Gaia is a log line. The fight is the game. | Play | M | 3 |
| 531 | **Immune log next to the gaia log** <br>needs `immune` | Wounds, antibodies, scars, stance changes. `W.gaiaLog` caps at 60. Do not mix "raised solar" with "impact at cell 412". Two streams, one dashboard. | Show | S | 2 |

## The planet notices the hand

`notice.js` is civilisation: awareness from `interventionLog`, worship styles, a prayer queue, eight named people. The biosphere does not notice you at all. `playStyle` and `restraintStats` in `receipt.js` already classify the god; `forecastAct` already predicts a tool. Those numbers are sitting on the table and Gaia never picks them up. A sentient planet that cannot tell a brush stroke from weather is not looking.

| # | Item | What and why | Kind | E | I |
|---|---|---|---|---|---|
| 532 | **Notice is civilisation; the planet needs its own** <br>gives `planetsee` <br>needs `pdrive` | `noticeTick` returns early unless `meanBuild` is high. A Precambrian world cannot notice you, which is correct for a civ and wrong for a biosphere. A second awareness — `W.gaia.awareness` — from climate anomalies correlated with `interventionLog`, no cities required. | Model | M | 3 |
| 533 | **The intervention log is the sensory stream** <br>needs `planetsee` | `W.interventionLog` already stores tool, time, cell. That is what the planet can know about the god. Do not invent omniscience. A brush the log missed did not happen, the way a being does not see past `sensory.js`. | Model | S | 3 |
| 534 | **`playStyle` is how it knows who you are** <br>needs `planetsee` | Vandal, gardener, scientist — already computed. Feed it to the immune stance and the voice. Recalculate it on a decay so an old vandal can become a gardener, which is the moral layer `notice.js` was built for, pointed at the sphere. | Model | S | 3 |
| 535 | **Restraint is a signal** <br>needs `planetsee` | `restraintStats` exists. A planet that notices you stopped is more interesting than one that only notices you struck. Silence after a wound is information. | Play | M | 3 |
| 536 | **Learn the player's style, bounded** <br>needs `planetsee` `lifelearn` | A small decaying estimate: this god uses fire, this god seeds, this god watches. Not a language model. A handful of weights on tool ids, the same learning this backlog already asked for in animals. | Model | M | 2 |
| 537 | **Forgetting after idle** <br>needs `planetsee` | `next.mjs` already asked that civ awareness decay. Gaia's should too, slower. Being an ex-god to a planet is stranger than being an ex-god to a city, and it is the same exponential. | Play | S | 2 |
| 538 | **Worship versus immune — two responses** <br>needs `planetsee` `immune` | Civ worships; the biosphere inflames. Do not reuse `civ.worship` for both. A scientific culture and a furious forest can coexist, which is the late-game argument. | Model | M | 3 |
| 539 | **The planetary analog of a prayer is a demand** <br>needs `planetsee` `gaiavoice` | `civ.prayers` asks for rain. A biosphere demand is a drive that has been loud too long: *the reef is going, the conveyor is going*. Same queue shape, different nouns, never a cartoon speech balloon unless the fiction switch is on. | Show | M | 3 |
| 540 | **A planet that hides** <br>needs `planetsee` | Cloud up, night side, dust — reducing what the player can see after a vandal run. Cheap, visible, a little rude. `seedClouds` is the verb. | Play | M | 1 |
| 541 | **A planet that performs for the camera** <br>needs `planetsee` | The opposite: phenology and aurora leaning into the view when the player is gentle. Dangerous if it breaks realism; gated on the fiction switch; delightful if it is a 2% albedo wink rather than a dance. | Play | S | 1 |
| 542 | **Correlation detection, the same as civ** <br>needs `planetsee` | `noticeTick` already correlates recent interventions with awareness. Copy the structure, change the threshold, drop the `meanBuild` gate. Do not invent a new statistics module. | Model | S | 3 |
| 543 | **Receipts as memory of the god** <br>needs `planetsee` | `W.receipts` is the medical chart. The mind reads it the way a being reads its carcass memory. Forecast versus outcome is how it learns you lie, which `forecastAct` already sets up. | Model | M | 2 |
| 544 | **Being forgotten** <br>needs `planetsee` | After enough quiet, myths garble for the civ and the planetary voice goes back to weather. The product already wanted this ending. Implement it on both layers so they can disagree about whether you were real. | Play | M | 2 |
| 545 | **Defiance as a Gaian stance** <br>needs `planetsee` | `civ.worship` includes `defiant`. The biosphere version is a controller that spends budget to undo the player — re-icing a melt, drawing down a CO₂ gift. A game mode, a tutorial in feedback, and a way to lose as a god. | Play | M | 3 |
| 546 | **Scientific civ as a rival mind** <br>needs `planetsee` `bandtociv` | They build the machines from the tool palette. Late game you negotiate with the planet *and* with them. `civ.techAge === 'industrial'` is currently a coal-sum heuristic; it has to become an actor. | Play | L | 3 |
| 547 | **The hand through the sky, locally** <br>needs `planetsee` | `living.mjs` already asked for the valley that flinches. Gaia's version is local NPP drop, startle in beings, a cloud. The global mind notices because the local field screamed, not because a UI flag flipped. | Play | M | 3 |
| 548 | **VR: it warms where you hold it** <br>needs `planetsee` | A palm on the night side, a little heat, a little life answering, then settling. Presence as a forcing in `interventionLog`. If the headset cannot do this, the embodiment pitch is a trailer. | Play | M | 3 |
| 549 | **Assert awareness tracks interventions** <br>needs `planetsee` | No acts: awareness stays near zero. Twenty climate tools: it rises. A thousand years of quiet: it falls. Three numbers, same seed. If they do not move, the planet is not looking. | Prove | S | 3 |
| 550 | **Assert a dead world does not notice** <br>needs `planetsee` `alienmind` | Airless, magma, sterilised: `gaia.awareness` stays 0 and no demand is queued. The fiction switch must not wake a rock. | Prove | S | 3 |
| 551 | **Do not let the civ speak for the forest** <br>needs `planetsee` | Prayer text and Gaian demand in the same HUD without labels is how players think the trees asked for a sign. Two channels, two typefaces, or one muted when the other talks. | Show | S | 3 |

## A mood that is a readout, not a story

Pain is `rateStress`. Hunger is whatever nutrient or carbon is limiting. Fear is tipping proximity. Ease is high resilience with low gain. None of those words should be invented — they are projections of numbers `gaiaTick` already computes. The voice, the analog face, the soundtrack and the haptic in VR all hang off one labelled vector, and a dead world has none of it.

| # | Item | What and why | Kind | E | I |
|---|---|---|---|---|---|
| 552 | **Mood is a projection of the state vector** <br>gives `planetmood` <br>needs `pdrive` `gaiavoice` | A small named vector — ease, pain, hunger, fear, fever — each a function of numbers `gaiaTick` already computes. No extra simulation. The voice, the face, the sound and the haptics all read this one struct, so they cannot disagree by accident. | Model | M | 3 |
| 553 | **Voice only from numbers** <br>needs `planetmood` | Templates over real fields, never invented suffering. `author` already demanded this for the chronicle. A planet that claims to hurt when `rateStress` is 0 is a liar, and liars end this product's credibility. | Show | S | 3 |
| 554 | **Dreams are forks you can watch** <br>needs `planetmood` | `forkRun` exists. A dream is a cheap lookahead the fiction mode is allowed to show as a ghost disc: *this is the snowball I can still become*. Turn the fiction switch off and the ghost is a forecast overlay instead. Same machinery. | Show | L | 2 |
| 555 | **Sleep is a snowball, or a desert** <br>needs `planetmood` | Low inhabitance, low disequilibrium: the voice slows, the face stills, drives decay toward zero. Not death — `gaiaMode` stays, metabolism ticks. A hibernating planet is a better image than a chatty rock. | Show | S | 2 |
| 556 | **Season as a mood swing** <br>needs `planetmood` | The green wave is already a year. Map it onto ease in the growing hemisphere and hunger in the dying one. A planet that does not feel the year is a planet whose metabolism this backlog already called a still life. | Show | M | 2 |
| 557 | **Affect that changes policy weights** <br>needs `planetmood` `pdrive` | High fear: spend budget on the nearest tip. High hunger: spend on the limiting nutrient. Mood that does not change action is a skin. Keep the change small and asserted, or the planet will look moody and act like the four ifs. | Model | M | 3 |
| 558 | **A face you can misread** <br>needs `planetmood` `gaiaface` | Green monoculture looks well; brown healthy desert looks sick. Overlay NPP versus life versus diversity and let them disagree. SimEarth hid that disagreement behind one Health number. | Play | M | 2 |
| 559 | **Music from the mood, not from a loop** <br>needs `planetmood` | `audio.js` is 111 lines. A drone that tracks ease/pain is a lot of atmosphere per oscillator. Gate it, keep it quiet, never a melody that implies a composer unless the fiction switch is on. | Show | M | 2 |
| 560 | **Chronicle tone from mood** <br>needs `planetmood` | The same event, two wordings, chosen by the vector: a trip during ease reads as a surprise, during fear as a confirmation. Templates, not a model. The numbers still print. | Show | S | 2 |
| 561 | **Do not invent suffering** <br>needs `planetmood` | A glossary line and a lint: mood fields are display names for measured quantities. If a writer wants the planet to scream, they raise `rateStress` in the sim, they do not type the scream. | Prove | S | 3 |
| 562 | **Calm after a tip recovers** <br>needs `planetmood` `tipgeo` | Ease should lag recovery the way pain lagged the trip — hysteresis in the readout. A needle that snaps back teaches the wrong lesson about bodies. | Show | S | 2 |
| 563 | **Mania is a hothouse** <br>needs `planetmood` | High temp, high NPP, high rateStress: a flushed face, a fast voice, a controller that overspends. Label it as a state, not as a personality, and it becomes a warning. | Show | S | 2 |
| 564 | **Depression is a snowball** <br>needs `planetmood` | The opposite: slow voice, white disc, drives near zero, verbs expensive because nothing grows. If the fiction switch is off, this is just `W.state === 'snowball'` with a quieter HUD. | Show | S | 2 |
| 565 | **Anxiety is high rate stress with tips close** <br>needs `planetmood` | The interesting case: not sick yet, about to be. That is the moment SimEarth's Health needle earned its keep. Put it on the analog face as a shimmer, not as a chat line every frame. | Show | M | 3 |
| 566 | **One sentence on the HUD** <br>needs `planetmood` `censusline` | The running account this backlog already asked for, sourced from mood + attention. One sentence, one at a time, templates over numbers. The planet talking is this sentence with the fiction switch on; with it off, it is a status line. | Show | M | 3 |
| 567 | **VR haptics from mood** <br>needs `planetmood` | A low rumble when pain is high, a slow pulse of ease. Easy to overdo; cap it. Presence is the product, and a planet that has no tactile tell is a picture in a headset. | Play | M | 2 |
| 568 | **Assert mood tracks state** <br>needs `planetmood` | Pulse temperature: pain up then down. Trip a tip: fear up. Empty the biosphere: hunger up, then sleep. If mood is authored independently, delete it. | Prove | M | 3 |
| 569 | **A dead world has no mood** <br>needs `planetmood` `alienmind` | Lava, airless, sterilised: struct is null, HUD says n/a, no sentence. Assert it, or Mercury will feel sad and the whole claim collapses. | Prove | S | 3 |
| 570 | **The planet that will not speak** <br>needs `planetmood` | Fiction switch off, or Daisyworld, or a world below the integration threshold. Silence is the default. Speech is opt-in. That is the only way a talking Earth is special. | Play | S | 3 |
| 571 | **A shared mood struct, one writer** <br>needs `planetmood` | Voice, face, sound and haptics must not each recompute ease and pain. One writer in `gaiaTick` or the policy tick, four readers. If they diverge, the planet looks possessed rather than alive. | Model | S | 3 |

## A mind that is not Earth's

`gaiaTick` runs on every world. A Mercury with a resilience score is a category error; a lava ocean with seven Earth tipping elements is a joke. Titan's mind if any is a haze; Europa's is a chemical gradient under ice; Venus is a failed regulation with a face; Mars is the archaeology of one. The 120-world catalogue is the experiment that decides how often regulation happens, which is Chopra and Lineweaver's bottleneck made playable.

| # | Item | What and why | Kind | E | I |
|---|---|---|---|---|---|
| 572 | **Gate Gaia on air, water and a biosphere** <br>gives `alienmind` | `gaiaTick` runs everywhere. A Mercury with resilience is a category error. Gate the face, the tip list, the daisy mix and the mind on a world that has air, water and life, or print `n/a`. One predicate, used by every layer in this pass. | Model | S | 3 |
| 573 | **Titan's mind if any is a haze** <br>needs `alienmind` `metab` | Photochemical haze, anti-greenhouse, lakes of methane. If the fiction switch is on, the voice talks about veils and rain that is not water. If it is off, show the metabolism without a pronoun. Either way do not paste forests. | Model | M | 2 |
| 574 | **Europa: a mind with no photons** <br>needs `alienmind` | `sensory.js` already ranks electroreception and flow first under the ice. A planetary analog is chemical disequilibrium at the ice–ocean interface, not a green disc. The face is a shell with a warm seam, or it is a lie. | Show | M | 3 |
| 575 | **Venus is a failed regulation with a face** <br>needs `alienmind` | Moist-greenhouse as character, not as a tint. The lesson of a world that lost the loops. Catalogue card: *mind gone*, photograph of a white-hot disc, no Health needle. | Show | M | 3 |
| 576 | **Mars is the archaeology of a mind** <br>needs `alienmind` | Valley networks, leftover ice, a thin CO₂ air. The core sampler as a medical record of a patient who left. Do not give it a voice. Give it scars. | Show | M | 2 |
| 577 | **Tidally locked: two hemispheres, two moods** <br>needs `alienmind` `planetmood` | A dayside fever and a nightside sleep, an eyeball climate as a split face. If mood is one global vector this world is unsayable. Allow a bimodal readout or admit the architecture is Earth-shaped. | Model | M | 3 |
| 578 | **Super-Earths, a thicker blood** <br>needs `alienmind` | Gravity sets the relief ceiling and the air column. A mind on a 10-bar world has different muscles — more atmosphere to move, slower loss to space. Do not reuse Earth's verb costs. | Model | M | 2 |
| 579 | **Ice giants have no Gaia** <br>needs `alienmind` | No surface, no Walker, no forest. `n/a` on the face. If the product wants a weather-mind for a giant, that is a different labelled toy, not this pass wearing a deep-atmosphere hat. | Model | S | 3 |
| 580 | **Daisyworld is the tutorial mind, not a catalogue peer** <br>needs `alienmind` | Demote it. Keep it as the first five minutes. Do not let it sit beside TRAPPIST-1 e as if it were a place. The sentience claim depends on players knowing which world is a proof. | Play | M | 3 |
| 581 | **The catalogue is the bottleneck experiment** <br>needs `alienmind` | How many of 120 worlds ever regulate with autopilot off. Print it. Chopra and Lineweaver become a number this build can defend, and the sentience mode is rarer than the marketing instinct wants. | Prove | M | 3 |
| 582 | **M-dwarf flares as chronic pain** <br>needs `alienmind` `immune` | `stellarFlare` exists. A world under a flaring star has a pain drive that never quite goes to zero. That is a temperament with a physical cause, which is this product at its best. | Model | M | 2 |
| 583 | **Anoxic minds, purple and sulphur** <br>needs `alienmind` | The life pass already colours by guild. A world that regulates without O₂ should look like it and should use CH₄ and H₂S as its disequilibrium, not a fake oxygen needle. | Show | M | 3 |
| 584 | **Chemosynthetic Gaia at a vent chain** <br>needs `alienmind` | No photon loop. The metabolism is rock and water. Gate the mind on hydrothermal power, not on insolation, or Europa inherits an Earth hunger for light it cannot feel. | Model | M | 2 |
| 585 | **Techno as a second cortex, late** <br>needs `alienmind` `bandtociv` | `technoTick` hangs a technosphere off `unlockedClass`. A planetary mind that has to share the air with industry is the Anthropocene as a game, and it needs the rival-mind item, not a night-lights overlay pretending to be a thought. | Model | L | 2 |
| 586 | **Do not paste Earth's tipping list** <br>needs `alienmind` `tipgeo` | Per-world organs, compiled from climate regime. A desert world has no monsoon row. A table that is always Lenton's seven is a poster, not a physiology. | Model | M | 3 |
| 587 | **Bond albedo worlds, a clothing of ice and haze** <br>needs `alienmind` `metab` | Some catalogue bodies are their albedo. Let clothing be the whole metabolism where carbon is a trace, and do not insist on a Keeling pulse that cannot exist. | Show | M | 2 |
| 588 | **Assert lava worlds say n/a** <br>needs `alienmind` | Generate a magma-ocean ruleset, tick Gaia, assert face, tips, mood and voice are disabled. One test, catalogue-wide via the kind table, so the 40-Io class of bug cannot happen to minds. | Prove | S | 3 |
| 589 | **A contact sheet of planetary moods** <br>needs `alienmind` `planetmood` | One thumbnail per catalogue body, face on if gated in, `n/a` if not. The realism pass asked for a contact sheet of looks; this is the same object with a pulse. You cannot art-direct 120 minds without seeing them together. | Show | M | 2 |
| 590 | **High-O2 Carboniferous as a manic Earth** <br>needs `alienmind` `planetmood` | Giant arthropods, bright fire, a flushed face. A temperament from a gas, which Kleiber and body scale already know about. Do not author it as a personality pack. | Show | S | 2 |
| 591 | **The silent majority of the shelf** <br>needs `alienmind` | `rankByBiosignature` already sorts the shelf on `disequilibrium`. Most entries should be quiet. A talking shelf is a toy box. Keep the sort, add a column for `gaiaMode`, and let silence win. | Play | S | 2 |

## The face of the planet

SimEarth's Gaia window was three analog gauges. Ours is a HUD chip that says `Gaia on` or `off`, a Lab line with `feedbackGain` and `medeaScore`, and an orb colour from `W.health`. A green planet can be a monoculture; a brown one can be a healthy desert. The face has to be the metabolism, the tips as places, and a fight you watch — or Gaia is a setting.

| # | Item | What and why | Kind | E | I |
|---|---|---|---|---|---|
| 592 | **Show the metabolism, not a resilience number** <br>gives `gaiaface` | `W.resilience` is a blend of diversity, meanLife, temperature error and rate stress. Earth as a face is fluxes. Draw the budget on the planet — greening, browning, freezing — or Gaia is a HUD. This is the picture every other sentience item hangs on. | Play | M | 3 |
| 593 | **Three analog gauges SimEarth would recognise** <br>needs `gaiaface` | Temperature, biomass, diversity — big, analogue, labelled with the range the model can actually occupy. `habitability` versus `inhabitance` is a good split and currently a pair of similar scalars. Add a fourth needle only if it is disequilibrium. | Play | S | 3 |
| 594 | **Tipping elements as places on the disc** <br>needs `gaiaface` `tipgeo` | The list in the Lab is a spreadsheet. The face is a globe with organs lit. If the player has to open a panel to know the Amazon is dying, the planet does not have a face. | Show | M | 3 |
| 595 | **A god-act the planet fights in view** <br>needs `gaiaface` `immune` | Raise CO₂, watch ice, cloud and green answer. The fight is the game. Script it as the first thing a new player sees after Daisyworld. | Play | M | 3 |
| 596 | **Overlay of intent** <br>needs `gaiaface` `pdrive` | What the controller means to do next, as a faint mark: a planned cloud deck, a planned ice line, a planned seed. Cancelled if the fiction switch is off and replaced with *forecast* wording. Same marks. | Show | M | 2 |
| 597 | **Before and after discs** <br>needs `gaiaface` | The closed demo as a UI object: two small planets, same seed, controller on and off, or player on and off. `forkRun` is the capture. This is also a test harness. | Play | M | 3 |
| 598 | **Health that can lie, taught** <br>needs `gaiaface` `planetmood` | A green planet can be a monoculture. Teach the misread with a toggle: NPP, life, diversity. If they agree, the face is calm; if they disagree, the needles quarrel. That quarrel is the content. | Play | M | 2 |
| 599 | **Daisy bands doing the work Watson said they would** <br>needs `gaiaface` | `uDaisy` mixes toward luminance. Show the black belt and the white belt moving as the sun brightens, in the tutorial, full screen, no HUD. That picture is the thesis. Everything later is a variation on it. | Show | M | 3 |
| 600 | **Autopilot on as a visible stance** <br>needs `gaiaface` | The chip that says `Gaia on` is a boolean. A stance is a posture: ice leaning in, a cloud deck forming, a colour temperature shift. If you cannot tell the controller is working without the chip, the face is a badge. | Show | S | 3 |
| 601 | **Lab page for Gaia, one card** <br>needs `gaiaface` | Resilience, gain, rate stress, tipping proximity, biosphere trend, mood, what it intends, what it last did, whether the fiction switch is on. Numbers that already exist, in one place, instead of a chip and a Lab-meta line. | Show | M | 3 |
| 602 | **Analog needles with the range the model occupies** <br>needs `gaiaface` | If temperature only ever lives in 0.4–0.7, a 0–1 needle is a lie about precision. Label the occupied range from a catalogue sweep, the way Kleiber got a real exponent. | Prove | S | 2 |
| 603 | **Do not HUD it only** <br>needs `gaiaface` | If the disc does not change, the needles are a game about needles. Rule: every needle has a disc correspondent, or the needle is cut. | Prove | S | 3 |
| 604 | **Mobile: one face, three needles, one sentence** <br>needs `gaiaface` `planetmood` | The product has a mobile HUD. Sentience that needs a Lab page will not exist on a phone. Compact the card without dropping the metabolism picture. | Show | M | 2 |
| 605 | **Night-side life as a tell** <br>needs `gaiaface` | `uNight` currently adds a flat green term scaled by `meanLife`. Bioluminescence and city lights are different stories. A living ocean at night is the face with the sun put away. | Show | S | 2 |
| 606 | **Limb as a history of oxygen** <br>needs `gaiaface` `metab` | Pale then blue. The ring is a needle that does not look like a needle. Deep-time runs should be identifiable from the limb alone. | Show | M | 3 |
| 607 | **A screenshot test of a living face** <br>needs `gaiaface` `livemetric` | The thrive probe already demanded one image of life growing. Add a second: a planet answering a pulse, ice and green moving, no HUD. If capture-site cannot take that frame, the face is not shippable. | Prove | M | 3 |
| 608 | **Assert the face matches the numbers** <br>needs `gaiaface` | When `iceFrac` is high the disc is white. When `inhabitance` is high the disc is not a desert. When `gaiaMode` is gone the needles are dead. Pixel tests can wait; field-to-colour assertions cannot. | Prove | M | 3 |
| 609 | **The first ten seconds teach the pairing** <br>needs `gaiaface` | Load: Daisyworld or a living Earth, a pulse, the face answering, one sentence. The living backlog asked for this for the 2D/3D window. Gaia needs the same authored moment or nobody will know the planet is the other character. | Play | S | 3 |
| 610 | **A god-mode that is looking at a someone** <br>needs `gaiaface` `planetsee` | When awareness is high and the fiction switch is on, the camera treats the disc as a face — not a zoom, a regard. Easy to make silly. A 2% bias toward the attention organ is enough. | Play | S | 1 |
| 611 | **Silence as a look** <br>needs `gaiaface` `alienmind` | `n/a` is a dim disc, no needles, no sentence, no chip. It has to look like a mineral, not like a UI error. Most of the catalogue should look like this. | Show | S | 3 |

## Where a language model actually belongs

Not in the tick. At authoring time: proposing guild tables, naming clades and settlements, writing the chronicle sentence, generating behaviour trees that get **compiled to frozen data** and reviewed, exactly as `lifegrammar.mjs` compiles authored JSON into `lifeGrammar.js`. The pattern this repo already trusts is author-then-compile-then-validate, and that is the pattern that makes a generative model safe to depend on.

| # | Item | What and why | Kind | E | I |
|---|---|---|---|---|---|
| 332 | **Author-then-compile is the pattern this repo already trusts** <br>gives `authorai` | `lifegrammar.mjs` validates authored JSON and compiles it into a frozen `lifeGrammar.js`, and the validator caught a real error on its first run. Any generative model in this product belongs at that step: it proposes rows, a human reviews them, the compiler validates them, and the runtime only ever sees frozen data. | Model | M | 3 |
| 333 | **Generate the guild table, then check it** <br>needs `authorai` `guildfield` | Forty guilds with feeding modes, size ranges, habitats and interaction coefficients is exactly the kind of table that is tedious to author and easy to validate. Generate a draft, validate against physical bounds, review, commit the JSON — and the model is not in the tick. | Model | M | 3 |
| 334 | **Behaviour trees as compiled data** <br>needs `authorai` `actiontable` | A tree is a small structure over a fixed action vocabulary, so it can be generated, validated against the vocabulary, diffed and reviewed. That gives the variety a generative model is good at without giving up determinism, which is the whole trade. | Model | M | 3 |
| 335 | **Names that a model wrote and a human kept** | `nameFrom` uses two syllable tables and `cladeName` builds a pronounceable name from traits. A generated name bank per world — settlements, lineages, landforms, regions — compiled to data, gives a hundred and twenty worlds their own vocabulary at no runtime cost. | Show | M | 2 |
| 336 | **The chronicle sentence, from a template over real numbers** <br>needs `gaiavoice` | `logEvent` writes fixed strings. Richer sentences must be assembled from measured quantities by templates, never invented, because a chronicle that says something the simulation did not do is worse than a terse one. This is the discipline that makes generated prose safe here. | Show | M | 3 |
| 337 | **A scenario author** <br>needs `authorai` | `god/scenario.js` exists. Scenarios are a small structured object — initial conditions, a forcing, a goal, a scoring rule — and generating a hundred of them, validated and reviewed, is a large content win from a small mechanism. | Play | M | 2 |
| 338 | **Species descriptions, bounded by the genome** <br>needs `authorai` | A paragraph about a creature that reads only from its expressed plan, its habitat and its diet. Every claim traceable to a field, nothing added. That constraint is what separates a description from a fabrication. | Show | M | 2 |
| 339 | **Provenance for anything a model wrote** | `substrates.json` and `cover.json` carry 35 provenance tags between them and it is the best discipline in the data layer. A generated row needs a fourth tag beyond measured, inferred and invented: *generated, reviewed by*, with the date. | Prove | S | 3 |
| 340 | **A review gate that cannot be skipped** <br>needs `authorai` | No generated row reaches `vr/` without a human-reviewed commit. Make it structural — generated output lands in a staging directory that the compiler refuses to read until it is moved — because the whole safety argument depends on the gate being real. | Prove | S | 3 |
| 341 | **Validate against physics, not against taste** <br>needs `authorai` | A generated guild with a transfer efficiency above one, a body mass outside the grammar's range or a diet that violates chirality has to fail the compiler. The validator is what makes generated content trustworthy, and it exists already for the life grammar. | Prove | M | 3 |
| 342 | **Do not let a model invent a mechanism** | The failure mode is generated content that implies physics the simulation does not have — a creature described as photosynthetic on a world with no usable photons. The generator must be given the same tables the simulation reads, and its output must round-trip through them. | Prove | M | 3 |
| 343 | **A diffable format for generated data** | Stable key order, one row per line, stable number formatting. The world-space pass made this point about a 27 KB generated JSON and it applies harder to generated content, because the review is the only thing standing between a draft and the product. | Prove | S | 2 |
| 344 | **Version what generated it** <br>needs `authorai` | `GRAMMAR_VERSION` exists for the life grammar. A generated table needs the same, plus which prompt and which model produced it, so a regression can be traced to a batch rather than to a row. | Prove | S | 2 |
| 345 | **Keep the hand-authored rows as the fixture** <br>needs `authorai` | The pattern the life and open-world passes both used: the hand-written version becomes the expected output of the generated path. It is how you find out the generator drifted. | Prove | M | 3 |
| 346 | **A budget for how much of this is generated** | A product where most of the content is generated and none of it is reviewed is a different product. State the fraction, count it the way `dataratio` counts data against code, and let the number be visible. | Prove | S | 2 |
| 347 | **The player as author, with the same gate** | The strongest argument that behaviour is data is a player editing it. A behaviour editor in the Lab that writes the same JSON the compiler validates makes the data layer visible and makes the gate symmetric. | Play | L | 2 |
| 348 | **Share a creature, share a behaviour** | `seedword.js` encodes a world in four words and the life pass asked for a shareable genome seed. A behaviour genome is a short vector, so the same encoding carries it, and a creature that arrives with its temperament intact is a much better thing to share. | Play | M | 2 |
| 349 | **Say plainly where the model is and is not** | A page that states it: a generative model authors tables offline, a small evolved policy decides what a being does at runtime, and no network call happens in a tick. Users of a product that says *AI life* will assume the wrong thing unless it is written down. | Prove | S | 3 |
| 350 | **Assert the runtime has no model in it** <br>needs `authorai` | A lint that fails the suite if `vr/` gains a fetch to a model endpoint, a bundled weights file above a stated size, or an import of an inference library. The invariant is cheap to state now and expensive to recover later. | Prove | S | 3 |
| 351 | **One command that runs every compiler** | `scripts/data.mjs` already runs the eight compilers. A generated-content step joins it, so the tree is either consistent or the build fails — which is the same argument the open-world pass made and the reason the pattern is worth reusing rather than reinventing. | Prove | S | 3 |

## What a thousand lives cost at sixty hertz

`MAX_ENT` is 1400, capped to 560 on Earth-like worlds, at 8 floats each — 45 KB of position data. The tick budget is stated in code: `simTick` plus `agentsTick` over 12 ms triggers `noteDroppedTicks` and the accumulator is dumped. So the whole of behaviour, population and settlement has to fit in single-digit milliseconds, at `N_ALLOWED` up to 768 — 3.5 M cells. Any design here that does not state its cost is not a design.

| # | Item | What and why | Kind | E | I |
|---|---|---|---|---|---|
| 352 | **State the tick budget for life** <br>gives `entbudget` | `simTick` plus `agentsTick` over 12 ms calls `noteDroppedTicks` and dumps the accumulator. Everything in this backlog spends from that. Write down the split — how many milliseconds for perception, decision, population, groups, settlements — before building, because the alternative is discovering the limit as a stutter. | Prove | S | 3 |
| 353 | **Measure what beings cost today** <br>needs `entsim` `entbudget` | `W._msSim` times the sim tick and `agentsTick` runs outside it, so the cost of 326 to 489 beings is currently unmeasured. Measure it at 560, at 1400, and at the resolutions in `N_ALLOWED` up to 768. | Prove | S | 3 |
| 354 | **Parallel arrays, not meta objects** <br>needs `drivevec` | `ENT.meta` is an array of plain objects with fifteen fields including a nested `plan`. Every drive, target and memory added there is another allocation and another pointer chase. Typed arrays for the hot fields, objects only for the rare ones. | Model | M | 3 |
| 355 | **A stated maximum population, defended** | `MAX_ENT` 1400, `capForWorld` 560 on Earth-like, a stride computed from the cap, and a top-up that refuses above 85%. Four limits, one design question: how many individuals does this product want to simulate. Answer it once. | Model | S | 3 |
| 356 | **Groups as the scaling answer** <br>needs `group` | A herd of ten thousand is one record. Aggregating is how a simulation shows a swarm without simulating a swarm, and the design question is where the boundary sits and how visible the transition is. | Model | M | 3 |
| 357 | **Level of detail by distance** <br>needs `utility` | Full behaviour near the camera, coarse behaviour in the mid-field, a density everywhere else. It is the only way the numbers work at globe scale and it needs to be a designed transition rather than an emergent artefact. | Model | M | 3 |
| 358 | **A spatial index that is already right** | `rebuildBuckets` builds a per-cell head-and-next linked list over beings every tick and `eachNearby` walks a cell plus four neighbours. That is the correct structure for this grid; the item is to keep it, keep it incremental, and not replace it with a tree. | Model | S | 2 |
| 359 | **Do not rebuild what did not change** | `respawnEntities` rebuilds from zero and `settleCities` rescans the whole build field every 40 years. Both are O(NC) sweeps triggered by events that change a handful of cells. Dirty-marking is the routine fix and it frees budget for everything else here. | Model | M | 3 |
| 360 | **Field passes, counted** <br>needs `guildfield` | Four trophic arrays, a predation-pressure field, a trail field and a being-count field is seven new per-cell passes. At 3.5 M cells that is not free. Count the passes, fuse the ones that share a loop, and state the total. | Prove | M | 3 |
| 361 | **A memory budget for the biosphere** <br>needs `guildfield` | Seven `Float32Array`s at N=768 is 100 MB. The open-world pass made exactly this calculation for a per-cell material stack and concluded the design has to state which it is before it is written. Same discipline, same reason. | Prove | S | 3 |
| 362 | **The GPU is already here** | `gpgpuClimateTick` runs the climate in shaders when available and `atmoMetaTick` handles the fallback. Trophic fields are the same shape of problem. The item is not to do it — it is to know the door exists and what it costs to walk through. | Model | L | 2 |
| 363 | **Behaviour on a worker** | `vr/sim/worker.js` exists. Behaviour is a good candidate for off-thread work because it reads fields and writes beings, but it must not become non-deterministic in the move, which is the whole difficulty. | Model | L | 2 |
| 364 | **Amortise across ticks** <br>needs `utility` | Not every being needs to think every tick. Round-robin a fraction of the population per tick with a stated staleness bound, which is a large constant-factor win and is invisible if the bound is chosen well. | Model | M | 3 |
| 365 | **A frame budget the player can see** | `W._msSim` exists. A performance line that breaks down climate, ecology, behaviour, population and render is what makes it possible to have this conversation with evidence instead of instinct. | Show | S | 2 |
| 366 | **The mobile case** | The product has a mobile HUD, and mobile is where a 12 ms budget is a real constraint rather than a comfortable one. Every item here needs a stated behaviour under half the budget. | Prove | M | 3 |
| 367 | **Degrade the model, not the frame rate** | When the budget is exceeded the current code drops simulated time. For behaviour that is acceptable; for population it is a leak. Decide per system and write it down, because a silently skipped birth is a bug nobody will find. | Model | M | 3 |
| 368 | **A performance regression test** <br>needs `entbudget` | A headless run that times N ticks with the biosphere at full population and fails above a threshold. The suite has 542 assertions and none of them is a clock, so nothing prevents this backlog from making the product slow. | Prove | M | 3 |
| 369 | **Garbage, per tick** | `updateFoodWeb` allocates a `nodes` array, a `links` array and a `prey` array per lineage per tick, and `settleCities` builds objects for every settlement. Sustained allocation is the usual cause of periodic stutter in a loop like this one. | Prove | S | 2 |
| 370 | **Assert the tick stays inside the budget** <br>needs `entbudget` | At the stated maximum population, on the stated reference resolution, with every system in this backlog enabled. One number, in CI, that fails when somebody adds a per-being loop. | Prove | M | 3 |
| 371 | **The cheapest possible version of everything here** | For each of the categories in this backlog, write down the version that fits in a tenth of the budget. Most of the value — a drive vector, a birth, a group with a heading, a front with a shape, a planetary scorer that reads scalars — is arithmetic, and the expensive versions are optional. | Prove | S | 3 |

## Reading a living world

The chronicle logs births of towns, extinctions and tipping points, and `viewCensus` counts kinds in the local view. What no readout answers: who is eating whom right now, which population is rising and which is falling, where the front is, which settlement is growing and why, what the biosphere is trying to do. A living world needs a running account, not a state dump.

| # | Item | What and why | Kind | E | I |
|---|---|---|---|---|---|
| 372 | **A running account, not a state dump** <br>gives `censusline` | `viewCensus` ranks cover shares and counts beings by kind. What a player of a living world wants is a sentence: *the herd moved south, the reef is bleaching, the second settlement is short of grain*. Generated from real fields by templates, one at a time, in the readout. | Show | M | 3 |
| 373 | **A hunt camera** <br>gives `huntcam` <br>needs `hunt` | `huntGlance` already exists as a camera hint in the local view. Point it at an actual predation event and the product gains its most watchable moment for the cost of a queue of recent events. | Play | M | 3 |
| 374 | **Follow a lineage through the run** <br>needs `popbook` | Pick a node in the Lab tree and get a running story: where it spread, what it ate, what ate it, when it nearly died, what it became. All of it is in the phylogeny and none of it is presented as a narrative. | Show | M | 3 |
| 375 | **A species page that includes behaviour** <br>needs `behgenome` | The life pass built a species page from the genome. Add what the animal does: diet, feeding mode, sociality, temperament, activity pattern, learning. The behavioural half of a species is currently invisible. | Show | M | 3 |
| 376 | **The ecosystem of this cell** <br>needs `guildfield` | Inspect already lists fields for a cell. It should list the community: guilds present with abundances, dominant lineages, what limits production here, and the three biggest flows of energy through this square. | Show | M | 3 |
| 377 | **A population dashboard** <br>needs `popbook` | Top lineages by population, their trends, births and deaths this interval, and the cause breakdown of the deaths. Four numbers per lineage and it is the readout that makes a food web comprehensible. | Show | M | 3 |
| 378 | **What is limiting the biosphere right now** | Light, nutrients, temperature, water, oxygen, space, predation. The model computes every one of these per cell and never says which is binding — which is the single most useful sentence a biosphere readout could produce. | Show | M | 3 |
| 379 | **The chronicle, filtered by life** | `W.chron` mixes tectonics, weather, tipping points and biology. A biosphere-only view of it is the history of the thing this backlog is about, and it is a filter on data that already exists. | Show | S | 2 |
| 380 | **Milestones that are behavioural** | `MOMENT_KEYS` has eight firsts and all eight are metabolic or morphological. First predation, first group, first migration, first tool, first taught behaviour, first settlement, first trade. Same mechanism, and these are the ones a player will remember. | Show | S | 3 |
| 381 | **A time-lapse of the biosphere** | Occupancy, dominant clade and population every N ticks, replayable. The product captures moments already; a replay is what lets a player see a 1.7 Gyr run as a shape rather than as a final state. | Show | M | 3 |
| 382 | **Show the counterfactual** | `forkRun` exists. *This is what would have happened if you had not done that* is the strongest possible readout for a god game and the fork machinery is most of the work. | Play | L | 2 |
| 383 | **A biosphere report card** | `health`, `resilience`, `habitability`, `inhabitance`, `medeaScore`, `disequilibrium` and `shannon` all exist and are scattered. One card, with each number explained and its trend, is a small piece of work with a large effect on legibility. | Show | M | 3 |
| 384 | **Tell the player what to look at** | On a planet with 3.5 M cells and a thousand beings, the interesting event is somewhere else. A notification that names it and offers to go — the storm layer already tracks named cyclones this way — is what turns a simulation into something watchable. | Play | M | 3 |
| 385 | **A glossary entry for every new quantity** | `glossary.js` exists. Predation pressure, grazing pressure, transfer efficiency, minimum viable population, carrying capacity, encephalisation — each needs a definition, a unit and a typical value, or the readouts are decoration. | Show | M | 3 |
| 386 | **Say when something is a sprite** | The realism discipline in this repo is to distinguish what is modelled from what is drawn. If ambient fauna is decoration and the herd is simulated, the interface should be able to say which, because a player who cannot tell will trust the wrong thing. | Show | S | 3 |
| 387 | **A readout that works while paused** | Most of what a player wants to understand is best examined stopped. Every panel here must be meaningful with `dtYr` at zero, which is a constraint on what can be an instantaneous rate. | Show | S | 2 |
| 388 | **The mobile version of the census** | The product has a mobile HUD and the census line is the readout most likely to be looked at on a phone. One sentence and three numbers, not a table. | Show | M | 2 |
| 389 | **A shareable summary of a run** | `serializeRun` saves a run and `seedword.js` encodes a world in four words. A run summary — the seed, the age, the tree, the biggest extinction, the settlement that lasted longest — is the artefact somebody would actually post. | Play | M | 2 |
| 390 | **Assert the readouts agree with the model** | `civPop` sums city populations, `meanBuild` samples every seventh cell, `W.trophic` derives from a global mean. Assert each displayed number equals a direct computation from the fields, because a wrong readout is worse than a missing one. | Prove | M | 3 |
| 391 | **One screenshot that shows life growing** | `site/img/` holds four screenshots and all four are Earth. The test of this entire backlog is a single image, or a short capture, where a player can see a biosphere spreading, a herd moving and a settlement growing at once. If that image cannot be made, the work is not done. | Prove | M | 3 |

## Five hundred and forty-two assertions, one about a being

`vr/sim/test.mjs` has 542 `ok(` calls covering Eigen thresholds, Kleiber scaling, chirality, genome round-trips and a 24-tick biosphere that must not collapse. Exactly one mentions an agent, a city or a behaviour. Every claim in this backlog is a claim about something that moves, and the measured facts in the audit above were all found in an afternoon with a twenty-line probe — which is the strongest argument for building the probe into the suite.

| # | Item | What and why | Kind | E | I |
|---|---|---|---|---|---|
| 392 | **Commit the probe that found these numbers** <br>gives `aliveassert` <br>needs `liveprobe` | Every measured claim in this document — zero years in 300 ticks, 38% biomass decline, zero deaths in 1.7 Gyr, one settler, 4 sprite kinds, 200 food-web links at the cap — came from a twenty-line script. Commit it, run it in the suite, and print the numbers so a regression is a diff rather than a discovery. | Prove | S | 3 |
| 393 | **The metrics that must move** <br>gives `livemetric` <br>needs `aliveassert` | Partial. `vr/sim/livemetric.js` + `livingLine` on thrive headless runs. Baselines and catalogue fleet run still open. | Prove | M | 3 |
| 394 | **A budget per metric, and a direction** <br>needs `livemetric` | Each of the eleven gets a target range and a direction of travel. A metric with no target is a number in a log; a metric with a target is a test. This is the loop the realism pass warned about and the open-world pass restated. | Prove | M | 3 |
| 395 | **One assertion about a being, then twenty** <br>needs `entsim` | One of 542 assertions currently mentions an agent, a city or a behaviour. The first twenty are enumerated across this backlog — a kill, a birth, a death with a cause, a flock converging, a front advancing, a town crossing a stage, a policy diverging on two worlds — and each is a handful of lines. | Prove | M | 3 |
| 396 | **A worked biosphere, end to end** <br>needs `livemetric` | The life pass has worked lineages as fixtures. The equivalent here is a full run committed as a fixture: seed, ruleset, tick count, and the expected population, tree, groups and settlements at the end. It is the only test that catches an interaction between two systems. | Prove | M | 3 |
| 397 | **Two worlds should not be the same world** <br>needs `livemetric` | Run the same tick count on ten seeds and assert the biosphere metrics have a stated spread. A model that produces the same numbers everywhere is not reading the planet, and the world-space pass found exactly this failure at a much larger scale. | Prove | M | 3 |
| 398 | **The fleet run, for biospheres** <br>needs `livemetric` | The open-world pass asks for 120 worlds through a render harness. The biosphere version runs the same catalogue headlessly and reports which worlds are alive, which are dead, which are identical and which are absurd. Cheap, because it needs no pixels. | Prove | M | 3 |
| 399 | **Catch the dead planet that should be alive** <br>needs `livemetric` | And the alive one that should be dead. A Europa with a surface biosphere or a Titan with an Earth food web is the biological equivalent of forty bodies rendering as Io, and nothing currently looks. | Prove | M | 3 |
| 400 | **Assert the biosphere does not decline by default** | The measured 38% fall in `meanLife` over 300 ticks on the default world is the single most important number in this document. Assert a non-declining biosphere on modern Earth under no forcing, and the whole product gets a floor. | Prove | S | 3 |
| 401 | **Assert time advances** <br>needs `unpin` | Three hundred ticks, zero years. One assertion — `ageYr` increases at every speed stop — would have caught it, and it is the reason nothing in this backlog can be verified today. | Prove | S | 3 |
| 402 | **Assert the visible layer matches the modelled one** <br>needs `entsim` | Measured: 49 living lineages, 4 distinct sprite kinds. Assert that the number of distinct visible forms tracks the number of living lineages up to a stated cap, and that the cap is a stated design decision rather than an atlas size. | Prove | M | 3 |
| 403 | **Assert reef is not the majority** | Measured: 196 of 326 beings on modern Earth were the reef sprite. Assert the kind distribution against the actual biome distribution, because the first-match ordering in `kindForCell` is producing a picture of Earth that is 60% coral. | Prove | S | 3 |
| 404 | **A determinism test for behaviour** <br>needs `entsim` | Same seed, same tick count, identical population state. `rngOf` gives behaviour its own stream, so this should already hold once beings are inside the tick — and it will not hold while they are on the render loop. | Prove | S | 3 |
| 405 | **A save round-trip for the population** <br>needs `entsave` | Landed. Save v8 round-trip in `test.mjs`: ids, cells, ages, clock, cities. | Prove | M | 3 |
| 406 | **A generate-reset test for the living layer** <br>needs `entsim` | Landed. Beings block asserts `generate` resets population and pinned vs thrive clock behaviour. | Prove | S | 3 |
| 407 | **Assert the food web is not truncated silently** | Measured `links.length === 200`, exactly the slice bound. Assert the count and log how many were dropped, because a silently cut web reads as a complete one. | Prove | S | 3 |
| 408 | **Compare against a real ecosystem, once** | The Serengeti, a kelp forest, a hydrothermal vent community: published biomass by trophic level. One comparison, honestly reported, tells you whether the trophic model is within an order of magnitude — and this repo has a strong record of that kind of check. | Prove | M | 3 |
| 409 | **Population cycles, against the published ones** | The lynx–hare cycle is the most-reproduced dataset in ecology. If the predator–prey model produces a cycle, its period and amplitude can be compared to it — and if it produces none, that is the finding. | Prove | M | 2 |
| 410 | **A scenario that demonstrates each claim** | One scenario per category: watch a front cross a continent, watch a cascade after a cull, watch a herd migrate, watch a town become a city, watch a policy evolve. A claim with a scenario attached is a claim somebody can check in a minute. | Play | M | 3 |
| 411 | **Print the scoreboard on every run** <br>needs `livemetric` | Partial. Thrive headless runs append `livingLine`; probe prints full census. | Prove | S | 3 |

## Sequencing

1. **Commit the probe, then assert time advances.** `liveprobe`, `aliveassert`. The numbers in this document took twenty lines. Commit them and add the one assertion that would have caught the headline: `ageYr` increases at every speed stop.
2. **Move beings inside the tick.** `entsim`. One call moved from the render loop into `simTick`. It puts the visible layer into headless runs, into saves and into tests, and roughly a third of this backlog is blocked on it.
3. **Unpin the clock and give life its own rate.** `unpin`, `biotime`, `livespeed`. A world that can advance, a biology clock separate from the geology clock, and sub-stepping so the cheap systems run often. Without this nothing else here is observable.
4. **Give a being an inside.** `energy`, `drivevec`, `percept`. Energy, a handful of drives, and perception gated on what the animal can sense. `pickBehav` and its five strings are deleted at the end of it.
5. **Then birth and death.** `birth`, `death`, `popbook`. Counted individuals, causes of death, and an accounting identity that can be asserted every tick. This is the item that turns a rendering into a population.
6. **Then hunting, where it happens.** `hunt`, `preyfield`, `carcass`. A predator, a prey on a cell, a pursuit that can fail, an energy transfer, a carcass, and a predation-pressure field. Local grazing lands here too.
7. **Then groups, with a heading.** `flock`, `group`. The alignment term that is missing, and a group record with an identity so it can be named, followed, drawn as a mass and sent on a migration.
8. **Then the trophic field.** `guildfield`. Four per-cell stocks instead of three multiplications of a global mean, guilds above the microbial layer, mutualism, and decomposition that closes the nutrient loop.
9. **Then the front.** `front`, `disperse`. A colonisation edge with a shape and a speed, dispersal with a distance, barriers and corridors from terrain that already exists. This is the picture the phrase *life growing before your eyes* describes.
10. **Then show it, on both surfaces.** `tileact`, `behovl`, `globeswarm`. Behaviour drawn in the grid of squares, rates and movement on the globe. Twenty categories of model are worth nothing if the two views still show state.
11. **Then settlements that grow.** `citysim`, `hinterland`, `roads`, `trade`. Unthrottle the loop, give a town a persistent identity, feed it from a catchment, connect it, and let it fail with a stated cause.
12. **Then the policy, and let selection have it.** `utility`, `actiontable`, `behgenome`. Scored actions from an authored table, weights in the genome, temperament that speciates with the body. The generative model authors the tables offline and never runs in a tick.
13. **Then learning and culture, the two fast loops.** `lifelearn`, `culture`. An individual that changes because of what happened to it, and a behaviour that passes between individuals without a genome. Both are visible in seconds rather than eons.
14. **Then give the planet a drive.** `planetclaim`, `gaiapolicy`, `pdrive`. Name the three modes so Daisyworld stays a proof, replace the four if-statements with the same drive architecture beings get, and act through the player's verbs on the player's budget.
15. **Then a metabolism you can see, and organs that can fail.** `metab`, `homeo`, `tipgeo`, `gaiaface`. Carbon breathing on the disc, Walker and ice-albedo that are physics rather than clamps, tipping elements as places, three analog gauges. A mind with fake vital signs is a cartoon.
16. **Then the planet notices the hand, and can fight.** `planetsee`, `immune`, `planetmood`. Awareness without cities, an immune stance from `playStyle`, wounds with clocks, a mood that is a projection of numbers. Fiction switch on for a voice; silence the default.
17. **Then minds that are not Earth's.** `alienmind`. Gate Gaia on air, water and a biosphere; give Titan a haze, Europa a seam, Venus a failure, Mars a scar; measure the Gaian bottleneck across the catalogue.
18. **Then measure all of it, on every world.** `livemetric`. Eleven numbers, committed as baselines, with a direction of travel, run headlessly across the catalogue. And one screenshot in which a biosphere is spreading, a herd is moving and a settlement is growing at the same time — and a second in which a planet answers a pulse with ice and green, no HUD — which is the only real test of whether any of this worked.

The through-line: this product has a genuinely good biosphere model and a visible layer that is not connected to it. The genome spans 1.6 × 10²⁸ bodies and `agentsTick` reads two fields off it. The food web computes chirality-gated trophic links and applies them to one number per lineage for the whole planet. `sensory.js` integrates Planck spectra against atmospheric transmission and nothing that acts asks it anything. Closing that gap is most of this backlog, and almost none of it is hard — it is arithmetic that nobody has written because the layer it belongs to was never in the simulation.

The one thing that must go first is time. Three hundred ticks and zero years is not a tuning problem; it means the default experience of this product is a still picture of a declining biosphere. Unpin the clock, give life its own rate, sub-step the cheap systems, and every item below becomes something a player can actually watch happen.

And the agentic part is smaller than it sounds. A drive vector, an authored action table, and the weights that score them carried in the genome gets you life that behaves, adapts and diverges — selected by the same process that already shapes bodies, deterministic, offline, and under a tenth of a millisecond for the whole population. A generative model is genuinely useful here, at authoring time, compiled to frozen data with a provenance tag and a review gate, exactly as `lifegrammar.mjs` already does for the life grammar. Those two sentences are the design for beings; the rest is doing it and measuring it.

The planetary mind is the same design one level up. `gaiaTick` is the sensory stream, the tips and the rates are the drives, the god verbs are the actuators, the energy budget is the metabolic cost of thought. Wire them, label the mode so Daisyworld stays a proof and a talking Earth stays a fiction you opted into, paint the metabolism and the organs on the disc, and let the planet notice the hand. Watson and Lovelock never claimed a mind; this product should not either unless the switch is on. Silence is the default across a hundred and twenty worlds. Speech is rare. That rarity is the claim.

